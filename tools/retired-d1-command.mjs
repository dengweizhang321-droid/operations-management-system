console.error("This D1 maintenance command is retired. Business maintenance must use the Django/PostgreSQL domain operator. Historical D1 helpers are reserved for isolated audit/test imports.");
process.exitCode = 1;
