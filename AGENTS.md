# TERUISI 运营管理系统协作规范

2026-09-16，11 条正式数据工作流已按用户要求完成早班调整。吉客云 `J8kY2mQ5vR7sT4pN` 保持 00:10；京东四店 10:00、京东市场 10:30、京准通商用设备 10:40、切肉机 10:50；天猫亿玖、丽力、拓丰、炊之王、马思图、亿用依次为 11:00–11:50，每店间隔 10 分钟并继续共用原子 helper 排队。全部工作流 `active=true`、`versionId=activeVersionId`，8 条变更流程的最终发布事件为 activated，节点名称与 cron 一致；n8n 健康 200，未重启 n8n/Django、未迁移数据库，也未触发下载或导入。编辑切肉机计划节点时产生的手动 execution `2446` 仅执行计划触发节点 8 ms，未到达协调、helper 或业务节点。Codex heartbeat `automation-5` 改为每小时 05/15/25/35/45/55 分运行，只检查已超过计划时间 5 分钟的流程，按日维护 OPEN/CLOSED 并跟踪现有 execution/共享安全重试，状态不变时保持安静。52 项相关测试通过；下一次自然早班执行是端到端终验。见 `docs/evidence/data-workflow-morning-schedules-production-20260916.json`；本记录不授予后续停服或手动补跑授权。

2026-09-16，吉客云五表平台时钟偏差修复已获用户明确停服与重跑授权并在本机受控采用。源码 `be2c4dff85ba80e2c8b1020dd04b1d25c158f29c`；Worker/helper `20260916T004429Z-914353db1e3250b3`，manifest SHA `223db866221ab8961b8c25431b4aeba6d28ab59aaa3639c49b5883c85091822f`。根因为平台 HTTP Date 比本机接收时刻快 901 毫秒，旧代码用平台时钟确定任务下界、却直接用本机时钟确定观察上界，形成假窗口倒置；任务观察和下载交接现统一换算到同一已验证平台时钟。旧 execution `2285` 只已提交唯一库存任务 `sys-113011002`，通过 create-only、30 分钟、单 execution 消费的许可由新 execution `2437` 复用，未重发库存 POST；其余四表正常新建任务。`2437` 于 08:54:23–08:56:42 成功导入货品 8,504、库存 22,787、库龄 5,683、销售 13,628、组合装 4,431 行；库存/库龄快照 9 月 16 日，销售覆盖 9 月 1–15 日，五个精确批次独立 API 回查通过。过期等待重试 `2431/2433` 已在 n8n UI 取消，避免成功后再次完整导出。最终 Running/Ready/exact_release、12 组件、helper idle、启动绑定、发布前备份独立恢复及前后 E 盘三文件逐项摘要归档通过；无迁移、Django 未重新部署、n8n 未重启。详见 `docs/JACKYUN_SESSION_API_EXPORT.md` 与 `docs/evidence/jackyun-clock-recovery-production-20260916.json`；本记录不授予后续停服授权。

2026-09-16，京东商智新版行业榜单已在用户授权短暂停服后受控采用。最终源码 `9a3ba905`，Worker/helper `20260915T162551Z-78d9ad08f15648ec`，manifest SHA `5db41a3324bbd93a91ce6174c26213d10b0adf73ba2a4bb72541081c559f36b7`；Django manifest `ec56c6b5d5f327a45f980b567ef06bccfed84973388100f66511e5a224bb1856` 不变、无迁移、n8n 未重启。入口改为 `industry-top.html`，类目/SKU 后须查询，单日自定义须两次端点点击并校验回显/退出日历；页面原生 XLSX 下载严格绑定商品热销/SKU/POP/全部渠道/三级类目/单日，拒绝旧 v3 计划与 CSV 恢复。n8n `2289` 于 00:13:06 完成七类目共 1,400 行，数据日期为原 9 月 15 日计划的截止日 **9 月 14 日**；跨日恢复不扩大日期范围。最终唯一普通公告自动关闭经隔离浏览器验证，不能关闭验证码或安全验证；实际成功业务使用前版 `4dda3816`，公告当时人工关闭，未为最终公告补丁重复下载。79 项相关测试、不可变构建、正式启动/版本绑定、独立覆盖及备份恢复/E 盘归档通过；全库类型检查 220 行既有错误与线上基线一致，无新增错误。维护期周报检查 `2292/2293` 失败、`2294` 自然成功，未补发。见 `docs/京东市场商品榜单SKU日数据n8n工作流.md` 和 `docs/evidence/jd-market-new-ui-production-20260916.json`；本记录不授予后续停服授权。

2026-09-15，广东仓与 Worker 存活自愈已获用户明确停服授权并在本机受控采用。源码 `5e3cdeb7658542c68c3d8cee501af2eadb58efc7`；Worker/helper `20260915T143857Z-c71f254c9e28b6f2`，manifest SHA `09f3e18d9d7e3f9d8cb50255b0c946e0c60982963cc3ce6d121b6fd1bf7aa27a`；Django manifest SHA `ec56c6b5d5f327a45f980b567ef06bccfed84973388100f66511e5a224bb1856`。广东仓仅在精确 `on_hand_quantity` 较上一当前成功快照严格增加时清除人工健康并结束旧备货跟进；下降/不变保留，先降后升可触发，缺失仅建基线，历史补传、重复导入、其他仓或仅可用/在途变化不触发；该变更与审计在库存发布事务内回滚。下单剩余库存仅按最新非取消备货量减下单日后完整每日实物库存正向增量核算，下降不抵扣、可为负数，缺日/重复/超 366 天返回待核算，不当作入库流水。不可变 Worker supervisor 保留 3072 MiB 堆适配，只用不依赖 Django 的 loopback liveness 连续 14 次失败触发所属进程树终止与恢复；readiness 失败不重启，导入 120 秒有界请求不得被过早中断，10 分钟最多 5 次后失败关闭。正式 42 条广东仓 API 新字段、Running/Ready/exact_release、12 组件、启动绑定、守护进程及钉钉 connected 通过；发布前后备份均独立恢复并逐文件归档 E 盘。维护期既有每 5 分钟周报检查三次因 PostgreSQL 停止而失败，服务恢复后的自然检查无错误，未补跑；n8n 未重启。见 `docs/INVENTORY_MANAGEMENT.md`、`docs/WORKER_LIVENESS_RECOVERY.md` 和 `docs/evidence/guangdong-worker-liveness-production-20260915.json`；本记录不授予后续停服授权。

2026-09-15，店铺年度目标 Excel 导入及四项进度已获用户明确停服授权并在本机受控采用。最终源码 `71ce54cb`；Worker/helper `20260915T113701Z-5c7c6173998295bb`，manifest SHA `d2bc10037f5d3cd70b9e7037f8ad2acd70d5104d12c8ee56adb93720214fa7f6`；Django manifest SHA `788cd95480c09af4fcedff2c246998b92e4d0e06353d51051c55b29bd6cdc969`。`finance.0002_finance_target_gross_margin` 已迁移；导入固定首表及六列表头，金额万元转分、比率转基点，按财报/既有目标的权威店铺身份匹配，仅允许唯一的去尾括号别名，歧义或未知店铺整批回滚。两次正式导入均因“京东-志高商用设备旗舰店”与权威名称/分组不一致而原子失败且零写入，补齐全名唯一别名后最终导入 21 家、跳过 6 行全空目标；未列目标不删除。年度进度按成功月度财报累计销售/利润，并按金额加权计算大毛利率/推广费率，不平均月比率。最终 Running/Ready/exact_release、启动绑定、全部 12 组件、钉钉 connected、前备份独立恢复、发布后备份与 E 盘三文件摘要归档通过；n8n 未重启，20:05 自然启动的既有新品周报不属于本次发布，未干预并已自然成功。详见 `docs/WORKFLOW_ANNUAL_TARGETS.md` 与 `docs/evidence/store-annual-target-import-production-20260915.json`；本记录不授予后续停服授权。

2026-09-15，三项竞态修复已获用户明确停服授权并在本机采用。源码 `0452a15e`，Worker/helper `20260915T081612Z-730408eeaa9cbc1c`，manifest SHA `b89d4354929695d55cd818b44719a43bbae37f34be663c4cc632d3fc81212929`；Django manifest SHA `3ecceb7a0ba67f175b4513a751349c169ba1f303016181a6d3bd43b4434cc931`。完整 Stop/Restart 必须由 Worker 唯一引擎连续持锁覆盖后端，Django 核心与子域保持同线程连续互斥；多步维护使用 `EnterMaintenance`/精确 ID 的 `ExitMaintenance`，异常后保留标记，不因 PID 消失或超时删除。DeployApp/RollbackApp 须核验 Worker/helper 端口和 supervisor 回执已清除；首次引导协调旧入口、精确恢复前驱入口后进入维护，再由 successor 安装新版，不降低摘要门禁。周报隔离迟到数据/错误/loading，写后刷新当前周；Prompt 空类目也在事务锁内分配版本，保留唯一约束和审计回滚。实际维护启动拦截、正式页面/API/资源、Running/Ready/exact_release、守护 healthy、钉钉 connected、前备份独立恢复和前后 E 盘归档通过，59 条迁移清单不变。维护期 n8n 周报检查 2186/2187 失败，2188 自然成功，未补跑或重启 n8n。详见 `docs/RACE_CONDITION_FIXES.md` 与 `docs/evidence/race-fixes-production-20260915.json`；本记录不授予后续停服授权。

2026-09-15，市场榜单分页及错误恢复已合入 main 并在本机采用。Worker/helper `20260915T054303Z-5704048d5158895c`，manifest SHA `821d7d88314910fda858abf8ce90ace40121c8d370f42ce1646f4a9465054de0`；补充 Django 源码 `2c8d1059`，manifest SHA `f0d4dc882cdc6bbf235d8107bdb92100b87ed89018ea9a90e2ab31bfa8233def`。数据库内去重、筛选、投影排序后只取当前页，关联销售也只查当前页；价格带按不同键计算，网店投影只聚合一次，页身份确定后计算其历史排名。失败保留导航/日期/筛选，独立选项读取，每页20条替换显示，30秒只读期限覆盖响应体。正式31.8万条全范围、第二页及未确认价格筛选均200、约12–13秒，浏览器通过；最终隔离PostgreSQL66项及真实角色负向探针通过。无迁移、权限扩展或SQL超时放宽；行业汇报仍有界，主数据列表与长期内存增长未在此解决。第二轮在两次排空后切换，Running/Ready/exact_release、Django守护healthy、钉钉connected及前备份独立恢复/后备份/E盘归档通过。首次接收器stream连接失败后由既有控制器恢复，未重放；首轮维护天猫2146失败交原n8n重试，n8n未重启。隐藏启动Windows PowerShell守护应使用其系统模块路径和受保护runtime工作目录，避免继承不兼容模块路径。见 `docs/MARKET_QUERY_PAGINATION.md`、`docs/evidence/market-query-recovery-production-20260915.json`。

2026-09-15，数据工作流 Codex 监控已合并为单一 heartbeat `automation-5`，每天上海时间 14:25 统一核验吉客云五表、京东四店、京东市场、京准通两店及天猫六店共 11 条现行正式 n8n 工作流。原 `automation`、`automation-2`、`ai-n8n`、`11-n8n` 与每小时 `ai` 均暂停后续调度；n8n 共享错误工作流及 60 分钟完整 execution 安全重试保持不变。统一监控默认禁止子 Agent，先用有界只读批量快照，只有异常才读取对应专项手册；只做脱敏诊断、终态核验和既有通知，不在监控任务中修改源码/配置、部署、迁移或启停服务。数据库备份与钉钉事项归档不属于数据工作流监控，继续独立运行。

2026-09-15，单条钉钉图文已在用户重新明确授权短暂停服后受控上线。源码 `4d0757cd`；Worker/helper `20260915T031124Z-50915712d427032f`，manifest SHA `6b5a86de7e2d57b7bc73819890af62fa12cf474f2cb46edcd97863e332971b78`；Django manifest SHA `d1305a381c2dd259fe3c04bd880d14100773d3ee36c21c55777950cbccdad138`。截图非空文案使用企业机器人 `sampleMarkdown` 与内部 MediaId 合成一次消息调用；上传后复验版本/权限，失败不拆分、不重放。原任务 v3、09:00、目标和历史保持原样，无迁移。Node 2167 通过、隔离 PostgreSQL 230 通过、页面 20 项及正式编辑页通过；Running/Ready/exact_release、接收器 connected、Django 守护 healthy。尚未触发真实新版消息验收。维护时需先协调其他活跃任务暂缓共享服务恢复；此次京东监控并发启动旧 Worker 导致首轮目录移动中断，经原文件逐项摘要恢复后重新受控部署成功。后台守护须以受保护 runtime 为 WorkingDirectory，避免占用开发 worktree。证据见 `docs/evidence/scheduled-single-message-production-20260915.json`；此前审批拒绝记录保留在 candidate 证据中。

2026-09-15，定时截图固定文案已合入 main 并在本机采用。源码 `4507d966`，Worker/helper `20260915T022221Z-62869892e2c72ed9`，Django manifest SHA `31cae8dd96cb323ee9ecb39bdcef335a71890aecd873914142780bb5339fae58`。截图任务复用 `prompt` 保存最多 4000 字原文，不调用 AI；先准备截图，再由同一机器人依次发文案和图片，第二次发送前复验版本与权限。部分发送和未知结果不自动重放，无迁移。原新品周报 v3 标题为“新品周销量趋势数据”，原目标/时间/历史保留；真实组合未立即投递。Node 2167 通过、20 跳过，隔离 PostgreSQL 227 通过、2 跳过，页面 20 项及正式编辑窗口回读通过；备份独立恢复、发布后备份/E盘归档与 Running/Ready/exact_release 均通过。见 `docs/AI_DINGTALK_SCHEDULES.md`、`docs/evidence/scheduled-caption-production-20260915.json`。

2026-09-15，上新跟进“钉钉周报表格预览”完整截图已合入 main 并在本机采用。源码 `e09a46da`，Worker/helper `20260914T174729Z-5d49f49fa3939989`，manifest SHA `695bb18f3e139c97d342371d7569cfd5782b80321a46a0bd3918d9d7481d3c7d`；Django manifest SHA `39020d49c6e18331b00ef9acfd971ff57cf0688b5a28d64ce2a0131c811b6c39`。来源 `workflow:launch-followup` 固定上海最近完整周，等待精确日期、完整行列和图片/字体，再截取全部表格；不裁切超限内容。原“新品上新周销量趋势”由文字改为截图 v2，保留每天 09:00、本人目标和历史；截图与真实机器人投递分开验收。本机周报 12 行/6 周完整截图通过，225 项 PostgreSQL 通过/2 跳过，20 项页面通过；Node 2166 通过/1 超时取消/20 跳过，超时所在文件独立复测 10/10 通过。无数据库迁移；详见 `docs/AI_DINGTALK_SCHEDULES.md` 与 `docs/evidence/weekly-table-capture-production-20260915.json`。
2026-09-15，统计卡片筛选/再次取消、编辑产品线学习代码、新品店铺规划/可见备注、店铺全年销售/利润目标已合入 main 并受控采用。Worker/helper `20260914T154612Z-36b6fd5a8dbe31f7`，manifest SHA `47ce8f8f6ff8a5993d4f9d5fb60b167099c4729d3124ac2ae2036d3e9c3b5cd2`；最终后端源码 `1abe2a1c`，Django manifest SHA `d68b74c2e098df145c5fe5b16ae0d6c5cc215e711e9c468c129ad748883e843a`。仅分类卡片筛选，纯金额/比率继续展示；年度目标按平台+店铺整年设置，实际按成功月度财报累计，不混入月度目标或报表年累列。`workflow.0008_new_product_shop_plan` 新增 nullable 文本，NULL 回显旧规划，空串可清空展示但不删除旧 targets。学习 GET 必须注册生产 reader URLConf 并拒绝 POST，保存仍走 writer；不能只测 development 混合路由。最终 PostgreSQL169项、页面/API/资源、权限、25张可读业务表原行摘要、Running/Ready/exact_release、备份独立恢复和E盘归档通过。完整 Node 中一项无关浏览器超时已单独重跑7项通过，20项原有跳过；未保存业务记录或触发通知，n8n未重启。见 `docs/WORKFLOW_ANNUAL_TARGETS.md`、`docs/SUMMARY_CARD_FILTERS.md` 与 `docs/evidence/workflow-annual-cards-production-20260915.json`。

2026-09-14，AI 定时任务截图及已复核 Excel 报告按钉钉企业机器人身份发送的能力已在本机受控采用。源码 `997f307e10c5e88d84f3edae26e83b53be52fab3`；Worker/helper `20260914T143237Z-3aaaa72257429512`，manifest SHA `d473be7a6bddad5642b713117888df7a3998cd78667aba5c2e4f683a1a706960`；Django manifest SHA `1c3463cc851571da8fbaf8e4592ba8329f41d47a1512ce03add27ad74974c33a`。AI `0013_dingtalk_schedule_media` 已迁移；Chrome 专用登录目录在受保护 runtime 同级的独立 ACL 树，正式 BI 截图成功。原 2 条文字任务与 5 条历史执行保留，未建媒体任务或真实图片/文件投递，因此平台受理及客户端呈现未验收。发布前备份独立恢复、发布后备份验证和 E 盘三文件逐项摘要归档通过，最终总控 Running/Ready/exact_release、AI reader/writer/接收器运行。见 `docs/AI_DINGTALK_SCHEDULES.md` 与 `docs/evidence/dingtalk-scheduled-media-production-20260914.json`。

2026-09-14，报告模板、文字 Skill 与固定步骤 AI 流水线第一阶段已合入 main 并在本机采用。源码 `13ad0770cb51ab3f6ae3f8c01dc34fcfd63e1db7`；Worker/helper `20260914T103055Z-476613e9da6a6b1d`，manifest SHA `31d59a8a5c188309f10cfec4e670855a46747914a891dfb08ed0b170e66b4292`；Django manifest SHA `877bcb1060acd418bcf50bba270a076982b3a63cb602b37de3f7b01b95dec91e`。AI `0012_report_library` 增至56张自有表，原52张表完整行摘要不变，新四表为空；资源库默认v0无需生产种子写入。任务固定模板、技能和范围，复用Agent/DAG，人工复核后从持久证据交付HTML/Excel，通知单独确认，未知结果不重放。首版仅文字技能和手动固定步骤，不导入脚本/ZIP、不增加定时报告或自动业务写入。最终Node 2165通过/0失败/20跳过，隔离PostgreSQL 220通过/3跳过；正式页面、资源字节、权限拦截、启动绑定及Running/Ready/exact_release通过。市场任务自然完成后排空，Django守护与原钉钉接收器已恢复，n8n未重启；保留同期财务修复和3072 MiB堆设置。共享发布前备份独立恢复、本次复验及发布后备份/E盘归档通过；真实付费报告生成与通知投递未验收。见 `docs/AI_REPORT_LIBRARY.md` 和 `docs/evidence/ai-report-library-production-20260914.json`。

2026-09-14，workerd 老生代堆上限已受控调整为固定 3072 MiB。源码 `cda760a`，集成 `6abf234c`；Worker/helper `20260914T100626Z-4462809b1aa7a958`，manifest SHA `0e0b52c8ef93d71eee880c0f0711b83068a3b934e57fca19d04f67d7b8b5896b`。Miniflare 4.20260515.0 的 postinstall 适配绑定原始与目标文件摘要，supervisor 每次启动/重启固定参数并复验；禁止用只影响 Node 的 NODE_OPTIONS 冒充内部 workerd 堆调整，也不得在正式 release 原地改依赖。未来升级依赖必须显式移除或复验该适配。3072 MiB 是各 V8 老生代上限，不是进程 RSS 或全栈共享预算；未根治内存增长或自动恢复缺口。2163 项单元、20 项页面、隔离实际配置与请求验证通过；发布前备份独立恢复通过。原任务自然完成后两次排空，保留隔离记录，数据库/Django/n8n 未重启；系统 Running/Ready/exact_release。详见 `docs/WORKER_HEAP_LIMIT.md` 及 `docs/evidence/workerd-heap-3gb-production-20260914.json`。

2026-09-14，市场 AI 旧图候选阻塞批量入库修复已合入 main 并在本机采用。源码 `75c73f748de00eb2dfec9fbe45ecdab92fefdfac`；Worker/helper `20260913T162304Z-3733181f1543a083`，manifest SHA `2c9cfc1661a48c66e4f3bfa805040775d7be76083829cf63506445b944dcb8b8`；Django manifest SHA `c5651c760123f4652301b3bceaa55b2e0b6ee1bb6cfd3f980bd96ca8c2943411`。复核、数量、选择与入库共用精确月份/身份/当前图片哈希；旧图不可选或编辑，入库先筛最多 500 条有效项并锁快照复验，回执记录跳过数，全部失效有界零入库。保留旧图人工复核，不调用尚未实现的 rebuild API，不自动重放模型或未知结果。镜像 PostgreSQL 61/61、全量 Node 2147 通过/0 失败/20 跳过、正式页面/API/资源字节、Running/Ready/exact_release、启动绑定及备份独立恢复通过。生产验收不执行人工入库；4 条原计划先暂停并两次排空，再停启发布，恢复后自然新增完成 71 条，74 条隔离完整行摘要不变。无新迁移、角色变更或 n8n 重启；主数据默认列表超时与既有连接容量告警单独保留。详见 `docs/MARKET_ANNOTATION_RELIABILITY.md` 和 `docs/evidence/market-stale-candidates-production-20260914.json`。

2026-09-13，市场批量入库审计日期序列化修复已合入 main 并在本机采用。源码 `0b96b86cd23f44f40ab6d557fedc0819a4069d1c`；Django manifest SHA `bb1d44779115d8b0fdc5b8caceae9b3b14a7f9e725c2039bb4bd2a650927354d`，Worker/helper 继续使用 `20260913T143708Z-cd14607ae3bd71e3`。正式标注旧值快照中的 ORM 日期须转换为保留时区和微秒的 ISO 字符串，再写入 JSON 审计；其余类型、空值和版本不得改变，禁止删减审计以回避序列化失败。真实 PostgreSQL 市场 55 项测试及 reader/writer 权限验证通过，无迁移。停机前单次 `activeClaims=0`/helper 空闲快照不能阻止之后的定时领取；后续维护须有受控停派与在途任务排空的证据，结果未知项继续隔离，不得自动重放。采用、备份恢复与本次切换竞态见 `docs/evidence/market-annotation-commit-json-production-20260913.json`。

2026-09-13，市场标注读取恢复与 AI 回复黑色字体已合入 main 并在本机采用。源码 `8f93ec2f5beea4475656da514f4d60546e73c5e9`；Worker/helper `20260913T143708Z-cd14607ae3bd71e3`，manifest SHA `fe1255754246e12c3f2c5264ed4a06b7d032766943a3f4292a75e4fb070deb6f`。进度与复核采用独立错误通道，只有同通道最新请求成功才能清除错误，后台刷新不能清除写操作错误；读取及响应体限 30 秒，切换任务/筛选和卸载须取消并隔离迟到响应。重试仅限只读，不重放创建、入库或模型调用。无数据库迁移、Django/n8n 重启；正式资源、标注 API、Running/Ready/exact_release、发布前备份独立恢复及发布后备份复验通过。最初 503 原因未复现，主数据列表超时仍未解决。见 `docs/evidence/market-annotation-refresh-production-20260913.json`。

2026-09-13，AI 对话布局与配置设置第一阶段已合入 main 并在本机受控上线。源码 `5520b3d7d51e3c32fbf90898fb3baac508335894`；Worker/helper `20260913T083423Z-5b9bc1d755228cae`，manifest SHA `dcba1b2342da4163cc25de91f9a7140b94597eb8a646bde29fc1e61f7dc5e06c`；Django manifest SHA `d6bbf9f727475c7dab0a347f33a6ccc1523604bab55af074ba3659644c6d211e`。AI `0011_prompt_settings` 与 reader/writer grants、健康及备份清单已采用，AI 自有表52张；原51张AI表迁移前后摘要一致。默认六类口径已通过正式API保存为v1并由页面刷新回读，旧版本覆盖409、跨站写403、无签名内部读401。普通聊天、板块、钉钉和定时问数共用每次提问固定快照，从下一次提问生效；权限与计算公式继续由代码控制，独立Agent/DAG不接入。持久会话工作区/长任务仍为第二阶段，不扩大沙箱边界。整栈Running/Ready/exact_release；付费模型完整对话效果未验收。具体实现、备份恢复与采用证据见 `docs/AI_PROMPT_SETTINGS.md`、`docs/evidence/ai-chat-settings-production-20260913.json`。

2026-09-13，市场 AI 标注多计划派发与跨页复核已合入 main 并在本机受控上线。源码 `2a1867c244fc1c8d3b4cb55675b8dd71d11044a7`；Worker/helper `20260913T024434Z-bc351d47b7209e2b`，manifest SHA `33d3c8c0e7b7d2809a2076f983ca5ff5b5016d70cd0c0f9026384e5c057b16e0`；Django manifest SHA `77251eba11f4cc925e323bcafcbd52a5c05ac24ce686cf575887ce0ec66903b2`。调度必须复验真实创建人，公平选择最多 5 条计划、共享最多 50 个并发通道；准备完成后才开启 10 秒领取窗口。不存在过期推理时跳过关联积压扫描，避免持有模型锁拖慢派发；同模型共享限流退避，同图跨月复用，未知结果不得自动重放。已完成单条结果可跨页筛选复核，每次入库最多 500 条，继续保留资格、版本、权限及审计校验。镜像 PostgreSQL 52/52、全量 2138 通过/0 失败/20 跳过，正式 Running/Ready/exact_release、资源字节、5 条原计划自然推进、发布前备份独立恢复和发布后备份复验均通过。无数据库迁移、n8n 重启、历史失败重置或人工批量入库；既有启用计划自然恢复包含真实模型调用与结果写入。“主数据与价格”默认列表另有约 30 秒超时，尚未解决，不能据此宣称市场全页面性能通过。见 `docs/MARKET_ANNOTATION_RELIABILITY.md` 和 `docs/evidence/market-annotation-reliability-production-20260913.json`。

2026-09-13，钉钉接收器随系统自动启动已合入 main 并在本机受控启用。源码 `8b25c7005b6d6af4e04ee75937f2496b257ebf0e`；Worker/helper `20260912T234549Z-017f8196dda91b2b`，manifest SHA `908f4920a699240cbd241ae442eb2e6267baef470d976e09a8b8cadfef586e48`；Django manifest SHA `8c87f48c44b850baa0c426d00ef26970db671d656ebdb7ffd1d558e88d0beaa5`。受保护 `config/dingtalk-startup.json` 必须绑定接收器配置摘要和 AI authority；唯一系统 Start 引擎在后端与 Worker HTTP 就绪后调用 `AutoStartDingTalk`，继续保留服务 mutex、进程身份和 PostgreSQL 单例。`EnableDingTalkStartup` 只核验并保存配置；`StopDingTalk` 同时持久停用自动启动；整栈 Stop 保留配置，单次 `StartDingTalk` 不取消持久停用。已验证系统从停止状态自动拉起并 connected、重复 Start 复用原进程及启动绑定；没有实际重启 Windows，不新增无人登录运行、接收器崩溃自动恢复或过期任务补发。见 `docs/AI_DINGTALK_SCHEDULES.md` 和 `docs/evidence/dingtalk-receiver-autostart-production-20260913.json`。

2026-09-13已按固定计划受控清理137份旧Worker `node_modules`，D盘可用空间净增加99.899 GiB；当前版本、最近两个前驱、另11个链外版本的依赖，以及全部151个发布目录和版本链仍保留。删除后完整链、当前版本及Running / Ready / exact_release复验通过，见 `docs/evidence/storage-dependency-cleanup-20260913.json`。这些历史依赖目录缺失是已验证的归档状态，不得作为损坏批量重新安装或据此删除整个旧发布目录；恢复必须绑定原始依赖树摘要、从私有归档完整下载/解包验证后按受控路径处理，恢复依赖不授予旧版启动资格。业务数据库及旧备份保留规则未改变。

用户于2026-09-13指定业务数据库备份副本保存到 `E:\运营管理系统业务数据`；首次新备份已复制校验，每日22:30由“清理无用文件释放D盘空间”任务中的自动化（ID `e`）生成并归档，依赖电脑、桌面应用及数据库正常运行。受保护程序仍先在D盘生成备份，D盘旧备份未删除，业务数据库未上传GitHub。详见 `docs/STORAGE_RETENTION.md`。

本机存储保留：GitHub归档必须使用独立私有仓库，禁止向源码仓库上传业务数据库、附件、配置和恢复密钥。2026-09-13已按用户要求将账号全部4个仓库设为私有并核验仅所有者具有协作者权限，个人资料亦已设为私有；后续新仓库默认私有，不主动添加他人访问权限。旧Worker依赖只能按完整验证链规划，保留当前与最近两个前驱，按原始依赖树摘要归档并完整下载/解包验证；正式清理须持有生命周期互斥、逐目录复验与进程引用检查、写持久审计，仅删除精确历史`node_modules`，保留所有manifest、回执、fence、successor、源码、dist及helper。数据库云端归档须先完成认证加密、独立离线密钥与隔离恢复验收，尚未满足时不缩短原本机保留期。详见 `docs/STORAGE_RETENTION.md`。

2026-09-12，绿色主题与跨模块沙箱修复已合入 main 并在本机采用。源码 `9237d53ebba8b06886d473f7f8236b3f8d0644ca`；Worker/helper `20260912T143827Z-79e971ff2b5bce03`，manifest SHA `35bd57a7ac3025fcafa85ae15e0062c0c244357c76bc6acd9eaa5a97921b547a`；Django manifest SHA `1a2e4773dc874f8303b6b1104862b3ba6806921f53fe1c8ae08a50570d3d19e1`。公共 UI 使用 AI 对话共享森林绿主题；沙箱仅移除配置页面，JSON/pandas 后端、权限与审计继续保留。跨 app 导出必须逐页验证完整性，仅可显式投影授权标量列，不放宽 3 数据集、2000 行、2 MiB、20 页等边界；结果允许中文单位列，保留 CSV 公式防护。无迁移、无 broker 镜像变更；正式状态、资源、签名探针/合成容器计算、发布前独立恢复及发布后备份复验通过。付费模型自然语言端到端尚未验收。证据见 `docs/evidence/unified-theme-cross-app-sandbox-production-20260912.json`。

2026-09-12，顶部单行导航、冻结模块 Tab、“章”账号菜单及统计周期新增选项已在本机采用。源码提交 `627ea7f104d4bcaa08fc989c31a64413ab1bc590`，Worker/helper 为 `20260912T095027Z-43c06ff99569edfa`，manifest SHA 为 `cf2bc75ea8030818e99aae06ea12201adc06b45eb76fb89d7b91efc879a9bb66`。近30天包含上海当天；去年同期以当前选中区间回退一年、重复选择不递减、闰日夹至二月末，并保留 URL 日期与原操作能力。全量 2121 通过/0 失败/20 跳过，正式资源字节、Running/Ready/exact_release、启动绑定、发布前备份独立恢复和发布后备份复验通过。此次仅切换 Worker/helper，无数据库迁移或 Django/n8n 重启；证据见 `docs/evidence/top-navigation-production-20260912.json`。

2026-09-12，AI定时任务已作为 AI 助理下独立 `scheduled` 子模块在本机采用，Worker/helper 为 `20260912T085820Z-cde69efbfa6cb274`，Django manifest SHA 为 `c9d97773fdb9884528e221b8a46701e814f2e125a969fb3bd545a117c84e7226`。AI `0010` 新增任务定义与执行槽，AI 共 51 张自有表；沿用管理员权限、实时绑定身份、上海时间、5 分钟认领窗口、单例接收器、发送前持久预留和未知结果不重发。任务默认停用，当前仅纯文本；接收器未运行时不执行，禁止补发停机期间旧槽。发布前备份独立恢复、发布后备份复验及网页/连接核验通过，尚未执行真实投递验收。详见 `docs/AI_DINGTALK_SCHEDULES.md` 与 `docs/evidence/ai-scheduled-production-20260912.json`。

本文件适用于仓库根目录及其全部子目录。目标是让后续开发、数据处理、自动化和 AI 能力变更始终遵守当前业务口径、数据安全边界与 Cloudflare 运行约束。用户在当前对话中的明确指令优先于本文件。

## 1. 任务开始与事实来源

2026-09-12 天猫已受控发布 `20260911T165424Z-941a792a6c05e33e`：丽力使用浏览器登录态 MTOP 分批 M 和每日 cadence；指定 9 月 11 日 22:44:54 原任务已保留证据归档；不同天猫店铺独立执行并各自安全小时重试，同店仍串行。丽力 1058 与亿玖 1059 已真实同时运行，业务终态证据见 `docs/TMALL_STORE_ISOLATION.md`。n8n 在空闲窗口获额外授权重启一次，数据库/Django 未重启。此采用不覆盖 P 协议扩店、不采用未发布逐日回填循环，也不绕过服务维护授权与 n8n 全流程验收。

1. 开始任何任务前，先读取仓库根目录的 `README.md` 和 `AGENTS.md`，再按任务需要加载相关代码、测试和 `docs/` 文档。不再读取外部 Obsidian 记忆作为项目上下文。
2. 以当前代码、测试、数据库迁移、`README.md`、`AGENTS.md` 和仍在生效的操作文档为事实来源。若文档与代码冲突，先验证当前行为，再更新过期文档，不能为了匹配旧文档回退正确实现。
3. 修改前先检查 `git status` 和相关差异。工作区中已有的改动默认属于用户；不得覆盖、格式化、暂存或提交无关文件。
4. 涉及当前运营数据的查询，必须先阅读并遵守 [docs/OPERATIONS_DATA_QUERY.md](docs/OPERATIONS_DATA_QUERY.md)。

### 1.1 默认开发隔离与合并流程

- 前端展示调整可在独立 worktree 使用 `npm run preview:isolated`，通过 3100 端口和独立合成 SQLite 数据先给用户观看；数据准备/快照和限制见 `docs/ISOLATED_PREVIEW.md`。不得向预览 worktree 复制生产 `.dev.vars`、密钥或数据库连接。演示验收不能替代 PostgreSQL/权限/写流程验收；用户确认效果后才合并，生产发布单独处理。

- 后续代码变更默认从最新 `main` 创建独立 `codex/*` 分支和独立 Git worktree，在该 worktree 内完成开发、测试、构建、迁移演练与复审；不得直接在生产运行目录或生产检出上开发、切分支、安装依赖、生成构建产物或运行会改变状态的脚本。
- 涉及数据库、导入、队列、缓存、对象存储或自动化的验证，默认使用与生产隔离的镜像库/镜像环境。镜像必须使用独立连接、凭据、端口和存储命名空间；任何写入、迁移、回填、清理、任务消费和回调都只能指向镜像，禁止把生产连接作为开发回退路径。
- 镜像数据只用于验证，需保留必要的业务口径和边界条件，并按最小范围处理敏感数据；测试邮件、机器人、Webhook、定时任务和浏览器自动化默认关闭或指向测试接收端，避免产生真实外部副作用。
- 只有在 worktree 内相关测试、构建、迁移 dry-run、数据回查、权限/审计检查及必要复审全部通过，且确认与最新 `main` 合并无冲突后，才允许合并到 `main`。检查失败、证据不完整或镜像与生产契约不一致时不得合并。
- 合并到 `main` 不等于生产发布。生产数据库迁移、服务重启、路由切换、自动化启用和数据写入仍须走对应模块的受控发布与回滚流程；未经用户明确授权，不得自动部署或改变生产环境状态。
- 每次系统调整完成后，均须以当前 `main` 为基准检查对应 `codex/*` 分支是否已合入；仅当分支已完整合入、对应 worktree 无未提交内容且未被其他任务使用时，移除该 worktree，并删除本地及同名远程分支。未合入、worktree 不干净或使用中的分支必须保留，并明确报告原因。

## 2. 当前系统与模块边界

- 技术栈：React 19、Next.js 16 API/组件约定、TypeScript、Vinext/Vite、薄 Cloudflare Worker、Django 5.2、PostgreSQL 17 和 R2；本机全部结构化业务域及聚合入口已正式使用 Django/PostgreSQL，日常启动/发布控制链亦已脱离历史 D1 文件。D1 仅保留历史审计与退役证据，不进入生产业务或日常控制调用链；不可变退役证明和保留边界见第 8 节。
- 页面入口集中在 `app/page.tsx`，市场分析主体位于 `app/market-view.tsx` 和 `app/market-annotation-view.tsx`；业务逻辑应放在 `lib/<domain>/`，API 路由只负责鉴权、输入解析、调用服务和稳定响应。
- 当前导航模块为：工作流、BI 看板、网店分析、市场分析、客服分析、销售分析、库存管理、货品详情、运营事务、数据导入、系统设置和 AI 助理。
- 主要业务域及代码目录：
  - 销售事实、导入、查询和分析权威实现：`backend/sales/`；Worker 适配与消费者：`lib/django/`、`lib/sales/*-contract.ts`
  - 库存、库龄、补货、设置、原始分片、迁移和审计权威实现：`backend/inventory/`；Worker 薄适配与有界消费者为 `lib/django/inventory-*.ts`、`lib/inventory/django-*.ts`、`lib/inventory/system-cost-reference.ts` 和 `lib/inventory/work-items.ts`。旧 D1 库存领域路径只允许作为隔离迁移、终态退役审计或测试材料保留
    - 库存总览“库存健康分布”只统计京东仓、天猫履约仓（仓库映射分类 `cainiao`）、精确广东仓和自营仓；工厂代发、售后、样品、海外等仓别不得计入。六类状态固定显示为无库存可用、紧急补货、补货预警、积压风险、低周转、库存健康；库存周转严格大于 180 天才属于低周转。广东入仓型号编辑必须经库存 writer 的 `PATCH /api/inventory/guangdong-monitor/items`，继续保留版本 fencing、最小权限和写后回查。
  - 商品经营汇总、SKU 快递费率、原始分片、库存投影、迁移和审计权威实现：`backend/products/`；Worker 薄适配为 `lib/django/products-*.ts`、`lib/products/summary.ts`、`lib/products/chunked-upload.ts` 与 `lib/products/inventory-projection-sync.ts`。旧 `lib/products/shipping-rate-database.ts` 只允许作为隔离迁移、退役审计或测试材料保留；商品经营消费库存 Django consumer 的版本化完整投影，不形成库存第二写入源
  - 京东/天猫网店 SKU/SPU、推广与商品主数据权威实现：`backend/netshop/`；Worker 适配为 `lib/django/netshop-*.ts`，旧 `lib/netshop/`、`lib/jd/` D1 路径只允许作为已隔离的迁移、审计或测试材料保留
    - 京东四店每日商品工作流固定每天更新一次各店商品主数据；SKU/SPU 分天在下载前必须按“上海昨天所在月 1 日至昨天”逐店、逐维度查询 PostgreSQL 权威覆盖，只下载和导入连续缺口区间，无缺口不创建日数据任务。覆盖响应截断、实际日期与缺失日期不完整互斥或导入后仍有缺口必须失败关闭；C 必须复核每个精确批次并回查全范围零缺口。
  - 吉客云自动化：`lib/jackyun/`、`tools/jackyun-*`
  - 市场 TOP 榜单、价格、标注、任务和缓存元数据权威实现：`backend/market/`；薄 Worker 和受控执行适配为 `lib/django/market-service.ts` 与 `lib/market/django-*`，旧 `lib/market/` D1 领域路径只允许作为已隔离的迁移、退役审计或测试材料保留
  - 客服会话、配对导入、分析标注、原始分片、迁移和审计权威实现：`backend/customer_service/`；Worker 薄适配与有界 consumer：`lib/django/customer-service*.ts`、`lib/customer-service/`。旧 D1 客服对象只允许作为空 tombstone、永久 guard、隔离恢复研究或测试夹具保留；旧 `inventory-upload/` R2 前缀已终态下线
  - 财务事实、导入与分析权威实现：`backend/finance/`；Worker 薄适配为 `lib/django/finance-*`，旧 `lib/finance/` D1 路径只允许作为受保护审计、隔离恢复研究或测试材料保留
  - ERP 货品主数据、组合装、原始分片、迁移和审计权威实现：`backend/erp_reference/`；Worker 薄适配与有界 consumer 为 `lib/django/erp-reference-*.ts`、`lib/erp-reference/django-*.ts`。库存库龄继续属于 `backend/inventory/`；旧 `lib/erp-reference/` D1 领域实现和 ERP bridge 只允许作为隔离迁移、终态退役审计或测试材料保留
  - 运营事务工作计划、评论/活动/提醒/关联、模板（变量配置）、附件元数据/清理队列、巡店/评价记录、结构化新品项目、多店目标、七阶段、产品线、周报配置、投递账本、迁移和审计的权威实现：`backend/workflow/`；Worker 薄适配与有界 consumer：`lib/django/workflow-service.ts`、`lib/django/workflow-consumer-reader.ts`。旧 D1 运营事务对象只允许作为空 tombstone、永久 guard、隔离迁移/恢复研究或测试夹具保留；附件字节继续由薄 Worker 使用现有 R2 命名空间，元数据与清理状态以 PostgreSQL 为权威
  - AI 助理模型/渠道/知识/个人记忆、对话/产物/工具审计、分析沙箱、Agent/DAG、图片任务/资产元数据/图片字节及控制证据的 Django 实现：`backend/ai_assistant/`；公开 Worker 薄适配：`lib/django/ai-service.ts`、`lib/ai/django-route.ts`，中央能力唯一声明仍为 `lib/ai/tool-registry.ts`，签名只读工具桥为 `lib/ai/django-edge.ts`。AI reader/writer 独立使用 8111/8112、DPAPI 角色、同一 AI authority epoch 与 `ai-enabled.json`；启用后的全栈连接门槛为 128。新逻辑不得写回旧 AI D1 路径；2026-09-05 本机已正式单写切换并完成 D1 终态退役；正式证据及门禁见 `docs/DJANGO_AI_ASSISTANT_MIGRATION.md`。
  - 用户、固定角色、数据范围、权限变更审计、revision、迁移和写权证据的权威实现：`backend/access_control/`；Worker 薄适配为 `lib/django/access-control-service.ts`、`lib/auth/authorization.ts` 与 `lib/ai/background-principal.ts`。未知/停用账号失败关闭，不自动登记 viewer；AI 工具执行审计仍属于 AI 域，不混入权限变更审计。
  - 全局搜索：`lib/search/` 仅通过各域 Django consumer 有界聚合；禁止 D1 表清单、SQL、数据库句柄和 legacy/shadow 回退。市场消费响应为直接的 `items/total/truncated` 与 revision header。
  - BI 看板只读聚合：`backend/bi/`；Worker 薄适配为 `lib/django/bi-service.ts`。BI 不拥有或复制销售、ERP、库存事实，不设 writer 或第二套业务 revision；生产 reader 固定使用 `127.0.0.1:8081`、独立 `teruisi_bi_reader` 只读角色和 `bi-service-enabled.json`，服务端通过源 revision 前后采样组成一致性快照，并只在 `bi_migration_runs` 保存采用审计。2026-09-05 已完成本机生产启用，采用 run 为 `bi-apply-1079734fb42842eeb1cb13b830bbb8a6`，当时 Worker release 为 `20260904T223540Z-40a783da7d4d5867`；当前整机采用记录见下节。
- 新增业务模块时，应同时补齐 API、领域服务、权限、审计、测试、必要文档，以及可被 AI 检索时的有界只读工具。不要把复杂业务继续堆进页面组件或路由文件。

### 2.1 Django 后端渐进迁移决策

- 2026-09-12，数据导入“链路规则”京东多店铺状态修复已合入 main 并在本机生产受控采用。当前列绑定已启用的 `JdN8nSilentCopy2026`，不再读取未启用的旧工作流；定时与自动重试的完成文案按 n8n 执行模式区分，时间按上海本机日显示。正式页面回读四店共用执行 `#1243` 于 11:07 重试完成；执行级成功仍不替代各批次和日期覆盖的业务验收。本次无数据库迁移、n8n 定义变更、真实下载或导入；Worker 采用版本 `20260912T093226Z-588560ad2bfc1239`，Django manifest SHA 为 `c5dea3021039645a650ff1c8a469d1eda6a5f64a071ba0f619717c4fd1a5cb3f`。发布前备份独立恢复、发布后备份复验、全栈状态与正式页面只读回查通过。证据见 `docs/evidence/import-chain-rules-production-20260912.json`。

- 2026-09-12，广东仓手工健康覆盖自动复位已合入 main 并在本机生产受控采用。源码采用提交为 `39f6ada4d32988ee94a20836611965e98816352c`；Django deployment manifest SHA 为 `68c16a99ef2a973c3f1ddc8369bf716c7020714bbc5619fc7e64755a5b795b6b`，app fingerprint 为 `c1cb0627b3397370c7b8e1705286d14506c867d023444e9b036e376be367fc04`；Worker/helper 继续使用 `20260911T165424Z-941a792a6c05e33e`。手工标为健康的型号只有在后续当前快照中精确广东仓 `on_hand_quantity` 发生变化时才清空手工覆盖并结束既有备货健康跟进；缺失记录不视为零，历史补传、其他仓、仅可用/在途变化和重复导入不触发，重置与库存发布、revision、审计同事务。此次无迁移、无权限扩展，发布前备份独立恢复、发布后备份复验和生产只读回查通过；验收未触发导入或改写风险记录。证据见 `docs/evidence/guangdong-manual-health-reset-production-20260912.json`。

- 2026-09-11，小特 pandas 容器分析工具已合入 main 并在本机生产采用。Worker/helper effective release 为 `20260911T081342Z-6b7a6a81082f9a5b`，manifest SHA 为 `fa1ff44c85aa3ab64fb276738742d9964590dd9aa9b9484e9f79fe5d43dfc8ff`；Django manifest SHA 为 `d337f14cf64caad2e8efe01c9ecb00f34f2cdca8abc65af6d4f11c2c41dfb0dc`，app fingerprint 为 `e9803056d3e2815796a4484a1e5f4531493f9a6c3fa988a8792dfc6a3c059d8f`。独立 `TERUISI-Pandas` WSL2 使用 rootless Docker、固定本地镜像、无网络/只读根/CPU/内存/PID 限额；Windows 磁盘自动挂载与互通关闭。AI writer 独立 DPAPI 通道连接 `127.0.0.1:8121`，受控 AI 启停负责 broker 和精确 WSL 保活进程，状态探针绑定镜像与源码摘要。真实容器、164 项镜像 PostgreSQL AI 测试、正式数据集计算及审计回查通过；无业务迁移、付费模型调用或真实消息发送。禁止回退为宿主 Python、绕过源权限、自动重放未知任务或忽略清理失败。见 `docs/AI_PANDAS_SANDBOX.md` 与 `docs/evidence/pandas-sandbox-production-20260911.json`。

- 2026-09-11，库存健康指标、广东入仓型号编辑保存与各板块 AI 对话框布局已合并 main 并受控上线。Worker/helper effective release 为 `20260911T035534Z-683ed76137b93190`，manifest SHA 为 `7160fc50304bd888282a09d9bf1c1eed1ab862e2994fd1e25a7d181deb965ede`；Django 部署清单 SHA 为 `da378980df03116093a14e03b4f4c1a599a5ea0365223d9d2d53437d63afba5d`，app fingerprint 为 `9be969e1f9f2c68407e6df512cb57280c3a7475c1569d5b165e0d7fd84329f8b`。库存健康分布固定只统计京东仓、天猫履约仓、精确广东仓和自营仓，六类状态及严格大于 180 天低周转口径不变；广东型号编辑继续经 inventory writer 的 `PATCH`、版本 fencing 和写后回查。板块 AI 对话输入区已下移到抽屉底部并缩小占用，其余高度用于历史内容。整栈 Running / Ready / exact_release，发布前备份独立恢复及发布后备份复验通过；生产 UI 验收只打开并取消编辑器，未保存业务记录，未调用付费模型。证据见 `docs/evidence/inventory-health-ai-panel-production-20260911.json`。

- 2026-09-11，n8n 数据下载导入每小时安全重试已在本机生产受控采用。固定共享错误工作流 `TeruisiHourlyRetry2026` 与吉客云、京东、天猫共 11 条正式流程均为 active 且 `activeVersionId=versionId`，所有者一致，11 个仅本机 POST Webhook 已注册；n8n 2.32.7 仅监听 `127.0.0.1:5678`。两个京东兼容模板继续未激活且定义未变。生产更新基于 live 已发布定义只追加重试节点、连线、`errorWorkflow` 与策略元数据，未带入其他候选业务改动，也未触发下载、导入或平台点击。首次自然可重试失败仍须补充“错误 execution 进入 60 分钟等待态并产生新完整 execution”的运行证据；此前不得宣称真实失败链已经端到端验收。采用证据见 `docs/evidence/n8n-hourly-safe-retry-production-20260911.json`。

- 2026-09-11，BI 广东仓配置读取权限与就绪检查修复已受控上线。Django manifest SHA 为 `79dfe7c0dc9da6a48f85da7142e0e6c9f5aaa3692cdedfcda8f7acd2c0282a66`，app fingerprint 为 `8d52ec53f49ffcd48daa788fbfcc23db295cdf0aa32ff6c8e2d903ee47b9f045`；Worker 继续使用 `20260910T161236Z-c0ab8ed9ff663df9`。BI 必须具备 `inventory_guangdong_monitor_items`、`inventory_guangdong_supplier_cycles` 的 SELECT，并将这两张表纳入就绪检查；仍禁止配置写入、监控审计表读取和新增 writer。以后变更被 BI 复用的库存查询时，须同步检查 BI 依赖与最小权限，并使用真实 PostgreSQL BI 只读角色验证完整 `/api/bi/overview`，不能仅用首页 200 或服务就绪作为验收。无新迁移；各业务 revision/authority 与迁移清单不变，浏览器与公开正负向 API 验收通过。发布前备份独立恢复及发布后备份复验通过；本次未对发布后备份重复恢复。证据见 `docs/evidence/bi-guangdong-permissions-production-20260911.json`。

- 2026-09-11，广东备货健康跟进、库存总览广东统一规则及数据导入运行记录/链路状态已合入 main 并受控上线。Worker/helper effective release 为 `20260910T161236Z-c0ab8ed9ff663df9`，manifest SHA 为 `d3fc11fe60e6148842135a48622231741e686f249481e28900e0d8b4ab1ef4ca`；Django 部署清单 SHA 为 `36de10c5e0ad5c401c9c9bc14179476dfafdd6ee4d24c254e314a531680fb188`，app fingerprint 为 `adfd0ab5fe9642ecf691369ea1832710389d30955f77f88e79a5972299bfae95`。库存 `0010` 已应用，53 条备货计划原业务字段摘要不变；13 个备货跟进型号显示健康，44 个有库存记录的监控型号与总览风险一致。11 条导入链使用显式配置的本机 n8n 只读执行元数据，执行状态不等于业务导入验收。整栈 Running / Ready / exact_release；证据见 `docs/evidence/guangdong-replenishment-health-production-20260911.json`。successor 已按 `docs/WORKER_SUCCESSOR_CAPACITY.md` 评审扩至固定上限 256，当前第 129 条，旧记录全部保留；下方 128 上限说明为此前历史状态。发布前必须把固定干净集成树 `D:\运营管理系统-sales-django-release` 同步到已验收 main，不能只更新生产保护入口根目录。

- 2026-09-10，AI 对话工作台、Markdown 排版、SSE 与模型生成参数已合入 main 并受控上线。当前 Worker/helper effective release 为 `20260910T140701Z-0194554bc4bcead5`，manifest SHA 为 `6b4f56b358c893ec2f648c5ad83edc50a8bc0d315b975a6ffe53a27748461140`；Django 部署清单 SHA 为 `29b3280d535f41ef3659f3abffc11ab26d34e0f3af09b6dc9b0a876485b274fa`，app fingerprint 为 `8b0296144da0350a740ab655dde6153335156b7452545761036df31d38eb0aa8`。AI `0009` 已应用，5 个模型及 69 条历史消息原字段摘要不变。新模型默认最大输出 65536、任务总时限 1000000 秒，移除文本/视觉单轮超时，合并温度设置；旧模型不自动增额。整栈 Running / Ready / exact_release、正式 SSE 无效输入流式返回、备份独立恢复均通过，未调用付费模型。完整证据见 `docs/evidence/ai-workbench-production-20260910.json`；以下旧版本保留为历史证据。
- Worker SSE 适配必须使用运行时支持的 `redirect: manual` 并拒绝重定向响应；workerd 不支持 `redirect: error`。涉及受保护入口（尤其 `package.json`）变更时，应先受控停止旧 Worker，再更新生产根目录；入口已不匹配时只能核验并恢复不可变前驱的精确字节后受控 Stop，不得跳过 guard。该次发布后 successor 链达到旧上限 128 条；2026-09-11 已按上文评审扩至固定上限 256，仍不得删除历史证据或绕过上限。

- 2026-09-10，备货计划 Excel 日期一致性与钉钉群投递修复已合入 main 并完成受控生产发布。当前本机 Worker/helper effective release 为 `20260910T131216Z-674af827668202c9`，Worker manifest SHA 为 `f66ee662e9ba6c36b6d532315356e8529d09f6bd88cd2c3a8a508e2d2fb5650c`；Django 部署清单 SHA 为 `600a5b49a2ed00b075327cc892688c87cb08e303eee012e47886d36245161c7f`，app fingerprint 为 `109feeb4aaba4665e91332844531a20e440009b2ba6f84a3f247c060d5fb0b49`。整栈为 Running / Ready / exact_release，全部组件就绪；首页与备货计划导入模板均回读 200。Excel 数值日期现按工作簿日历日解析，不再因 UTC+8 转换回退一天；钉钉发送重新精确定位“志高/特睿思备货计划群”，按采购负责人分组并 @ 对应采购，供应商标题使用 Markdown。此次无数据库迁移；发布前后备份均通过 SHA 校验及独立恢复，验收未创建或修改备货计划、未写钉钉表、未发送真实群消息。证据见 `docs/evidence/inventory-replenishment-date-dingtalk-production-20260910.json`。

- 2026-09-10，库存备货计划草稿多选和“一键确认并提交钉钉”已合入 main 并受控增量发布。当前本机 Worker/helper effective release 为 `20260910T100749Z-a4eaa6fb400fd7e5`，Worker manifest SHA 为 `258628cea22b668807c5d952b3e1b6daa816757ce5053c66ec2b96005416d8c6`，Django 部署清单 SHA 继续为 `7de0c05d2d9a4025cf60c57aa4fb165e3388e6d3391a7035b51e188236bb1f84`；整栈为 Running / Ready / exact_release，全部组件就绪。此增量不含数据库迁移或 Django 部署，只短暂停止 Worker；生产构建已回读新入口，发布前后备份均通过独立恢复，验收未创建备货计划、未写钉钉表、未发送群消息。增量证据见 `docs/evidence/inventory-draft-bulk-confirm-production-20260910.json`；此前钉钉 AI、模板导入和已确认计划批量提交的统一采用证据继续见 `docs/evidence/inventory-dingtalk-unified-release-20260910.json`。真实群消息与付费模型端到端调用尚未执行，不能据此宣称所有模型问数场景已通过验收；以下旧版本继续作为历史证据。

- 2026-09-06 后续已完成本机全局 D1 控制链正式脱钩。当前 effective release 为 `20260906T035823Z-fceee410b71f79b0`，Django 部署清单 SHA 为 `28587a32ef44290b974f3dc4af5cec01d4583c8da2cb3b02d91943e1bdab4114`；23 服务、Worker/helper、启动绑定、生产只读 API 和发布前后备份独立恢复均通过。详细门禁见第 8 节及 `docs/GLOBAL_D1_CONTROL_RETIREMENT.md`。以下聚合与各域记录中的旧版本均保留为历史采用证据。

- 2026-09-06，本机全局搜索、AI 财务工具、财务公开 API、市场标注与调度等残留聚合入口已完成受控 Django/PostgreSQL 发布。Worker effective release 为 `20260905T180043Z-7364a22437c52ae1`，manifest SHA 为 `589e304f0e60a8ee711840888b5090c8bcdb7580b2372275e2313e8e219a7f4e`；无 D1 binding，不携带 Drizzle 迁移。Django 应用已采用财务目标视图隔离与网店最新批次半连接优化，23 个服务健康；ERP 环境变量固定沿用 `TERUISI_DJANGO_ERP_*`。不新增数据域、写权限或业务 revision，现有 R2 图片/附件字节边界保持不变。后续以 `docs/DJANGO_AGGREGATE_CUTOVER.md` 的实际采用与前向恢复门禁为准；下方各域历史记录中的旧 Worker release 和全局 D1 用途只描述当时状态，不能恢复为当前生产依赖。

- 2026-09-05，本机 AI 助理完整数据域已完成 Django/PostgreSQL 正式单写切换与 D1 终态退役，cutover ID 为 `ai-pg-20260905T143048Z-489bd21bb811`，authority epoch 为 `be36a1d7-a84f-4617-baf3-8537a844750d`。正式 apply `ai-apply-489bd21bb8114654a954c9a9004b9757` 迁移并复验 39 张历史表、536 条记录，源/目标摘要均为 `1218c81a9cc6a36ddc68b584bf82f35634b5154fbf6e0ef32150d87946358cca`；新增 5 张领域控制表，共 44 张 AI 自有表。AI reader/writer 固定 8111/8112，以独立 DPAPI 最小权限角色和 `ai-enabled.json` 加入启动与监控链。operator-only `0113/0114` 不进入普通 Drizzle journal；旧 AI 对象现为 40 个空 tombstone view，120 个永久 guard 拒绝旧写入。2026-09-06，AI 图片字节亦已正式切换至 PostgreSQL；新增 `ai_space_asset_payloads` 后共 45 张 AI 表，字节与资产在同一事务发布，最大 6 MiB，具有 SHA/大小、不可变 payload 与 authority fencing 约束。旧 AI R2 存取入口已删除，`ai-space/` 对象、字节、multipart upload/part 均为 0；共享 R2 其余 38,050 个对象的保留摘要一致，其他域 D1/R2 继续保留。正式证据见 `docs/DJANGO_AI_R2_RETIREMENT.md`。切换已跨过 PNR，禁止恢复旧 D1/legacy/fallback、双写或反向迁移；恢复只允许 PostgreSQL 备份/WAL/PITR、兼容代码或受控前向修复。正式证据见 `docs/DJANGO_AI_ASSISTANT_MIGRATION.md`。

- 2026-08-27 已确认后端长期目标框架为 Django，并采用按业务域逐步迁移，不进行一次性整体重写。现有 React 19、Next.js 16、Vinext/Vite 前端继续保留；2026-09-01 用户最终确认网店分析前端也继续使用现有 React/Next.js 架构，不改写为 Django template。
- 2026-09-05，本机用户/角色/数据范围/权限变更审计域已完成 Django/PostgreSQL 正式单写切换和 D1 终态退役。cutover ID 为 `access-control-pg-20260905T091024Z-06e143235de1`，authority epoch 为 `5903a114-ec95-4d17-95bd-d612f013d0ed`；正式 run `access-control-06e143235de14abf80541c6d27e15781` 迁移并复验 1 个启用的无限制管理员，源/目标摘要均为 `92ff6dd59dd97011d692da1453a46f6d982451494c990dcb3207ffdb0364c497`。固定 reader/writer 为 `127.0.0.1:8101/8102`、独立最小权限 DPAPI 角色，并通过 `access-control-enabled.json` 加入受控启动链；Worker effective release 为 `20260905T090134Z-6a5d72609fb56a8a`。operator-only `0111/0112` 不进入普通 Drizzle journal；旧 `app_users` 和权限 authority 是 2 个空 tombstone views，6 个永久 guard 拒绝旧写入。权限域没有 R2 文件/字节所有权，不能据此删除仍供 AI、市场图片和运营事务附件等范围使用的全局 D1/R2。已跨过 PNR，禁止恢复 D1 权限读写、自动补建、双写或反向迁移；恢复只允许 PostgreSQL 备份/WAL/PITR、兼容代码或受控前向修复。正式证据见 `docs/DJANGO_ACCESS_CONTROL_MIGRATION.md`。
- 自本决策起，所有新增后端业务能力默认且必须在 Django 服务中实现。现有 TypeScript/Next.js API 仅继续承担尚未迁移业务的维护、缺陷修复、迁移适配、边缘路由和必须依赖 Cloudflare Worker binding 的兼容职责，不得把新的领域事实源或长期业务流程继续堆入旧后端。确需临时例外时必须获得用户明确批准，隔离实现并记录后续迁移项。
- 每个既有业务域在完成数据结构与数据迁移、API 契约对比、真实 principal 权限与 scope、审计、业务口径、并发/幂等、性能、回滚演练和单写所有者切换前，仍由当前 TypeScript/Worker 实现作为该域的权威后端。不得因已确定 Django 方向就声称尚未迁移的模块已经运行在 Django 上。
- 迁移期间禁止新旧后端长期双写。同一精确业务范围任一时刻只能有一个写入所有者；优先通过只读影子对比、按域灰度路由和可立即回退的切换完成迁移。现有金额、时区、日期边界、店铺身份、权限、审计、导入幂等、租约 fencing、跨店隔离和落库回查契约必须原样保留。
- 任一业务域在正式切换前可以按上述门禁使用影子对比和受控灰度，但一旦宣布正式切换，切换单元必须覆盖完整垂直调用链，不接受只迁数据库、只迁写接口或只迁读取作为完成：权威写入源和读取源、浏览器/客户端调用、公开 API 与边缘适配、Django 服务端领域实现、后台任务与自动化、配置、部署、启动和监控必须在同一次受控切换中全部指向新代码。除明确属于鉴权、解析或短期传输的薄边缘适配外，旧客户端分支、旧服务端领域逻辑、旧数据库读写、同步器、feature flag 和 fallback 必须退出可达生产路径；旧客户端与服务端实现必须从生产源码入口和运行包中删除，仅必要的迁移/恢复工具、审计材料、只读 tombstone 和测试夹具可以隔离保留在非生产执行面。正式切换后的恢复只能使用新架构的备份恢复、兼容修复或前向修复，不得把流量或写入重新导向旧后端。只有全链路正向与负向测试、静态旧路径可达性检查、真实部署回读和旧路径拒绝证明全部通过，才可宣称该模块迁移完成。
- 2026-08-29/30，本机销售域已完成 Django/PostgreSQL 终态单写切换。PostgreSQL 是销售事实、批次、导入范围与幂等、尝试审计、上传/暂存状态、revision 和全部销售读写的唯一权威；D1 不再是销售写入源，也不得作为读取、容灾或回滚路径。该结论只覆盖当前 Windows 主机和销售域，不自动批准远程生产、高可用、其他业务域或整个 D1/R2 的迁移。
- 公开 Worker 继续负责真实 `requireAppPrincipal()`、参数契约、HMAC principal 信封、Excel 解析、分片请求边界、请求超时与体积边界和边缘适配；销售查询进入 `127.0.0.1:8001`，销售导入及原始分片字节进入 `127.0.0.1:8002` 的 PostgreSQL 写侧。销售生产路径不得读写 R2；超时、签名、响应上限、JSON、revision、reader/writer 或权限异常必须失败关闭，不得回查 D1/R2。全局 R2 binding 仍供其他业务域使用，不得因销售退役 R2 而删除。
- D1 `0092_sales_domain_retirement.sql` 已退役销售事实、批次、上传、缓存、投影 outbox 和 authority 对象。保留的只读 tombstone views、retirement receipt、共享导入表销售永久写入 guard、迁移工具和测试夹具是防复活终态证据，不代表仍存在销售 D1 后端；普通 Drizzle 生成在建立新审计 baseline 前仍须失败关闭。
- 2026-09-05，本机 ERP 主数据域已完成 Django/PostgreSQL 正式单写切换、D1 终态退役和旧 `inventory-upload/` R2 路径下线，cutover ID 为 `erp-reference-pg-20260905T102200Z-8a8dbcb59fc8`，authority epoch 为 `69f7d42f-109f-43c7-9f84-c86629d8fa00`。PostgreSQL 是 ERP 货品、组合装、批次、scope head、内容指纹、导入尝试、请求 receipt、原始分片、revision、迁移和审计的唯一权威；生产 run `erp-reference-eb5fa9fc5cd0467ba58c9dc0a9c11b01` 迁移并复验 8,473 条货品、4,392 条组合件、83 个批次、58 个尝试、83 个规范化指纹和 2 个 scope head，源/目标摘要均为 `33b4d6032868f5d25532cc9333f09482bc2a205ee92f2cec0524a8f583f4d7fb`。公开 Worker 只承担真实鉴权、权限、Excel 解析、HMAC、超时/体积边界和薄适配，ERP 读写固定进入 `127.0.0.1:8091/8092`。唯一跨域写权限是按 ERP 映射更新既有 `sales_order_lines.resolved_category` 派生分类；不得新增或删除销售事实，不得修改金额、成本、销量、`gross_profit`、其他销售字段或批次。operator-only `0110` 已将 7 个旧 D1 ERP 对象变为空 tombstone view，并安装 18 个永久 guard；旧 ERP bridge 已撤权并退出启动链。切换已跨过 PNR，禁止恢复 D1/R2/bridge/legacy/shadow、双写或反向迁移；恢复只允许 PostgreSQL 备份/WAL/PITR、兼容代码或经审批的前向修复。正式证据见 `docs/DJANGO_ERP_REFERENCE_MIGRATION.md`。
- 切换已跨过 PNR，不支持 `pending→d1`、`legacy`/`shadow` 路由或反向迁移。故障恢复仅允许兼容代码、PostgreSQL 备份/WAL/PITR 或经审批的前向数据修复；成功正式备份、恢复演练、attestation、forward-recovery 和 retirement 证据不得清理。
- Worker bootstrap current/authority 只是 append-only 链根和不可变切换证据，当前运行版本必须以经验证的 effective successor head 为准。后续 release 只能在 Worker 停止时执行受控 `plan`，再用精确 plan SHA 执行 `apply`，通过 append-only successor record/sidecar 形成唯一、连续、有界的 effective-head 链；每个 release 的 activation fence 必须先使 predecessor guard 失败关闭。旧 release、分叉、环、篡改、孤立 sidecar、不可达记录、过期 CAS 或证据不一致均失败关闭。`plan` 会构建候选并写入计划，不是无副作用 dry-run；激活后必须立即把登录快捷方式重绑到 effective head 并回读验证。
- Worker supervisor 的 prelaunch 不得递归调用 PowerShell `Status`；只能直接、有界验证 service 原子写入的 create-only canonical process receipt，且等待预算必须覆盖 controller 建立精确 CIM identity 和写入 receipt 的时延。外层 controller 仍须按 PID、CreationDate、命令行和进程树二次核验。PowerShell 读取受控 JSON 时必须保留 ISO 日期字符串，不能让 pwsh 自动转换为 `DateTime` 后进入递归规范化。
- Miniflare 的 `Request.cf` 缓存固定写入 Worker runtime 的 `cache\miniflare\cf.json`，不得写入 immutable release、`node_modules` 或业务 `.wrangler/state`。每次启动和子进程重启前都必须清除继承的同名环境变量并安装固定绑定，核验 runtime/release/persist 边界、目录全链、文件叶和硬链接身份；release 出现 `.mf` 或其他未列入 manifest 的对象必须失败关闭。
- 本机 Django/PostgreSQL 服务固定运行于 `D:\teruisi-runtime\django-sales`：PostgreSQL 17.11 只监听 `127.0.0.1:5432`，销售 reader/writer 固定监听 `127.0.0.1:8001/8002`，财务 reader/writer 固定监听 `127.0.0.1:8011/8012`，网店 reader/writer 固定监听 `127.0.0.1:8021/8022`，市场 reader/writer 固定监听 `127.0.0.1:8031/8032`，商品经营 reader/writer 固定监听 `127.0.0.1:8041/8042`，库存 reader/writer 固定监听 `127.0.0.1:8051/8052`，运营事务 reader/writer 固定监听 `127.0.0.1:8061/8062`，客服 reader/writer 固定监听 `127.0.0.1:8071/8072`，ERP 主数据 reader/writer 固定监听 `127.0.0.1:8091/8092`。长期进程使用各领域独立最小权限 reader/writer，凭据仅保存为当前 Windows 用户绑定的 DPAPI 密文；readiness 必须验证各自 schema、索引、authority、attestation、revision、摘要和只读事务。网店、市场、商品经营、库存、运营事务、客服和 ERP 主数据分别通过 authority 绑定的 `netshop-service-enabled.json`、`market-service-enabled.json`、`products-service-enabled.json`、`inventory-service-enabled.json`、`workflow-service-enabled.json`、`customer-service-enabled.json`、`erp-reference-enabled.json` 加入现有受控开机启动链。完整 Django/BI 运行栈按最大线程连接数和运维余量要求 PostgreSQL `max_connections>=120`，本机正式值为 `128`；低于门槛时顶层启动和 ERP/商品经营/库存/运营事务独立启动均必须失败关闭。登录快捷方式不是 Windows Service；顶层进程崩溃后仍需受控检查和显式启动。
- PostgreSQL 日常逻辑备份必须使用 exported snapshot 将证据与 dump 绑定，在线备份不得自动启停服务；证据必须覆盖库中已经存在的销售、ERP、财务、网店、市场、商品经营、库存、运营事务及客服全板块表，并绑定各已迁移域的 revision、迁移 run 与 authority。恢复演练只能在独立端口和独立临时数据目录启动受控 PostgreSQL，禁止在生产 cluster 内创建、覆盖或删除演练数据库。过期备份清理必须保留至少 30 天和至少 7 份已验证成功备份，只能删除固定 `postgres-daily` 根目录下通过 manifest、SHA-256 与 archive 复验的精确 `daily-*` 直接子目录，并保留清理审计。具体 operator 和门禁见 `docs/DJANGO_POSTGRES_OPERATIONS.md`。
- Django runtime 守护必须随 runtime 部署并通过登录启动项持续运行；它只能在显式 `desiredState=running`、连续两次确认本部署 PostgreSQL 或任一 reader/writer 进程确实停止、且端口/进程/ACL 身份均正常时调用既有 `Start`。Windows 重启后仅当 receipt 的进程创建时间、启动时间及文件最后写入时间都严格早于当前系统启动时间，operator 才可只删除该上次开机遗留 receipt，绝不能终止当前复用同一 PID 的进程；当前启动周期内或无法证明来源的 PID/身份异常仍只能告警并失败关闭。状态探针失败、端口冲突、进程仍在但 readiness 失败或 ERP authority/revision/摘要分歧也禁止自动重启或调用 `Stop`。自动 Start 必须在服务 mutex 内复验 desired-state 文件 SHA-256 fencing token，15 分钟最多 3 次。告警只写脱敏本地 outbox；外部发送仍须动态唯一核验“志高助手”与“测试群聊”，不得保存或猜测机器人/群身份。启用与回退见 `docs/DJANGO_RUNTIME_SUPERVISION.md`。
- `GET /api/sales/data-health` 只允许无数据范围限制的 `operator/admin`，且只能复用 Django reader 已有的 `freshness` consumer；返回单写来源、动态 revision、上海业务日期、销售覆盖、机械 lag 天数和最近成功批次。不得为该接口扩大 reader 数据库权限、读取 runtime/备份/告警文件、定义未经确认的“过期”阈值，或在销售/财务页面模板中复制另一套新鲜度口径。
- 后续业务域复用 Django/PostgreSQL 时必须遵守 `docs/DJANGO_DATA_IMPORT_ARCHITECTURE.md`。每个领域保留独立 app、迁移、写权限、revision、幂等/范围 owner 和切换证据；新增领域故障只能使该领域失败关闭，不得改变销售或 ERP authority、销售事实、其他模块写入所有权或其他页面可用性。迁移开发和测试只使用隔离工作树与临时数据库，正式切换前不得停止或重启其他模块服务。
- 2026-08-31，本机财务域已完成 Django/PostgreSQL 正式单写切换，cutover ID 为 `finance-pg-20260830T194437Z-184fdca41051401f`。PostgreSQL 是财报事实、批次、月份、目标、版本、导入幂等/尝试审计、revision、读取和写入的唯一权威，`TERUISI_DJANGO_FINANCE_MODE` 必须保持 `django`；公开 Worker 只保留真实 principal、权限/scope、Excel 解析、HMAC、超时/体积边界和边缘适配。切换已跨过 PNR，禁止恢复 `legacy`/`shadow`、重新开放 D1 财务写入或把 D1 当作回滚事实源；D1 财务对象和 42 个永久 authority guard 只作为受保护审计材料保留。财务故障必须只使财务 API 失败关闭，恢复仅允许 PostgreSQL 备份/WAL/PITR、兼容代码或审批过的前向修复，不得改变销售或 ERP authority、销售事实、其他模块权威或现有财务前端模板。正式证据与运维步骤见 `docs/DJANGO_FINANCE_MIGRATION.md`。
- 2026-09-01，本机网店域已完成 Django/PostgreSQL 正式单写切换与 D1 终态退役，cutover ID 为 `netshop-pg-20260901T013053Z-4977777cec32`。PostgreSQL 是网店事实、批次、SKU/SPU、推广、上传、revision、幂等/尝试审计、scope head、读取和写入的唯一权威；现有 React `shop-module-view` 保留完整网店 UI 并继续通过同源公开 API 工作，前端不使用 Django template，也不直连 PostgreSQL。公开 Worker 只承担真实鉴权、权限/scope、签名、Excel/ZIP/图片解析、allowlist、超时/体积边界和薄边缘适配；网店读写固定进入 `127.0.0.1:8021/8022`。切换已跨过 PNR，禁止恢复 `legacy`/`shadow`、旧 D1 网店读写或反向迁移；旧 D1 网店对象已由 operator-only `0096` 变为 15 个空 tombstone view，共享导入表由 9 个永久 guard 拒绝网店域写入。原先为 D1 市场域保留的 `market_netshop_projection` 已随市场域正式切换由 `0098` 终态退役；当前兼容投影只由 Django netshop consumer 原子写入 PostgreSQL 市场表。故障恢复只允许 PostgreSQL 备份/WAL/PITR、兼容代码或审批过的前向修复，不得改变销售、财务、ERP、市场或其他模块权威。operator-only `0094/0095/0096` 仍不得由普通 Drizzle 自动应用；正式证据和运维边界见 `docs/DJANGO_NETSHOP_MIGRATION.md`。
- 2026-09-01，本机市场域已完成 Django/PostgreSQL 正式单写切换与 D1 终态退役，cutover ID 为 `market-pg-20260901T112221Z-a687294e320f`，authority epoch 为 `c69ee84e-836d-40f6-b7b8-875ecca6f8c6`。PostgreSQL 是市场事实、批次、价格、标注、图片缓存元数据、任务、revision、幂等/尝试审计、scope owner、网店投影、读取和写入的唯一权威；现有 React 市场页面继续通过同源公开 API 工作，公开 Worker 只承担真实鉴权、权限、签名、边缘解析/图片能力、超时/体积边界和薄适配，市场读写固定进入 `127.0.0.1:8031/8032`。两笔历史 `processing` 批次和 8,000 条未发布 staging 已在 PNR 前受控处置；旧 D1 市场对象现为 49 个空 tombstone view，共享导入表由 9 个永久 guard 拒绝市场域写入。切换已跨过 PNR，禁止恢复旧 D1 市场读写、`legacy`/`shadow` 或反向迁移；故障恢复只允许 PostgreSQL 备份/WAL/PITR、兼容代码或审批过的前向修复。operator-only `0097/0098` 仍不得由普通 Drizzle 自动应用；正式证据见 `docs/DJANGO_MARKET_MIGRATION.md`。
- 2026-09-02，本机商品经营域已完成 Django/PostgreSQL 正式单写切换与 D1 终态退役，cutover ID 为 `products-pg-20260901T164758Z-1c636a3a0f564bc9`，authority epoch 为 `82834127-5232-41fd-8e16-fa5c02bc3e8d`。PostgreSQL 是商品快递费率、批次、revision、幂等/尝试审计、原始分片、库存只读投影、读取和写入的唯一权威；商品汇总只读销售与 ERP 的现有 PostgreSQL 权威，并消费库存 Django consumer 的版本化完整投影，投影固定排除 `刷刷仓`。现有 React/API、AI 和 global search 保持原契约，公开 Worker 只承担真实鉴权、XLSX 解析、HMAC、超时/体积边界和薄适配，商品读写固定进入 `127.0.0.1:8041/8042`。旧 D1 商品对象已由 operator-only `0100` 变为 3 个空 tombstone view，18 个永久 guard 拒绝商品域写入；商品在线路径及分片不读写 R2。切换已跨过 PNR，禁止恢复 D1/legacy/shadow、双写或反向迁移；恢复只允许 PostgreSQL 备份/WAL/PITR、兼容代码或经审批的前向修复。全局 D1/R2 binding 继续供市场图片、运营事务附件和其他尚未退役业务域使用，不得因商品域退役而删除。正式证据见 `docs/DJANGO_PRODUCTS_MIGRATION.md`。
- 2026-09-03，本机库存域已完成 Django/PostgreSQL 正式单写切换、D1 终态退役与库存 R2 路径下线，cutover ID 为 `inventory-pg-20260902T162501Z-c6f5c5af1d254c0d`，authority epoch 为 `7cc50a33-91a6-4e2d-8253-1531c6de3a22`。PostgreSQL 是分仓库存、库龄、批次、revision、导入 owner/幂等/尝试审计、原始分片、备货计划、运营设置、读取和写入的唯一权威；公开 Worker 只承担真实鉴权、XLSX 解析、HMAC、超时/体积边界和薄适配，库存读写固定进入 `127.0.0.1:8051/8052`。商品经营投影、系统成本、AI、全局搜索和执行事项统一使用有界 Django consumer。旧 D1 库存对象已由 operator-only `0102` 变为 6 个空 tombstone view，24 个永久 guard 拒绝库存共享命名空间复活；库存 R2 前缀为空且生产源码不再可达，但全局 D1/R2 binding 继续供市场图片、运营事务附件和其他业务域使用。切换已跨过 PNR，禁止恢复 D1/R2/legacy/shadow、双写或反向迁移；恢复只允许 PostgreSQL 备份/WAL/PITR、兼容代码或经审批的前向修复。正式证据见 `docs/DJANGO_INVENTORY_MIGRATION.md`。
- 2026-09-03，本机运营事务新品项目子域已完成 Django/PostgreSQL 正式单写切换、D1 终态退役与新品 R2 路径下线，cutover ID 为 `workflow-pg-20260902T110500Z-bdfebd254007`，authority epoch 为 `ce0ceb5d-7bc9-4ea9-8812-0609f7fdf1aa`。12 条旧 `launch` 记录迁为 12 个项目、12 个目标、84 个阶段和 38 条活动；历史缺失字段形成显式缺口，没有伪造阶段事实。该新品 authority 与终态证据继续保留，并在 2026-09-04 的全板块切换中作为第二道 writer authority 共同核验。
- 2026-09-04，本机运营事务全板块已完成 Django/PostgreSQL 正式单写切换与 D1 终态退役，operations cutover ID 为 `workflow-ops-pg-20260904T094000Z-7438caa33f18`，authority epoch 为 `5ab6bed6-df07-4585-84d9-f650e0855ef1`。PostgreSQL 是工作计划、评论/活动/提醒/关联、模板（变量配置）、附件元数据/清理队列、巡店/评价记录以及既有新品子域的唯一业务事实与状态权威；正式迁移 run `workflow-ops-7438caa33f189924efd6bdbc04192660` 精确迁移并复验 35 个任务、1 条评论、34 条活动，其余旧 operations 集合为 0。现有 React 六个 tab、公开 API、AI、global search 和库存执行事项保持原入口，公开 Worker 只承担真实鉴权、scope、HMAC、R2 附件字节、超时/体积边界与薄适配，读写固定进入 `127.0.0.1:8061/8062`。operator-only `0106` 已将 14 个旧 D1 对象变为空 tombstone view，并安装 42 个永久 guard；切换已跨过 PNR，禁止恢复 D1/legacy/shadow、双写或反向迁移。附件字节继续保留在现有 R2 命名空间，当前迁移水位附件为 0；全局 D1/R2 binding 继续供市场图片、运营事务附件和其他现行域使用。恢复只允许 PostgreSQL 备份/WAL/PITR、兼容代码或经审批的前向修复。正式证据见 `docs/DJANGO_WORKFLOW_MIGRATION.md`。
- 2026-09-05，本机客服分析已完成 Django/PostgreSQL 正式单写切换、D1 终态退役与历史 `inventory-upload/` R2 前缀下线，cutover ID 为 `customer-service-pg-20260905T012130Z-5e02b476b398`，authority epoch 为 `6fbe9992-5f8d-44a3-8569-2f90f49a40e5`。PostgreSQL 是客服会话、批次、分析标注、删除审计、导入 scope/幂等/尝试、原始分片、revision、读取和写入的唯一权威；正式 run `customer-service-5e02b476b3984cb590f46fd11081c6d9` 迁移并复验 29,018 条会话、7 个批次和 1 个 scope head，源/目标摘要均为 `ed3c642fbbfa03f4ee956ffe8c0d0eed58aafde609c58a7413fd4cc9876b04fa`。现有 React 页面、公开 API、AI 和 global search 保持原入口，Worker 只承担真实鉴权、scope、Excel/聊天解析、HMAC、超时/体积边界和薄适配，读写固定进入 `127.0.0.1:8071/8072`。operator-only `0108` 已将 5 个旧 D1 对象变为空 tombstone view，并安装 18 个永久 guard；R2 `inventory-upload/` 对象、字节、multipart upload 和 part 均为 0。切换已跨过 PNR，禁止恢复 D1/R2/legacy/shadow、双写或反向迁移；全局 D1/R2 binding 继续供市场图片、运营事务附件和其他现行域使用。恢复只允许 PostgreSQL 备份/WAL/PITR、兼容代码或经审批的前向修复。正式证据见 `docs/DJANGO_CUSTOMER_SERVICE_MIGRATION.md`。

## 3. 统一业务口径

- 业务时区固定为 `Asia/Shanghai`。自动化日期、今日/昨日、导入覆盖和相对日期均以该时区计算；仅为避免日期漂移的纯日历运算可使用 UTC，但不能把 UTC 当作业务时区。
- 系统金额默认以人民币分存储和传输；展示为元时除以 100。净销售额包含负值退款，销量必须明确是正向销量、退货量还是净销量。
- 销售分析的“大毛利率”口径固定为 `(分摊后金额合计 - 货品成本合计) / 分摊后金额合计`，不扣费用分摊；“订单毛利”仍使用导入明细的毛利字段合计。销售总览、渠道/店铺、品类、趋势、API 和 AI 工具不得把订单毛利直接除以销售净额作为大毛利率。
- 日期查询使用左闭右开区间。店铺身份不能只靠模糊名称，至少保留并核验 `platform + shop_name`；销售源存在 `channel` 时还要交叉核验渠道。
- `刷刷仓` 是当前销售、库存和成本分析的业务排除仓。除非用户明确变更业务规则，不得删除该排除或把它混入经营指标。
- 库存总览与备货计划的销量窗口固定为销售最新截止日向前 30 个自然日，使用正向销量且退款不冲减备货需求；备货计划展示货品跨仓近 30 天总销量。分仓库存供应商优先使用吉客云“规格默认供应商”，老快照才回退 ERP。仓库分类与是否计入库存统一使用 `config/inventory-warehouse-mapping.json`，不得仅靠前端文案或临时模糊规则另算一套。
- 备货计划 Excel 导入只允许操作员或管理员使用标准模板，一次最多 200 行并原子提交；货品与仓库必须存在于同一次最新权威库存快照，过期快照须显式确认，任一行无效则整批拒绝。导入不得自动产生钉钉外部写入；草稿多选的一键操作必须逐条完成本地确认后再复用单计划钉钉幂等标记、契约核验和写后回查，已确认计划的批量同步也复用同一链路。任一阶段失败的项目保留勾选供人工重试，不回滚其他已确认计划，不因混合选择丢失另一状态的选择。
- 备货计划群消息只能由操作员或管理员多选已确认、且采购与工厂身份完整的计划后显式预览和确认发送；目标群、机器人和采购人必须动态精确匹配。话术按“采购 → 对应工厂”分组，货品行固定为“货品编码 货品名称，× 数量台”。发送前同步所选钉钉多维表记录，群消息使用库存域投递账本防重；新多维表标记统一为“运营管理系统备货计划ID”，只把旧“TERUISI备货计划ID”保留为历史查询兼容，任何流程不得据此新增重复记录。
- 市场分析只代表“当前 TOP 榜单覆盖口径”，不得表述为完整行业大盘。SKU 与 SPU、类目、榜单 scope、经营模式、价格段和统计周期必须保持完整身份隔离。
- 市场“正式市场定位价/主图展示价”和底层成交均价保留独立来源；商品榜单的展示契约例外：存在正式主图价时，该行成交均价展示为同一主图价，不存在时才使用底层成交均价。需要正式价格的价格带、AI 工具或发布流程只能使用符合当前确认契约的人工确认价；候选价、分期金额、定金、起售价等不能静默升级为正式价格。
- 市场行业汇报与机会矩阵只有在单一类目、单一榜单 scope、单一 SKU/SPU 维度、连续可比月份和相关商品正式价格完整时，才可输出“建议进入”或“谨慎回避”；任一条件缺失时只能保持“持续观察”并披露缺口。机会评分仅代表当前 TOP 榜单信号，未纳入采购成本、推广、安装售后、退货、滤芯复购和合规准入时不得当作最终投资或利润结论。
- SKU 维度的“标准售价”一经人工复核并正式入库，同一三级类目、榜单 scope、SKU 维度、SKUID 和图片哈希的其他月份直接沿用该分类与标准价，不再调用 AI 或重复标准入库；同一业务身份的 SKUID 图片变化时，只复用仍在当前细分类目字典中的人工确认分类，并重新执行价格专用视觉识别，不得沿用旧图价格；没有合格历史分类的新 SKUID 才执行分类与价格的完整识别。该规则不跨类目、scope 或 SKU/SPU 维度，不适用于定金、分期金额或无法判断的价格。
- 新品阶段矩阵中的“目标店铺”面向用户统一称为“店铺规划”，并可直接进入项目编辑；矩阵单独展示项目负责人和工作状态备注。建模、分析定价、图片、视频、备货五个节点只要求选择状态；阶段负责人、计划截止日、阻塞原因、节点说明/交付结果、证据名称和证据链接均为选填，受阻状态不得因缺少阻塞原因而拒绝保存。
- 上新销售跟进按运行机器本地时区划分和调度自然周，不再固定使用上海时区；销售归集唯一使用吉客云货品代码。产品线名称、产品图与吉客云名称学习关键词由用户维护，一个产品线可以包含多个吉客云代码与货品名称；产品图从本地选择 JPG、PNG 或 WebP，保存前转换并压缩为不超过 300KB 的 JPEG 后随产品线进入 PostgreSQL 权威，页面、Excel 与钉钉周报复用同一图片内容，编辑界面不得恢复手填图片链接。产品主数据导入后触发学习，名称/关键词只命中一个产品线时自动补入，多产品线命中时保留待人工判断，不得猜测归类。产品线从监控开始日持续跟踪，不设置截止周数；操作员可暂停或启动监控，暂停项保留但不得进入钉钉预览、Excel 或发送截图。图片周报的周维度固定从 2026-08-03 起逐周累积，固定展示品牌、产品图、产品名称、净销量趋势和各周净销量；不得静默删除历史周。机器人名称、目标群和发送时间统一在“系统设置 → 钉钉机器人”维护，“运营事务 → 上新跟进”只保留图片预览、下载和直达设置入口。周报外发必须先动态唯一核验“志高助手”和“测试群聊”，将经过校验的 PNG 上传钉盘后由机器人仅发送“新品销售周报”文案与截图在线预览入口，并以数据库投递账本防止同一报告周重复发送；AppKey、AppSecret、token、预览链接和原始外部回执不得写入业务数据库或日志。
- 页面、接口、AI 回复和导出中的同名指标必须复用同一领域口径；不得在前端重新定义服务端指标。

- 上新跟进表格预览支持生成 Excel 工作簿；主数据品牌为空时按当前业务口径展示“志高”，钉钉机器人仍投递同版式 PNG 在线预览。
- 上新跟进的产品图在产品线列表、编辑预览、周报表格、钉钉 PNG 和 Excel 中统一放入固定展示框，按原始宽高比居中完整显示；不得使用 `cover` 裁切产品外观，也不得强制拉伸成方形。

## 4. 运营数据查询规则

1. 查询当前 TERUISI 运营数据时，优先使用只读（read-only）`teruisi_operations` MCP；第一步调用 `get_data_freshness`。
2. 只有 MCP 不可用且用户明确指定本机已导入数据时，才可使用对应业务域 Django/PostgreSQL 的最小只读身份。当前所有业务域均已切换，不得使用本机 D1 或其历史副本查询当前运营数据。回复中必须说明替代来源。
3. 输出必须写明数据来源、数据截止日期、时间范围、渠道/平台/店铺/SKU 等筛选、金额和销量口径，以及缺数或映射异常。
4. 中文筛选必须参数化并保证 UTF-8。零结果要先核验覆盖日期、字段枚举、编码、退款和时间边界，不能直接断言无数据。
5. 只读连接不得导入、修改或删除数据，也不得声称已创建或改变补货计划。相关工具可调用时，不得用记忆、样例或估算代替真实查询。

## 5. 导入、自动化与多店铺隔离

- 所有导入必须保留来源、原文件 SHA-256、规范化业务内容指纹、业务范围、批次、店铺、数据集、覆盖日期、行数、告警和最终状态，并在返回成功前做落库回查。下载成功不等于导入成功，创建下载任务也不等于数据已经发布。
- 所有导入接口的重复判定统一为“业务域 + 精确业务范围 + 解析、清洗和业务过滤后的完整规范化内容”全部一致。行顺序、对象键顺序、文件名、原始行号和工作簿元数据等非业务差异不得触发新导入；通过校验的权威业务集合中任一业务字段、业务行数或业务范围变化都必须创建新批次。原文件哈希只用于签收、审计和原文件追踪，不能单独决定 `duplicate`，也不得用全局哈希把不同店铺、月份或数据集误判为重复。签名、解析、表头、范围或空数据等预校验拒绝只写尝试审计，不得创建业务内容指纹、抢占范围锁或改变范围状态；没有受控来源提供的权威空集合证明时，零行必须失败关闭，不能删除既有事实。分天数据的行身份仍必须包含数据集、业务日期和 SKU/SPU。
- SKU 快递费率导入固定读取名称精确为 `SKU累计` 的工作表，并以 B 列“代码”、M 列“实际金额”、Z 列“合计快递费”和 AA 列“快递费占比”为固定契约。规格代码是事实身份，快递费率必须由 `Z / M` 重算（M 为 0 时按 0）并与 AA 缓存值交叉核验；仅允许同费率或一条完整、一条空白的安全重复行折叠，冲突重复必须拒绝。每次通过校验的文件都是全量权威集合，须使用全局业务内容指纹、owner fencing、原子替换和落库行数回查；负值和超过 100% 的比率保留原值并告警，不得截断。商品经营明细只读取最新已完成批次，列位于退货率之后，未匹配规格显示 `—`。
- 同一月份、日期或快照的内容变化必须在精确业务范围内原子替换完整事实集合；被新版本移除的旧行不能残留，失败不得留下部分事实。大文件和分块上传要保留会话所有权、完成状态和可恢复性，过期 worker 或迟到响应不能覆盖新任务结果。
- 可能重叠的日期、月份或会话集合必须按稳定的业务基域串行抢占写锁，不能把整段范围集合本身当作互斥键；锁所有者必须是唯一导入尝试而不是确定性批次号。锁申请、处理中指纹和尝试审计要原子写入，完成与失败只能由同一尝试释放；事实已发布但响应丢失时，精确重试必须完成原尝试、推进范围状态令牌并另记本次 `duplicate` 尝试。粗范围状态令牌必须固定长度并从锁头 O(1) 读取，不能为每次导入扫描整个历史事实表；事实发布前要续验 owner，并在同一事实发布事务内安装 owner 提交栅栏。30 分钟无进展且状态令牌未变化的 `processing` 尝试才可原子接管，旧 owner 不得写事实、清理或完成新尝试；失败释放必须保留上一个已发布状态令牌。库存行身份使用稳定的“仓库 + 货品”业务键且拒绝同文件重复身份，不能把源行号写入事实键，也不能在 `duplicate` 路径补插事实。
- 多店铺京东任务按店铺串行执行，并使用 `config/jd-store-accounts.json` 中受控的 Chromium 可执行文件、独立 `profileName/profileDir`、调试端口和下载目录。允许四店共享同一 Chromium `userDataDir`，但启动参数必须同时传入 `--user-data-dir` 和唯一的 `--profile-directory`；商品主数据、商智 SKU/SPU 和市场榜单还必须共用一个带活进程校验的全局 Chromium 所有权锁，同一用户数据根目录不得并发启动。京东账号密码不得进入 n8n、注册表、环境变量、命令参数、日志或审计；仅注册表显式设置 `loginMode=windows_dpapi_credentials` 的受控店铺可由当前 Windows 用户绑定的 DPAPI 凭据库自动登录，在唯一账号密码表单中内存解密、填入并最多提交一次，且不得回读字段值。商品主数据导航若先停留在空白业务页、随后才跳转登录页，原首屏查询监听必须保持有效并把延迟跳转交给同一次 DPAPI 登录；登录成功却落到商家首页时，只允许在确认首轮未产生商品查询后单次返回目标商品页，不得再次提交凭据或补点“查询”。验证码、短信、滑块、安全验证、凭据缺失/损坏/拒绝、登录控件歧义或店铺身份不符都必须失败关闭并要求人工处理，不得绕过或在另一店 Profile 重试。京东商品主数据页面禁止使用带 `HeadlessChrome` 特征的模式，日常必须以普通有头 Chromium 后台运行；严格静默模式除离屏启动外，还必须按实际 browser PID 持续隐藏 Windows 顶层窗口，拒绝复用不受本轮窗口守护和执行所有权控制的既有实例。商智 SKU/SPU 分天可继续使用 `headless=new`；每个自动阶段只关闭自己启动的 Chromium，并在释放调试端口后才能进入下一阶段或切店。商品主数据自动流程必须从空白页启动，在唯一受控页面导航前监听首屏 `queryValidProductList`，以首屏响应和页面总数回显作为新鲜度证据；不得先打开未接管的重复商品页，不得在首屏查询后再次补点“查询”。普通流程在登录失效、验证码、安全验证、业务码 `601` 或店铺登录身份异常时，才允许关闭后台实例并打开当前店铺对应的可见 Chromium profile；Chromium 静默副本必须把 `silentNoWindow` 绑定进运行计划、runner 审计和所有子流程，异常时只失败关闭并保留证据，不得打开可见窗口。两种模式都必须保持原任务失败关闭、保留恢复清单并禁止自动重放业务点击；`601` 只能认定为京东服务端风控拒绝，不能仅凭无头、有头、CDP 或点击方式归因，更不能通过反检测或注入式系统输入绕过。发现同端口残留旧 `HeadlessChrome` 时，商品主数据步骤必须先关闭旧实例再启动后台有头实例；切换期间端口被其他进程接管则失败关闭。人工首次登录使用显式 `--interactive-login`。切店后必须验证当前账号/店铺身份；商智恢复清单、建立下载中心 baseline、确认创建任务和点击下载前必须从页头唯一商城链接精确核验 `shopName + shopId`，清单同时绑定 `storeKey + shopName + shopId`；不一致时失败关闭并保留/归档证据，不得接管、提交或下载另一店任务。商智公告必须等同一公告的唯一安全关闭控件连续稳定后才点击，首击无效仅可在公告身份不变时重试一次；跨日后昨天尚未开放时必须按来源未就绪失败关闭，不得缩短日期范围。不得共享会话、下载清单或恢复状态。
- 京准通 AI 推广自动化只允许显式白名单中的 `jd-yiyong-director`（“志高亿用-总监”/“志高商用设备旗舰店”/Default）和 `jd-maidehao-operator1`（“志高迈德豪-运营1”/“志高切肉机旗舰店”/Profile 2）；n8n A 节点必须通过受控请求头显式绑定店铺，后续 B/C 只能使用计划内身份，并与其他京东流程共用全局 Chromium 所有权锁。报表入口必须从当前账号的自定义报表列表按名称唯一选择“AI推广数据自动下载”，不得跨店复用另一账号的报表 ID；账号、报表名称和日期月份等异步渲染控件必须有界等待后精确读回。建立下载中心 baseline、生成任务、恢复任务和点击下载前都必须核验页面账号；只允许按报表名前缀、精确起止日期、创建时间和 baseline 之外的唯一任务接管，任务状态变化不得改变持久任务身份。CSV 必须为 UTF-8、非空、连续覆盖全部目标日且关键表头齐全；导入固定为 `source=jd_promotion`、`dataset=ad`、`platform=京东`，只有文件 SHA-256、行数、店铺、日期、零告警和精确 completed 批次全部回查一致才算完成。两店不得共享 profile、端口、下载目录、恢复清单或任务身份；生成点击未决、多个候选任务、登录或安全验证异常时保留恢复清单并失败关闭，不得重复生成或猜测历史下载。
- 天猫店铺使用共享 Chromium 用户数据根目录中的 Profile 时，注册表必须同时保存 `executablePath + userDataDir + profileName + profileDir`，且 `profileDir` 必须精确等于 `userDataDir/profileName`；启动时必须同时传入 `--user-data-dir` 与 `--profile-directory`。货品、商品日 Cookie 和推广阶段必须复用同一店铺注册项，不能回退到默认 Chrome 或旧的独立 `.runtime` profile。
- 天猫账号密码不得进入 n8n、注册表、环境变量、命令参数、日志或审计。允许的自动登录仅限注册表显式设置 `loginMode=windows_dpapi_credentials` 的受控店铺：凭据由操作者通过 `npm run tmall:credential:setup -- -StoreKey <店铺键>` 交互录入，仅以当前 Windows 用户绑定的 DPAPI 密文保存到 Git 已忽略的 `.runtime/tmall-credentials/`；A 节点只在唯一密码表单、无验证码/安全验证且店铺资源与执行所有权匹配时于内存解密、填入并最多提交一次，提交后不得回读字段值或记录明文。验证码、滑块、短信、安全验证、凭据缺失/损坏、登录按钮缺失/歧义或登录后店铺身份不符都必须失败关闭并要求人工处理，不得绕过、猜测或在其他店铺 Profile/凭据项重试。`saved_browser_credentials` 只作为兼容模式，仍只能检测并提交 Chromium 已自动填充字段，不得读取浏览器密码库。
- 天猫货品、生意参谋商品日和推广导入固定绑定 `platform=天猫`，`shop_name` 必须来自 `config/tmall-store-accounts.json` 中已启用的受控店铺；未提供店铺名时仅允许注册表恰有一个启用店铺的兼容路径，不得接受未注册或未启用店铺。每店使用独立 profile、调试端口、下载目录、签收单和恢复清单并串行运行，切店后必须核验页面可见店铺身份。商品日与推广日下载前必须分别核验当前店铺注册起始日至上海昨天的 Django/PostgreSQL 日期覆盖；按两类缺失日的并集从早到晚选择本轮目标日，保持每个完整 execution 最多一天。B/C 只补商品缺口，P 复核同日商品覆盖后只补推广缺口；已覆盖的无动作跳过不等于新批次导入完成。覆盖读取失败、旧活动清单冲突或空计划复核出现缺口时失败关闭，不得视为空库或清理证据；明确授权重采仍须走受控入口。实际导入的重复判定继续遵守完整业务内容指纹规则。采用与实测边界见 `docs/天猫商品与推广缺失日规划.md`。推广费率和推广成交占比只按两类报表的日期交集计算，商品访客只表达商品×日累计，不得估算为店铺去重 UV。阿里妈妈推广自动化只能在商品日签收导入成功后运行，入口固定为阿里妈妈“报表”→“商品报表”的受控路由；逐店精确核验页面身份与目标日期，营销场景必须全选并逐项回读货品全站推广、关键词推广、人群推广、店铺直达，维度必须同时选择并回读“商品、计划”。DPAPI 登录回跳或单页应用懒加载导致目标日期路由漂移/页面骨架不完整时，只允许一次无业务副作用的精确路由恢复；恢复后必须重新核验店铺身份、目标日期路由以及“商品报表/营销场景/商品数据明细”语义，全部确认前禁止任何报表业务点击，第二次仍未就绪即失败关闭。目标日期按升序逐日串行，每个报表起止日期必须为同一业务日，下载弹窗必须再次核对日期、时间粒度“分天”和“全部数据指标”。确认后只能点击同时包含“离线数据生成成功”和“下载任务管理”语境的成功提示内“立即前往/前往下载”，不得把广告或普通前往按钮当作下载入口；恢复已提交任务时才允许使用受控下载任务地址。下载中心必须按“商品报表”名称、唯一目标日、任务创建时间、成功状态和唯一下载动作锁定本轮任务，不得接管旧全站推或历史任务。ZIP 只有在单 CSV、商品主体、唯一目标日、店铺、大小、行数和哈希均通过后才可继续；商品+计划维度的多行必须先按“日期 + 商品”完整汇总为唯一商品日事实，再导入并完成精确批次和日期覆盖回查。旧协议活动清单一旦进入 `report_submitting`、`report_submitted`、`downloaded` 或 `importing`，新流程必须保留原证据并失败关闭，不得重置、覆盖或重复生成。
- 天猫 n8n execution 在 A 前通过原子协调门禁领取 helper execution owner，领取成功后固定按 A→B→C→P→M 串行；A/B/C/P 每日执行，M 终态入口每日调用，但只有店铺注册表 `productMasterCadence` 到期、存在未决货品活动清单或 n8n 手动完整运行显式发送强制标记时才允许产生货品导出与导入业务动作。亿玖、亿用固定每日到期，成功后从实际完成日顺延 1 个上海日历日；其余四店保持三日错峰：拓丰/炊之王初始到期 2026-08-25，马思图 2026-08-26，丽力 2026-08-27，成功后顺延 3 日。失败不得推进，下一次新的完整 workflow 自动补跑。定时未到期必须返回 `status=not_due`，不创建货品任务、不改节奏状态，但仍关闭本店 Chromium、释放 helper，并可作为该日 M 正常终态；不得使用日历取模 cron 或单独 M 工作流替代该持久门禁。节点之间保持该店独立 Chromium 打开，M 实际成功、`not_due` 或任一阶段失败后，helper 必须按受控店铺注册表中的调试端口关闭该实例，已关闭视为满足。M 失败不得回滚或重标已完成回查的商品日与推广事实，但整个 execution 必须失败并保留货品活动清单；`export_submitted`、`export_confirmed`、`downloaded` 等已发生业务点击的状态只能按原快照续接，不得重置或重复发送导出指令。不得扫描、复用或终止其他 Chromium 实例；关闭失败必须让 execution 失败关闭并保留业务清单和恢复证据。
- 天猫货品活动清单跨日时，`export_submitted`、`export_confirmed` 或 `downloaded` 必须先绑定原快照日续接，不得重新发送导出指令；`export_submitting` 继续失败关闭并要求人工核对，只有未发生业务点击的 `planned`、`browser_ready` 或已完成残留清单才可安全丢弃后开始新快照。
- 天猫平台任务已明确失败，或操作者明确确认某一条旧任务作废时，不得直接删除或重置清单；只允许通过受控作废入口把 `export_submitted` / `export_confirmed` 原样归档并记录作废时间、原阶段和原因，之后再从 n8n 启动新的完整 execution。`export_submitting`、`downloaded`、单纯超时或自动恢复不得使用该入口。
- 上述 `downloaded` 禁止作废规则只有一个逐页 M 专用例外：全部分页任务和文件均已绑定，但合并前明确报出“唯一商品数少于出售中总数”的内容完整性错误时，操作者可显式确认该批作废；受控工具必须原子归档活动清单并保留任务、文件、错误与确认原因，随后仍只能从 n8n 启动新的完整 execution。该入口不得用于超时、网络错误、缺页、点击未决、页码不连续或没有精确内容错误的清单。
- 天猫商品管家发送新导出指令前必须记录当前聊天中的确认、任务受理和完成卡片基线，发送后只允许新增卡片推进状态；页面重排导致旧卡片位置变化不得视为新任务。下载记录时间优先绑定精确的 `exportSubmittedAt`，旧清单缺失时才回退运行开始时间。
- 天猫拓丰、炊之王和马思图货品 M 节点固定使用注册表 `productMasterExportMode=on_sale_pagewise_excel` 的 UI 逐页路径；亿玖、亿用现行唯一发布版本分别使用各店受控 MTOP 直连 M，每 20 个 itemId 串行创建一批导出、逐批校验并合并为一个权威 XLSX，只导入一次。亿玖旧 UI 逐页工作流不得承接新的定时或恢复 execution，只作为历史审计和生成基线保留。UI 逐页店出售中商品按页面显示的总数和 20 条页容量逐页全选并创建 `excel商品批量导出` 任务，最后一页才进入导出记录。进入列表或翻页后必须围绕唯一“共 N 件商品”锚点读取最近分页区域，并在有界等待内连续两次稳定读回相同的“商品总数 + 当前页/总页数”，页面其他区域页码不得混入；固定列或包装节点造成多个“商品标题”DOM 时，只能合并指向同一全选框的等价候选，不同全选框并列时失败关闭，点击后还必须精确读回当前页“已选 N”；“下一页”的按钮、外层分页项和图标父子 DOM 按可操作语义与空间位置归并排序，独立入口同分时失败关闭，点击后仍须稳定读回下一页身份。业务点击前失败只归档脱敏 preflight 证据并释放活动槽。活动清单必须逐页记录任务创建点击前后的阶段；点击结果未决时失败关闭。导出记录只接管本轮时间窗内数量等于总页数的唯一任务集合；全部分页文件分别校验后必须合并成唯一商品数等于出售中总数、无跨页重复的单一权威 XLSX，再一次性导入并回查，禁止逐页导入导致同快照互相替换。丽力继续使用商品管家路径；其他店铺未经单店验证不得切换。
- “工作流”板块中的 n8n 编辑器只允许 `operator`、`admin` 加载，`viewer`、`analyst` 只能查看原生流程概览；即使角色允许，helper 处于 checking、offline、busy 或所需 profile/Cookie 缺失时也不得挂载 iframe 或 n8n 外链。嵌入页必须继续依赖 n8n 自身登录态，不得把 n8n Cookie、账号、密码或 Token 代理进运营系统，也不得在页面加载时自动发布、激活或执行工作流；工作流默认保持 `active=false`。本地 Worker 启动器必须守护只监听 `127.0.0.1:5791` 的一次性辅助进程并在每轮退出后有界退避重启。共享该 helper 的定时和手动 n8n 入口必须先调用受控原子协调领取接口，以 `workflow key + n8n execution ID` 绑定唯一执行所有者；未获授权时只能在 A 前每 5 分钟等待，累计 72 次后以明确错误失败关闭，不能通过先读 `/health` 再调用 A 的检查后执行竞态抢占，也不能让手动分支绕过门禁。同一多段工作流继续以同一 execution ID 贯穿全部业务节点，空 ID、跨执行接管、并发或乱序请求失败关闭。页面健康检查只能返回阶段、忙碌状态和 Cookie/Profile 原文件是否存在，不得返回路径或内容。目标日计划和导入幂等仍由后端按受控店铺、数据集、业务范围和完整规范化内容判定，不得改成前端去重。京东市场榜单的日常生产调度只允许 n8n 工作流 `JdMarketSilentCopy2026` 在上海时区每天 10:30 执行；统一 Codex 监控只核验 n8n execution、业务终态和发送钉钉通知，不得直接调用 `/jd-market/plan`、`/run` 或 `/verify`。恢复继续由 n8n 共享错误工作流从 A 启动新的完整 execution，不能直接重试 B；未激活的 `JdMarketDaily2026` 不得与静默副本同时启用。
- 两条京东京准通推广工作流的 A 节点必须先按受控店铺从 `promotionInitialStartDate` 至上海昨天读取运营系统 `jd_promotion/ad` 日期覆盖，手动范围也执行同一检查；只把缺失日期按升序写入后端计划，每轮最多 31 天。B 必须逐个单日串行下载、校验、导入和回查，不得把多日合并为一个下载任务，也不得下载已有覆盖日；C 必须逐文件复验精确批次并再次核验本轮计划日覆盖。无缺口是正常终态，必须零浏览器、零生成点击、零下载、零导入；覆盖响应范围不一致、含非法日期或导入后仍缺日期时失败关闭。缺口规划、逐日结果、恢复清单和最终回查继续绑定同一 execution、storeKey、shopName 与 shopId，前端不得自行判重。
- 所有 Codex 或 n8n 工作流运行通知遵守 [工作流钉钉通知规则](docs/WORKFLOW_DINGTALK_NOTIFICATIONS.md)：只在整条工作流完成并通过精确批次/覆盖回查及收尾、失败需要人工协助、或持续阻塞无法恢复时发送。禁止逐节点、逐数据集、逐类目、逐归档事项完成通知，以及普通过程、短暂异常、独立恢复和重复汇总消息。吉客云五表、京东四店整条流程各算一个完成单位；天猫和京东 AI 推广按每店独立完整工作流。人工协助立即提醒；安全恢复已用尽、同一卡点连续 3 次检查或 30 分钟无实质进展时知会，正常有界队列等待不算失败。完成与故障按逻辑运行/事件防重，恢复更换 execution 不得重复发送同一事实；同一卡点的人工与持续阻塞提醒合并。无缺口、未到期、重复批次如实并入完整流程结果，不伪报新导入。消息只能由名称精确等于“志高助手”的唯一企业机器人发送到标题精确等于“测试群聊”的唯一群聊；每次发送前动态唯一核验群、机器人及其已安装可用，禁止猜测 ID、回退个人单聊或未经授权 @。认证失效、身份歧义或不可用时停止发送重试，只在当前任务记录脱敏未投递错误，业务诊断和安全恢复继续遵守原门禁。
- 自动化只能复用经过文件名、生成时间、来源店铺、日期覆盖和工作簿内容共同验证的下载文件。不得因文件“看起来最新”就导入，也不得吞掉等待登录、权限不足、下载超时或导入拒绝状态。
- 吉客云五类任务保持既定顺序：货品、分仓库存、库龄、销售、组合装；分仓库存和库龄只有在页面历史日期控件精确读回目标业务日，且本轮 XHR/Fetch 的请求 URL 或载荷同时命中当前模块与目标日期、同一请求收到 2xx 响应并完成后，才可把稳定表格视为该日历史结果；任意 MiniUI grid lifecycle、非 2xx/缺失响应、实时库存、无日期框、日期读回不一致或仅由调用方传入 `snapshotDate` 均不得标记为历史快照。原子 handoff 与 provenance 必须精确绑定 `RUN_ID + module + policyVersion`，文件必须位于 `downloadDirectory/jackyun/<RUN_ID>/<module>/`；OSS 数字对象名不是模块身份，不能再以固定中文文件名替代运行身份。C 节点必须从归档审计重算完整输入证据摘要，与规范路径下的原始 handoff、归档源/输出、文件 SHA/字节数和落库回查交叉核验；非销售批次号必须由归档输出 SHA-256 确定性推导，库存批次还必须是系统当前实际选用的快照，销售批次的 `totals.rawFileHash` 与可选 `systemCost.sourceBatchId` 必须分别绑定本轮输出和本轮库存批次。接受 completed 前缀或 duplicate 前必须复验上述归档证据及原子 handoff。公共自动化入口在启动浏览器和 daily worker 前必须预检任何既有运行契约和 manifest：未显式续跑、策略/日期/模式不一致、completed 前缀损坏或原子 handoff 缺失均须零副作用拒绝。续跑还必须精确匹配快照日、销售截止日、本机 API 根地址和 formal/dry-run 模式；缺少当前运行契约的旧批次不得升级为新口径完成，dry-run `prepared` 也不得原地升级为正式完成。销售渠道白名单和自动化策略从 `config/*.json` 读取。调整白名单或排除仓时要同步领域查询与测试，不能只改某一条导入链路。
- 2026-09-06 用户要求的吉客云 n8n 新模板采用独立协议 `2026-09-06.export-first.1`：先按分仓库存、组合装及子件、发货时间销售明细、库龄、SKU 货品顺序完成五表导出，全部校验后再按货品、库存、库龄、销售、组合装依赖顺序统一导入并独立回查。库存与库龄只标记实际上海采集日的当前查询结果，不冒充历史余额；旧历史协议的日期与网络证据门槛不变。新协议遵循 Django 内容幂等，精确绑定服务端批次、内容哈希及原文件哈希，不再从 XLSX 字节推导批次号。原计划、任务绑定、失败 execution 和审计必须保留，禁止删除 active 或改 ID 重复导出；只有文档中精确绑定原始证据的闭合/续跑/续导例外可以推进。工作流仍为手动、未激活，实际完成状态见下方采用记录及 `docs/吉客云n8n五表先导出后导入.md`。
- 京东/吉客云任务优先遵循仓库内对应的 `.agents/skills/*/SKILL.md` 和 `docs/` 操作手册。修改这些链路时，必须覆盖跨店隔离、重复任务、日期覆盖、恢复清单、批次幂等和“下载后真正导入”的回归测试。
- 吉客云五表网页会话优化使用 `web_session_batch_v1`：一个专用浏览器按既定五表顺序完成查询和导出，调用原启用菜单处理函数，保留平台校验和列定义。提交前持久化任务基线与 intent，任务查询只用网页只读列表接口，按模块、数量、本轮窗口和唯一任务 ID 绑定；提交未决不得重发或回退 UI 点击。新批量计划、旧单表计划和只读诊断相互隔离；Cookie/Token/签名不进入工作流、日志或 Git。库存真实采集日、销售发货区间、原 Excel、全表校验屏障、Django 幂等与精确批次回查不变。组合装 MiniUI 确认按钮必须等原 `onclick` 处理函数完成异步绑定，复验唯一位置和启用状态后只点击一次，并以弹窗消失确认；处理函数消失不等于弹窗消失。单次历史确认失败的审计续跑例外见五表操作文档，不能推广为一般失败自动重发。
- 吉客云专用登录采用 `config/jackyun-login.json` 中的 Windows DPAPI 绑定：凭据由操作者在本机窗口录入，密文仅位于当前用户的 `%LOCALAPPDATA%/TERUISI/JackyunCredentials`，绑定吉客号和专用 Profile，并限制目录 ACL。账号密码不得进入 n8n、Git、命令参数、环境变量、日志、截图或记忆。仅由登录器在内存中解密，最多向已观察到的唯一吉客云三字段表单提交一次；必须先核验浏览器可执行文件、进程用户、Profile、端口、精确 HTTPS origin，提交后从受控菜单和 `#jlink-sn` 验证企业号。空白/加载中有界等待，验证码、拒绝、其他企业号、控件歧义或不确定提交均停止，不回退到其他账号或 Chrome 自动填充。纯登录维护使用同一全局运行锁，不能清除未完成的导出计划或重新触发业务导出。详见 `docs/吉客云DPAPI登录配置.md`。
- 吉客云五表协议允许显式 operator 对首次库存节点的 `inventory 导出未完成：login_unknown` 作无业务效果闭合：`tools/jackyun-preflight-recovery.ts` 必须只读核验本机 n8n 精确失败 execution、无活跃同工作流 execution、原计划及 active 摘要，并证明浏览器事件、下载、演练和导入四类当轮路径均不存在；持有同一运行锁、helper 空闲、计划摘要精确匹配后，只 create-only 写入闭合回执。原计划和 active 字节保持不变，不标为 completed；新完整 n8n execution 只能从 A 重新建计划并原子推进 active，旧 execution 永久拒绝重放。该入口不是跨 execution 接管，不适用于任何导出点击未决、超时、部分文件或已开始导入的情况；上述保留旧证据和禁止直接删除 active 的约束继续适用。
- 上述恢复另支持 842 实际遇到的精确首次库存 `TABLE_TIMEOUT [query_refresh]`（旧诊断“包含目标日期 缺失”）。必须证明唯一 controller 文件只含 inventory/queried、仓库读回和查询超时，时间顺序属于原 execution，且不存在 tableStable、exportIntent、下载、验证、导入或其他模块；闭合回执绑定 controller SHA 并由新计划再次验证。该特例只闭合导出前失败，不能推广到其他超时。当前查询识别补齐实际网关 `/jkyun/erp-stock/warehouseStock/stockSkuList`，仍须同一请求的 HTTP 2xx 与加载完成；历史模式继续要求目标日期。
- 2026-09-06 23:23:54 上海时间，吉客云逻辑运行 `n8n-export-first-844` 已完成五表下载、真实导入和独立批次回查：库存 22,642、库龄 5,554、SKU 货品 8,476、组合装关系 4,392、发货时间销售 4,478 行（9 月 1–5 日；库存/库龄采集日 9 月 6 日）。n8n 845 原 error 保留：它续用 844 的唯一原库存任务，组合装名称含制表符在 Django 写入前被拒绝，经精确审计例外规范展示空白并续导后只运行 verify；不能称为全程无人介入或把 error 改成 success。采用 Worker/helper `20260906T151730Z-e07832f459944ae8`，manifest `6add8a64a22e95f13a65905e242289bdadc981f3413391f8bf739c9454134e85`，source `947e80d83856b5eb2941dd22863ed5f57d28598d`，Status/启动绑定/readiness 通过且 helper 空闲。库存/库龄精确历史查询通过同 revision 的 `freshness` 与无筛选、limit=1 的 `inventory_search/age_search` 补齐当前批次实际归属行数；不得从声明 rowCount、SKU 聚合投影或其他批次推算。旧 841–843 及原失败证据保留，日调度仍未启用。完整证据及恢复边界见五表文档。
- 未经用户明确授权，不删除、改写或重新导入业务数据；需要清理时必须先只读定位精确批次与影响行数，并提供清理后的同范围验证。

### 天猫逐日补缺候选与采用边界

2026-09-09 用户已授权开发六店逐日补缺循环。仓库模板现在使用 `/plan-backfill` 和 `/next-day`，`A → (B → C → P → N)循环 → M`，每个 B/C/P 仍限定一天；每店最多 14 天，A 开始 30 分钟后不再开启下一日，M 按原节奏仅执行一次并收尾。旧单日 `/plan` 仅供尚未更新的旧发布版本兼容；新模板在旧 helper 必须以 404 拒绝。只有 helper 与六个原 n8n ID 一起受控采用并回读后，本规则才替代上文“完整 execution 最多一天”的生产约束。N 必须重新查询两类权威覆盖、核验完成日期未回退，并绑定 execution、店铺和单调循环序号；无进展或预算耗尽不得伪报补齐。新增接口不授权监控绕过 n8n；进度文件不是第二事实源。M 后若仍有剩余缺口，n8n 必须明确失败并报告范围。发布与验证见 `docs/天猫商品与推广缺失日规划.md`。

## 6. 鉴权、密钥与敏感数据

- 应用角色固定为 `viewer`、`analyst`、`operator`、`admin`。所有权限和数据 scope 均使用服务端 `requireAppPrincipal()` 得到的真实身份；客户端、模型参数和请求正文中的身份/角色声明不可信。
- 读取也要应用 principal scope；写入、配置、导入、发布、回滚、删除等操作按现有角色契约收紧，不能为了修复页面流程绕过鉴权。
- `.dev.vars`、API Key、Token、Webhook、AES Key、浏览器登录状态和原始客户聊天不得提交、打印到日志、写进审计摘要或持久记忆。列表接口只返回掩码。
- AI 模型域名若被代理 DNS 返回为 `198.18.0.0/15` 虚拟地址，只能对已有精确 HTTPS origin 白名单中的域名，通过固定公网 HTTPS DNS 取得并校验真实公网单播 IP 后固定连接；保持原域名 TLS 校验、总超时、响应上限、禁止重定向和模型 POST 不自动重发。不得放行虚拟地址段、私网或关闭证书验证；DNS 查询不得携带模型密钥和对话正文，内部回环工具桥不走该恢复路径。说明见 `docs/AI_ASSISTANT_SETUP.md`。
- 本地免登录管理员仅在 `TERUISI_LOCAL_DIRECT_ACCESS=true`、`TERUISI_RUNTIME_ENV=development` 与真实开发/受控本地构建标记同时满足时可用，并必须在 Worker 入口和身份解析两层把请求限制到精确回环地址；Host 不能作为开发证明，LAN 地址、任意域名和 DNS rebinding 必须失败关闭。所有非 Webhook AI 写请求还必须提供精确同源 `Origin` 或明确的 `Sec-Fetch-Site: same-origin`；生产环境必须保持拒绝匿名直连。
- 外部回调必须验签、解密、校验接收方并防重。聊天平台消息不能绕过后台权限直接修改运营数据。

## 7. 中央 AI 工具注册表

- AI 面向用户的自我介绍、系统称呼和连接测试文案使用中文名称，不添加 TERUISI 英文品牌前缀。保留既有内部标识、协议字段、数据库角色、环境变量及路径；不得以清理显示文案为由改动这些运行契约。

- 钉钉无响应排障必须区分 Stream 建联、callback 接收/拒绝、持久入队、模型处理与外部投递。进程 running、历史 connected 或定时任务 sent 不能替代新聊天验收；使用 `callback_received/accepted/rejected/unavailable` 固定标签及账本状态定位，不输出原始消息、身份 ID、凭据、Webhook 或 ticket，不自动重放旧消息和未知结果。诊断日志上线不等于聊天故障已修复，说明见 `docs/DINGTALK_READONLY_ASK.md`。

- `run_pandas_analysis` 的 Python 代码只能在独立 Linux/rootless 容器执行；Worker 与 Django 仅做权限内数据集导出、签名传输和被动结果验证，不得使用宿主 Python 或 AST/eval 过滤模拟隔离。容器必须无网络、无宿主挂载/业务凭据、固定镜像与资源配额，成功或失败都须核验精确清理；源分页/字段截断、权限变化、未知执行或清理失败均失败关闭。源码接入不代表独立运行环境已部署，采用门禁见 `docs/AI_PANDAS_SANDBOX.md`。

- 钉钉“志高助手”问数使用独立 `dingtalk_chat` surface，在中央注册表逐项开放跨系统领域的只读工具，嵌套数据集查询必须保留此 surface；不得把未来新增工具自动开放给机器人。AI 群设置独立于周报，由 AI 0008 的 `ai_dingtalk_settings` 保存，仅无范围限制的管理员可改；受控接收器首次启动采用 runtime 中已有应用身份和群，后续启动不得覆盖管理员设置。组织/应用/staffId 与系统 principal 仍显式绑定，接收、执行、发送前重查；群名、精确 ID 和机器人入群关系在外发前动态核验。群内 @ 的回执和结果回复原群，私聊回复原提问者；禁止失败后改投私聊或其他群。群、用户和单聊历史隔离，配置版本变化撤销旧请求，不附加个人记忆或知识库，不开放凭据、原始客户聊天或其他用户私有内容。接收/投递账本属于 Django AI 域，使用既有 authority，外发未知结果不重试。新增表进入角色、readiness、备份和恢复清单；旧 45/46/48 表备份按其迁移版本继续验证。此增强须经受控发布后才改变首版生产行为，未经授权不得启动真实监听。销售品牌仍按 ERP 当前品牌精确关联。详见 `docs/DINGTALK_READONLY_ASK.md`。

- 所有供模型调用的系统能力必须且只能在 `lib/ai/tool-registry.ts` 声明一次。不得从数据库表、任意 SQL、API 路由或 handler 自动暴露工具。
- 每个条目必须包含稳定名称、标题、精确描述、`additionalProperties: false` 的对象 JSON Schema、允许角色、`scopePolicy`、`risk`、annotations、有界执行策略和可调用 handler。
- 执行策略必须明确入口 surface、超时、响应字符上限和单请求调用上限。内联直调只允许只读工具；写入或危险工具必须走人工确认或持久化后台任务，不能伪装成只读。
- 工具执行使用真实 principal 做角色和 scope 校验，输入和输出均有界且可取消。审计必须记录真实 actor、surface、request/invocation ID、脱敏参数摘要、状态、行数、耗时、响应摘要和错误码；审计不可用时应失败关闭。
- 每次注册表变更都要测试：名称唯一且条目完整、OpenAI/Anthropic schema 一致、handler 存在、角色与 scope 过滤、surface 和风险限制、参数/结果/调用次数边界，以及 registry/catalog/execution 同步。新增可检索业务数据却没有注册有界只读工具，或只有 schema 没有 handler，均视为失败。

## 8. D1、R2、迁移与缓存

- 当前所有结构化业务事实、配置、批次与运行审计均以 PostgreSQL 为权威。原始分片使用各域 PostgreSQL 的有界、可过期会话；AI 图片字节也以 PostgreSQL 为权威。D1 中的 tombstone、永久 guard、完成 receipt 和受保护旧对象只属于防复活、迁移和历史审计材料，不得作为读取、写入或回滚事实源。市场消费的网店兼容投影由来源固定的 Django netshop consumer 原子写入 PostgreSQL 市场表，不是网店第二事实源；商品经营消费的库存投影也不形成库存第二事实源。R2 继续保存市场/网店图片和运营事务附件等仍有效的对象，但销售、库存、新品项目、ERP、客服和 AI 退役前缀不再读写 R2；运营事务附件仅保留字节，文件名、MIME、大小、SHA-256、对象键与清理状态均以 PostgreSQL 为权威。不得从全局配置移除其他模块仍在使用的 R2 binding。
- 当前业务结构变更只使用新的 Django migrations，不得改写已应用版本或新增 D1 业务迁移、运行时 `ensure*Schema()`。历史 `drizzle/*.sql` 及相关升级验证只供隔离迁移、退役审计与测试保留，不进入生产构建或普通迁移链。
- 迁移先补列/补表、回填和去重，再创建依赖新结构的索引或唯一约束。升级必须可重复执行，并保护已有人工确认、审计和批次历史。
- 业务原子发布使用各域 PostgreSQL 事务；长任务继续使用租约、owner/execution token 或等价 fencing，防止旧 worker、重试和响应丢失造成 ABA 或迟到覆盖。
- 查询必须保持参数、分页窗口、结果体和执行时间有界。D1 表达式深度与复合查询项限制仅属于隔离历史实现的回归契约，不得为沿用旧 D1 限制恢复生产旧查询路径。
- 有效指标、月度汇总和 overview 响应缓存都只是派生数据。任何影响结果的事实、价格、图片状态、映射或主数据变更必须递增版本或精确失效；版本不一致、构建未完成或租约失效时不得返回旧缓存。

- 全系统生产入口 `app/`、`worker/` 及其传递依赖必须通过 `npm run check:backend-boundary`，包含动态导入检查。源码和本机已采用的生产 release 均无 D1 业务访问，`.openai/hosting.json`/Vite 不再绑定 D1；构建包不得复制 Drizzle 迁移。D1 退役 tombstone、guard、历史迁移与证据仅保留在隔离审计/测试面，不据此删除实体数据库或 R2。后续更新仍须按 `docs/DJANGO_AGGREGATE_CUTOVER.md` 受控发布并真实回读。
- Worker readiness 使用已配置的 23 个 Django reader/writer 健康端点，按服务角色核验并有界取消；失败返回 `django_unavailable`，总控显示 `BackendDegraded`。liveness 与 readiness 必须保持独立，不能因就绪探测失败重启服务。
- 2026-09-06，本机 D1 控制链脱钩已正式采用，effective release 为 `20260906T035823Z-fceee410b71f79b0`，manifest SHA 为 `ab44b00f096534c97651108c4bb75ea6d9343eb1d6d66f3f96a3b043dd3ea492`，全局退役 proof SHA 为 `24503a096bb6649b4d4f0988ccfaab9924100232129daa13f70edf78e1f22c52`。首次采用只读复验 12 个终态单元、23 个 PostgreSQL 服务 readiness 和既有保留证据；日常启动、自动子进程恢复及后续 successor 发布继承同一不可变证明，不再打开历史 D1。Django v5 的历史 D1 路径仅为元数据，v6 可不配置该路径；显式历史 operator 仍严格检查来源。无 D1 镜像、真实子进程恢复、正式启动项和 API 回读均通过，旧 release 拒绝启动。不得跳过 guard、把 completed 写死、删改历史 manifest/authority，或恢复 D1/legacy/fallback/双写。历史 D1 及审计材料保留，物理销毁和远程部署不在本次范围。实现、备份恢复与正式证据见 `docs/GLOBAL_D1_CONTROL_RETIREMENT.md`；`docs/GLOBAL_D1_RETIREMENT_ASSESSMENT.md` 仅描述采用前的核查。

## 9. API、前端与性能要求

- API 响应默认 `no-store`，除非实现了显式版本指纹、失效策略和并发复用的持久缓存。缓存不能绕过身份、scope 或数据新鲜度。
- 列表和搜索在服务端筛选、分页并设置硬上限，同时返回 `total/returned/truncated` 等必要元数据；不得把数万行完整数据发到浏览器再筛选。
- 用户连续切换筛选时取消旧请求，并用 request key/generation 等门禁阻止迟到响应覆盖新状态。不能只靠前端防抖掩盖慢查询。
- 市场 SKU 图片 AI 标注的模型并发数按“三级类目 + 执行器”持久化，范围为 1–50；云端默认 10、建议 10–20，本地 Ollama 默认且建议 1。Cloudflare `scheduled()` 只允许唤醒 Django 持有的市场队列，页面只允许启动/暂停和轮询进度，不得把调度依赖浏览器存活。运行状态、自适应并发、冷却截止时间和唯一协调租约必须持久化到 PostgreSQL 市场域；重叠定时触发、页面启动与暂停交错时仍由协调租约和单图租约双重防重。新任务设置与当前任务控制必须分区展示，当前任务运行中保存并发后要更新服务端租约上限，后台在下一批即时应用。每个模型 HTTP 请求只处理一张图片，服务端必须使用同一持久化配置和原子条件限制任务的有效推理租约，不能只靠调度器并发数防越界；同一任务中“类目 + scope + SKU + 榜单维度 + 图片哈希”相同的跨月记录只能领取一个推理单元，成功结果要批量扩散，扩散失败必须可恢复且不得重复调用模型。视觉模型调用、启动页面请求与推理租约依次限定为最多 90 秒、110 秒和 3 分钟。单次模型超时或网络异常先把当前运行并发降到约 75%（配置不少于 4 时首次不低于 2），从 5 秒短冷却开始，重复异常再阶梯减半且普通异常冷却最多 30 秒；普通异常的冷却只作用于触发异常的通道，其他通道继续运行，同一冷却窗口内并发返回的同类失败只计为一次退避事件。供应商限流按当前并发减半并使用 60 秒至 5 分钟全局有界退避或供应商 `Retry-After`；租约占满只让对应通道短等，不得暂停其他通道。连接每连续成功 3 张增加一路，直至恢复该类目的配置值；运行并发已为 1 后若没有成功图片又累计 3 个独立失败窗口，后台必须自动暂停，显示服务端返回的有界脱敏错误码与原因并要求人工检查后续跑，不能无限重试。续跑必须复用兼容且仍有可重试推理单元的原任务；单任务最多 10,000 条，`review_ready`、`committing` 或失败封顶的旧批次不得占用可运行工作键或拦截下一批，唤醒无可重试单元的任务必须失败关闭并提示创建下一批。不得因客户端超时重复创建仍在运行的任务或覆盖已完成结果。模型输入图可使用 Cloudflare Images 将最长边缩至 1600 像素并编码为质量 88 的 WebP，但只在衍生图小于原图时采用；转换失败或结果无效必须回退原图，衍生图可按原图内容哈希持久化复用，不得改变业务图片哈希、正式价格继承身份或人工复核所见原图。豆包 Seed 系列的该类严格 JSON 分类/抽取请求必须关闭额外思考，完整分类输出上限为 600 tokens，仅价格识别上限为 320 tokens。单图取图、图片处理、模型调用、总耗时和输入字节数要持久化，页面至少展示最近 100 张的分阶段平均值。
- 市场 AI 待标注总量必须按类目、scope、SKU/SPU 维度和商品编码去重，并互斥拆分为同图直接复用、新图仅识别价格、完整分类与价格、暂不可自动识别；同一身份跨月份命中多种路径时按算力需求“完整识别 > 仅价格识别 > 同图复用”归类，四类合计必须等于待标注总量。无图、非 SKU、有效 Prompt 缺失或失败封顶归入阻塞，不得误报为模型调用量。
- 新查询要避免逐行 N+1、无界 `IN`、重复全表迁移和无变化 UPDATE。对高频聚合优先使用有版本失效的派生表或响应缓存，并用真实规模数据比较结果等价性和时延。
- 全局搜索新增领域时同步更新 `lib/search/` 覆盖、权限、结果边界和文档；数据应可被 AI 查询时还要按第 7 节接入中央注册表。

## 10. 验证与本地服务生命周期

- 根据改动范围运行最小充分验证：优先相关测试，再运行 `npm run test:unit`、`npm run lint` 和 `git diff --check`。仅文档变更无需运行生产构建，但必须检查链接、命令和当前代码一致。
- `npm test` 会先执行生产构建。生产构建会改写 `dist`，而本地预构建 Worker 会监听该目录；构建前必须检查 `127.0.0.1:3000` 是否被本项目服务占用。
- 未经用户在当前对话中明确批准，不得停止、重启或中断本地服务，也不得在运行中的 Worker 监听构建产物时原地构建。当前本机正式 Worker 只能由 `tools/worker-local-service.ps1` 解析并管理不可变 effective head；禁止直接运行 Wrangler、旧 release、`dist` 或 `tools/start-local-worker.mjs`。
- 日常人工启动、`运行项目.bat`、`npm start`、`npm run dev`、登录启动项和桌面控制面板必须统一汇聚到 `tools/worker-local-service.ps1 -Action Start` 这一唯一启动引擎。该引擎先只读核验已部署 Django/PostgreSQL 的全部已启用 reader/writer 与 BI reader；仅在状态明确未就绪时调用既有 Django `Start`，全部回读 ready 后才进入原不可变 Worker release/进程身份门禁。`tools/operations-system-control.ps1` 只提供组合状态、外层非阻塞互斥、日志和最终 HTTP 回查，启动时不得复制 Django 或 Worker 的写入/生命周期逻辑。只有 Worker 内部状态明确为 `stale_or_invalid_receipt`、受保护回执可验证且 supervisor 不存在时，唯一引擎才可通过既有精确回执删除函数清理该回执，并必须回读为 `stopped`；端口占用、身份不明、回执损坏、状态错误或健康异常均失败关闭。
- 一次顶层 Django `Start` 或 `Stop` 中，完整 runtime 后代 ACL 审计只执行一次；同进程子域只能使用临时、不落盘、有界且同时绑定 PID、runtime 路径和部署清单的本轮证据复用完整审计结果，并必须重新验证根 ACL 与应用树。单独子域操作、跨进程、超时、绑定变化或上下文损坏必须回退到完整 ACL 审计，不得使用持久缓存跳过生命周期安全门禁。Worker release 完整性验证可以对文件元数据和内容读取使用有界并发，但不得改变已发布 tree-hash 的 ordinal path/length/content 协议、减少校验文件或放宽重解析点和硬链接门禁。
- 不得为了测试而覆盖本机 PostgreSQL/Django runtime、Worker release runtime、D1/R2、浏览器 profile、下载目录、备份、审计或 `.dev.vars`。测试优先使用临时目录、内存/临时 SQLite 和夹具。
- 未经明确要求，不执行生产部署、远程 D1 迁移、生产数据导入或外部平台真实发送。涉及这类动作时，应先说明目标环境、影响范围和验证/回滚方案。
- 修改注册表、鉴权、导入、市场迁移、缓存或自动化状态机时，必须增加针对失败、重试、重复、跨范围和并发交错的负向测试，不能只验证成功路径。

## 11. Git 与交付

- 市场榜单分页维护必须保持数据库内去重、正式价格与投影后排序的口径，只为当前页查询关联销售；不得通过提高原始记录上限或返回截断统计冒充完整结果。首次读取失败仍须保留日期、筛选及子菜单，选项独立读取；迟到结果不得覆盖新范围。行业汇报的有界计算与榜单分页分别验收，细节见 `docs/MARKET_QUERY_PAGINATION.md`。

- 每次源码或项目配置修改完成后，创建一个聚焦、可审查的 Git 提交，并推送到已配置远端；文档/规范修改也属于项目配置修改。
- 只暂存本任务文件。推送前再次核验 `git diff --cached`，避免纳入用户已有改动、生成文件、下载文件、凭证或运行产物。
- 不使用破坏性 Git 命令清理工作区。没有明确要求时不改写历史、不强推、不切换或删除用户分支。
- 交付说明必须列出：变更内容、验证结果、提交与推送状态、未执行的部署/迁移/服务操作，以及仍需用户决定的风险。

## 12. 任务结束与长期记忆

本项目不再读取或写入外部 Obsidian 作为长期记忆。任务结束时，只在仓库内维护以下两个入口：

- `README.md`：记录面向使用者的当前系统说明、启动方式、主要能力、必要操作和已确认限制。
- `AGENTS.md`：记录面向开发与自动化协作的稳定业务口径、工程约束、安全边界、验证和交付流程。

只有信息具备长期、稳定、可复用价值时才更新这两个文件；临时状态、单次运行结果、未经确认的推测和冗长过程不写入。禁止记录密码、令牌、个人敏感信息或原始客户数据。若任务没有产生需要长期保留的新信息，不为“留痕”而修改文档。
