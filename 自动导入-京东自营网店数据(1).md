# 自动导入：京东-特睿思自营店 → 系统网店数据（8 类 12 个文件）

> 工作日 09:00 定时任务的执行手册。2026-07-14 全流程跑通验证（批次 1768–1778、1783，零错误）。
> 执行方式：Cowork 定时任务由 Claude 按本手册用 Chrome 自动化执行。
> 所有京东后台操作都在资料卡/账号【特睿思自营】下进行（页面右上角确认）。

## 前置条件

- Chrome 已装 Claude 扩展；京东各后台（商智/京准通/京麦/客服管家）保持登录态。掉线需人工扫码/短信时**停止并通知用户**。
- 本地系统运行中：<http://localhost:3000>。当前项目已兼容旧路径 `/api/netshop/import/`、`/api/netshop/overview/`、`/api/inventory/import/`。
- `~/Downloads` 已授权（未授权则 request_cowork_directory 请求）。

## 阶段 A：取日期范围

在 localhost 标签页 JS 执行：
`await fetch('/api/netshop/overview/?shop='+encodeURIComponent('京东-特睿思自营店'),{credentials:'include'}).then(r=>r.json())`
各数据集 date_max（trade_overview / sku_daily / spu_daily / ad / b2b / cs_chat / cs_order / cs_suggest）。
**范围 = date_max+1 至 昨天**；若某数据集已到昨天则跳过对应下载。范围恰为昨天时，各站点直接用「昨天」快捷选项最省事。

## 阶段 B：六个模块的下载

### 1. 商智·交易概况（→ jd_shop_overview）

`https://jdsz.jd.com/szweb/view/tradeAnalysis/tradeSummary.html`
右上日期选「昨天」（多天用「自定义」）→ 核心指标卡右上「下载数据」→ 下拉选「分天下载」→ 弹窗「下载范围=不包括对比时间」（默认）→ 确定 → **直接下载**到 ~/Downloads（`特睿思自营_交易概况_…分天下载_起_止.xlsx`）。

### 2. 商智·商品明细 SKU+SPU（→ jd_sku_daily，两个文件同一 source 自动识别）

`https://jdsz.jd.com/szweb/view/product/productDetail.html`
日期选「昨天」→ 当前 SKU 卡：「下载数据」→ 弹窗**下载类型选「分天下载」**（默认是汇总下载，务必改！）、范围=不包含对比时间 → 确定 → 弹「操作成功…已创建」点关闭。
切「SPU」卡重复一遍（设置会记住分天下载，仍需核对）。
下载中心 `https://jdsz.jd.com/szweb/view/reports-center/download-center.html`：确认 2 条「…分天下载_起_止」状态已生成（生成中就点刷新等），逐条点「下载」。⚠️ 文件名不含 SKU/SPU 字样，两个都下即可，导入器按表头自动识别。

### 3. 京准通·推广报表（→ jd_promotion）

`https://jzt.jd.com/custom-report/#/createNew?id=1965817&businessFrom=list`（自定义报表「SUK报表」）
点日期区间选择器 → 快捷选「昨天」→ 表格右上下载图标 → 输入框旁「开始生成」→ 自动跳「下载报表」页 → 首行（刚生成的）点「下载」→ CSV 落盘（`特睿思自营_SUK报表_…csv`）。

### 4. 商智·企业购商品明细日报（→ jd_b2b）

`https://jdsz.jd.com/szweb/view/reports-center/recommend-report-temp.html`
滚动找「企业购 商品明细报表」卡 → 查看详情（新标签页）→ 统计时间默认昨天（多天则自定义）→ 展现方式下拉选「**按天展示**」→ 「下载报表」→ 弹窗「已生成…前往我的报表」点「查看」（新标签页 我的报表）→ 首行状态「生成中」时等 10 秒点「刷新报表」→ 已生成后点「下载」（`企业购-商品明细日报-….zip/xlsx`）。

### 5. 京麦·商品SKU信息（→ jd_product_master，主数据无日期）

`https://wares-jdm.jd.com/ware/wareList?businessModel=1&activeTab=AllWare`
「导出查询商品」→ 抽屉里「确定导出」→ 导出记录首行「处理中」→ 等 10 秒点「刷新列表」→ 已完成后点「下载」（`导出商品VC-SKU信息_….xlsx`）。

### 6. 客服管家·接待数据三项（→ jd_cs，三个文件同一 source 自动识别）

`https://kf.jd.com/#/72`（咚咚查询-促成订单查询）：
- 促成订单 tab：**日期模式下拉（默认"昨天"）改选「近30天」**（订单归因滞后，30 天滚动窗口幂等覆盖）→ 查询 → 「下载/导出excel」按钮 → 变「准备中...」→ **最长约 2 分钟**变绿色「下载」→ 点下载（`促成订单_….xlsx`）。
- 切「推荐订单」tab：同样**日期下拉改「近30天」**→ 查询 → 导出Excel（此 tab 通常直接落盘，`suggest_….xls`）。
- 左侧「客户咨询查询」：日期**保持昨天**（会话量大，且无归因滞后问题，不用改30天）→ 查询 → 导出excel → 弹「共N条，是否确认导出」点确认 → 「准备中...」等约 1–2 分钟变「下载」→ 点下载（`会话记录_….xlsx`）。
- 「准备中」超过 5 分钟视为卡死：刷新页面重新导出一次。

### 7. 商智·自营纵版 自营库存 RDC+配送中心（→ inv_selfop，两个文件，快照=昨天）

`https://jdsz.jd.com/szweb/view/reports-center/recommend-report-scpro-detail-temp.html`（供应链库存-商品明细日报表）
时间粒度保持「实时」不变。默认「中小件数据粒度=RDC汇总」：
1. 「下载数据」→ 下拉选「**全部纵版**」→ 弹「下载数据已生成」→ 点「取消」。
2. 「中小件数据粒度」下拉切「配送中心」→ 「下载数据」→ 「全部纵版」→ 弹窗点「**前往查看**」→ 新标签页打开「我的报表」。
3. 我的报表：确认两条「全部纵版-日期-全部-全部-RDC/配送中心-纵版-实时」，生成中则等 10 秒点「刷新报表」；都「已生成」后依次点「下载」直接落盘（`全部纵版-YYYYMMDD-全部-全部-RDC-纵版-实时-xxx.xlsx` 与 `…配送中心….xlsx`）。
⚠️ 这两份是**库存快照**，走库存接口而非网店接口，导入时快照日期填**昨天**；文件名里的日期是数据日（昨天），RDC/配送中心由导入器按文件名自动识别。

### 8. 亿美插件·三方导出SKU（→ jd_yimei_sku，主数据无日期）

`http://ym.shujymei.com/main.php?c=index&s=ymhome`（亿美店长，登录态为特睿思京东自营店）
1. 若首页不是「导出SKU」页：鼠标悬停顶部导航「商 品」→ 出现下拉后点「商品导出」（**带【新】标的那个**；若列表只剩一个商品导出就直接点它）。
2. 「导出的字段」区：点「选择已有模板」下拉 → 选「**全部**」（约 47 项字段全勾上）。
3. 页面下滑到底 → 点「开始导出」→ 自动跳到「导出记录」tab（看不到列表就往上滑）。
4. 首行任务「执行中」带进度条，**导出较慢（约 1–2 分钟，607 个商品）**，每 30 秒看一次；「已完成」后点「下载」直接落盘（`vc_product_export_任务ID_日期.xlsx`，约 1.8MB）。

## 阶段 C：核对文件落地

bash 检查 ~/Downloads 最新文件，应有 **12 份**：交易概况、商品明细×2、SUK报表 csv、企业购 zip/xlsx、VC-SKU信息、促成订单、suggest、会话记录、全部纵版 RDC、全部纵版 配送中心、亿美导出SKU（网店接口 10 份 + 库存接口 2 份）。

## 阶段 D：API 直传导入

在 localhost:3000 标签页注入临时文件框（同 ERP 手册）：
`let inp=document.getElementById('claude_api_upload'); if(!inp){…createElement('input')…}`
每个文件：file_upload 挂上 → JS POST `/api/netshop/import/`，form 字段：

- `file`＝文件
- `source`＝见下表
- `platform`＝`京东自营`，`shop_name`＝`特睿思（TERUISI）京东自营旗舰店`（缺一不可，映射见 系统设置-网店配置）
- `note`＝'定时任务自动导入'
- 当前项目本地直连模式不需要旧 Django 的 `X-CSRFToken`；保留 `credentials:'include'` 即可。若后续恢复权限校验，403 时先确认当前系统登录态。

| 文件（~/Downloads 中的文件名特征） | source | 接口 | 额外参数 |
|---|---|---|---|
| `特睿思自营_交易概况_…分天下载_起_止.xlsx` | jd_shop_overview | /api/netshop/import/ | platform+shop_name |
| `特睿思自营_商品明细_…分天下载_起_止_xxx.xlsx` ×2（SKU/SPU 自动识别） | jd_sku_daily | /api/netshop/import/ | platform+shop_name |
| `特睿思自营_SUK报表_起_止_….csv` | jd_promotion | /api/netshop/import/ | platform+shop_name |
| `企业购-商品明细日报-….zip/xlsx` | jd_b2b | /api/netshop/import/ | platform+shop_name |
| `导出商品VC-SKU信息_….xlsx` | jd_product_master | /api/netshop/import/ | platform+shop_name |
| `促成订单_….xlsx` / `suggest_….xls` / `会话记录_….xlsx`（自动识别） | jd_cs | /api/netshop/import/ | platform+shop_name |
| `vc_product_export_任务ID_日期.xlsx`（亿美） | jd_yimei_sku | /api/netshop/import/ | platform+shop_name |
| `全部纵版-YYYYMMDD-全部-全部-RDC-纵版-实时-xxx.xlsx` | inv_selfop | **/api/inventory/import/** | **snapshot_date=昨天**，无 platform/shop_name |
| `全部纵版-YYYYMMDD-全部-全部-配送中心-纵版-实时-xxx.xlsx` | inv_selfop | **/api/inventory/import/** | **snapshot_date=昨天**，无 platform/shop_name |

自营纵版两份即 ERP 手册里的 claudeInvImport 方式：form 字段 `file` + `source=inv_selfop` + `snapshot_date=昨天(YYYY-MM-DD)` + `note`。RDC 与配送中心各 POST 一次，快照日期相同，仓别由导入器按文件名自动识别。

导入顺序无强依赖，建议主数据（jd_product_master）在前。全部导完后重新 GET `/api/netshop/overview/?shop=京东-特睿思自营店` 核对各 date_max=昨天。

## 结果通报

- 逐类汇报 批次号/数据行/新增/更新/错误行；核对后的各数据集 date_max。
- 有错误行列出明细；任何环节卡住（需扫码登录、页面改版找不到按钮等）停止并说明，不要盲目重试。
- 全部完成后关闭本次会话创建的浏览器标签组。

## 注意

- 所有导入幂等，重复导入同一文件不会翻倍，可放心重试。
- 商品明细「下载数据」弹窗默认是**汇总下载**，忘记切「分天下载」会导致导入缺日期列（错误的任务留在下载中心不必理会，不要下载即可）。
- 企业购导出：商智仅支持近期日期，历史范围过长可能受限。
- 商智下载中心/我的报表 只保留 30 天内最新 100 份文件。

## 2026-07-14 跑通记录（核对基准，范围 07-13 一天）

| 数据 | 批次 | 行数 |
|---|---|---|
| 交易概况 | 1768 | 1 |
| 商品明细 SKU | 1769 | 199 |
| 商品明细 SPU | 1770 | 905 |
| 推广 SUK报表 | 1771 | 368 |
| 企业购日报 | 1772 | 11 |
| VC-SKU信息 | 1773 | 2529（新增1/更新2528） |
| 客服·促成订单 | 1774 | 9 |
| 客服·推荐订单 | 1775 | 3 |
| 客服·会话记录 | 1776 | 238 |
| 自营纵版 RDC（快照 07-13） | 1777 | 3960 |
| 自营纵版 配送中心（快照 07-13） | 1778 | 9327 |
| 亿美导出SKU | 1783 | 2529 |
