# finance.0006 原始工作簿字节验真侧车（默认关闭）

普通月度财报仍按 `finance-normalized-v1` 导入和返回。只有前后端均显式开启 `TERUISI_FINANCE_RAW_WORKBOOK_BYTES_V2_ENABLED`、且 v1 **新导入 201 或同原文件完整单月 duplicate 200／同请求 replay 201** 返回精确当前批次、月份、文件名与长度时，Next 才在同一次请求保留的原字节上追加签名 `application/octet-stream` POST。首次 v1 已提交但二次验真失败时，重传相同原文件可重试侧车；Django 仍以真实字节 SHA、当前 month→batch 和 finance revision CAS 拒旧批次或其他文件，失败只让侧车保持 `unknown`，不回滚或改写已成功的 v1 导入。`.xls`、多月批次和重复月 sheet 保持 v1 行为，但本侧车拒绝。

后端只接受最多 8 MiB 的 `.xlsx` ZIP 字节，并在读取前核 Content-Length；现有签名对精确路径、查询、管理员身份、request ID 和完整原字节 SHA-256 做 HMAC，后端再次计算请求体 SHA。独立的 `workbook_bytes_v2.py` 不使用 Worker 的 SheetJS JSON：它有界读取 OOXML workbook、关系、工作表和格，拒重复/越界/加密 ZIP 成员、超量展开、DTD/实体、重复格、公式与需跨语言舍入的金额或费率。原文件不入库。严格容量或口径以外的真实工作簿不会被推断为已验证。

`finance.0006` 增加只追加的字节观察父行、物理列和物理格三表，保存后端观察 SHA、完整 sheet 目录摘要、解析合同、当前批次与 finance revision、原始列/格的摘要，以及供未来受限财报读取的分列数值。写事务先锁 FinanceMonth→当前 FinanceImportBatch→FinanceDataRevision；服务从字节重建 0005 证据，0005 缺失时原子追加其三表，已有时逐字段和逐列格摘要一致才追加 0006，任何失败两侧车都不留下半行。读取重核当前发布批次、revision 与子行链。旧 `FinanceLine`、`FinanceMonth`、`FinanceImportBatch` 完全不修改。

旧 v1 解析以 `(section,scopeKey,subjectName)` 汇总，跨组同名店可被合并。本侧车的 `(section,rowIndex,columnIndex)` 物理格和组名保留 D/F 两列各自金额，并回卷确认它们之和等于旧规范行；旧行仍是合并行，不能将其用于独立店铺财报。0005 的数据库约束仍固定 `raw_workbook_bytes_verified=false`，仅 0006 记录后端本次观察字节；不保存原件，不能事后重放历史月份。`stableNetshopShopIdentityVerified`、`mappingAuthorityVerified` 和报告权限继续为 false。

部署边界仍关闭：新签名 Django 路由默认返回 404，0006 三表拒绝直接 finance/AI reader 与 writer 的 DML，当前拥有方仅在隔离 test/development 显式开启。正式 PrepareApp、备份和恢复遇到 0006 源码、收据或任一表提前拒绝。正式启用仍须独立受限 attestor 写身份、精确权限/备份恢复与开关发布；当前 finance_writer 的数据库角色不能直接写 0005/0006，此处不偷偷扩权。

隔离验收：固定合成 XLSX 字节 SHA-256 `098c31d5f64b19347baea3b33b5daa059cc7cd7aadfde8466170943a87680f54`，Python 独立解析与 SheetJS 产生同一 candidateDigest `673f6e3ff8f91cd3a9d79b4dd034569f638f00571faf8f1d7835fb96c07ae58a`、evidenceDigest `0b9f6f0021cad6b680c743995b0db96fbd1e769971cc3b729be8a731378f681d`；两组同名店各 100/200 元，旧行 300 元。55798 独立 PostgreSQL 角色套件 9/9 通过，真实 finance_writer 直接 INSERT 拒绝、0006 触发器及旧行全字段摘要不变；源集群已停。日志归档 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\finance0006-raw-byte-attestation-role-20260926\tests.log`，SHA-256 `55b43d5b4703821c0eb6b2779322db5a3136f1bd24a054d290f094096ef2a28b`。纯测试与正式门禁静态测试另行通过。真实客户原始财报、Microsoft Excel 原生打开/重算、稳定京东店铺 ID、多月与 `.xls`、正式受限写入/恢复尚未验证。

55796 finance-only focused 隔离升级另已 exit0：从 0005 空库前进 0006，旧 v1/0005 行摘要、旧 guard OID/正文/ACL/owner、三旧表目录保持一致；升级前后各自独立恢复、正式备份按 0006 固定错误提前拒绝、正式恢复在含 0006 表的 TOC 上提前拒绝、空逆迁移重装通过。两份测试用明文 dump 前均核 0005/0006 侧车表全空。合成证据/两个空侧车 dump 的 E 盘逐文件 SHA 清单在 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\finance0006-raw-byte-upgrade-20260926\manifest.json`，该清单 SHA-256 `64443a84e048974be0e63a2ba165667d1cd5b63ddc22bfe70d9b9701eed4186a`。这不是正式备份恢复能力。

0006 函数与表/列 ACL 进一步钉到 0005 安装 owner，并拒绝任意第三角色和 PUBLIC 的新增权限后，在独立 55800 合成 PostgreSQL **10/10** 重跑通过；新增 PUBLIC 表 SELECT、第三角色 INSERT 与 guard owner 漂移均由目录核验拒绝并回滚。最终日志 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\finance0006-raw-byte-attestation-role-final-20260926\tests.log` SHA-256 `63420ce24f87e006fa41f698ddde7ec11dbd299f812f328753852f127e8a8a50`，源已停机/端口无监听，取代前述 55798 9 项角色结果作为本代码版本的终态。

目录又钉三表全部物理列类型/空值/identity、四项权威布尔 CHECK、3 组业务 UNIQUE、PK、OneToOne 唯一与 7 个 FK 目标、非级联 NO ACTION、可延期及 validated。最终 55807 隔离真实角色 **12/12** 通过，包含同名弱化 CHECK、物理格唯一、FK 错目标和 varchar 类型漂移的事务内拒绝；日志 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\finance0006-raw-byte-attestation-schema-final-20260926\tests.log` SHA-256 `23eff3d2cac20f3eb9cf98ea409a2992ff42d4e84fe2936ea94a5ac2fe99d810`，取代前述 55800 10 项结果。55808 的最新 finance.0005→0006 focused 升级、前后独立恢复、空逆迁移重装及正式备份/恢复提前拒绝 exit0；E 盘合成证据/空侧车归档 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\finance0006-raw-byte-upgrade-schema-final-20260926\manifest.json` SHA-256 `092649186afe577e80636c17436973247930a968745f4e02c726f96ad22fd505`。所有源集群已停。这些仍不是正式发布或真实原始财报验收。

隔离测试 runner 为 finance.0006 增加**仅在精确 test_teruisi_ai_rehearsal/回环端口**的三张新表 TRUNCATE 守卫临时关闭/恢复：先核六触发器均启用，在同一事务内执行原 flush，再核全部恢复，失败回滚。55809 与原 finance revision `TransactionTestCase` 同跑 11/11 通过，跨两次清理回查六表触发器仍为 O/tgtype34；日志 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\finance0006-raw-byte-runner-combined-20260926\tests.log` SHA-256 `325506f1c0941dc3d427ef15f4ad76f3e73d88b83afa156b4cbfec9ab621380a`。正式触发器和连接路径从未为测试放宽；含 0076 AI TransactionTestCase 的整合树组合回归仍待执行。
