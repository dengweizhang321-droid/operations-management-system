# v3 报告准入意图（0034，内部暂停）

`business_v3_report_intent.create` 每次先在锁外重新验证已封存的 v3 混合来源和只读准入候选，再在 AI 写入事务内比对父证据的精确版本、完整封存字节与当前无范围管理员。相同用户/请求 ID/封存/候选只复用同一意图；不同内容冲突。0034 新表仅追加并不可变，冻结候选快照、完整工作流输入、封存摘要、来源修订与覆盖、以及六节点的暂停计划：五个 Agent 角色后必须是人工审核。所有节点 `dispatchRegistered=false`，状态 `paused`，原因为 `v3_agents_not_registered`。

该表不是 `AiReportRun` 或 `AiWorkflowRuns`，现有调度器不会扫描它；创建和重读均不调用模型，不生成 job、工具结果、报告或 HTML/XLSX 文件。单看数据库中的 `paused` 或 v3 父任务的 `sealed` 不能证明来源可信；内部 `inspect` 必须重新调用 `business_v3_seal.verify` 并重建候选、工作流输入和计划。旧 v1/v2 报告根、工作流与 renderer 1–7 字节保持原样。

闭合版本表清单由 66 增至 67；历史 65/66 表的升级演练保留冻结清单。独立 0033→0034 演练核验前后备份恢复、旧表/ACL/文件字节不变、新表最小权限、空表可回退及有意图时拒绝回退。没有生产迁移、公开入口或定时激活。下一步须另建 v3 Agent 本人来源阅读回执、计算/引用核验、人工审核及版本化 HTML/XLSX renderer，经过相应数据库和真实来源验收后才能考虑激活。
