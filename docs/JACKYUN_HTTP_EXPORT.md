# 吉客云网页校验与 HTTP 导出

## 适用范围

当前本机已采用工作流为 `automation/n8n/jackyun-five-dataset-http.workflow.json`。它与原工作流使用同一 n8n ID，部署时更新已有工作流，不能另外激活一条竞争链。文件默认未激活、只有手动触发；生成命令是 `node tools/generate-jackyun-export-first-workflow.mjs --direct-http`。不带参数的原生成命令和原工作流文件仍用于历史网页会话协议；后续发布当前版本应使用 HTTP 定义。

本版本借鉴独立签名请求、异步导出任务和统一续期，不依赖他人的源码。采用的是已登录网站自身的接口协议，并非吉客云官方开放平台的 AppKey 接入。该版本仍需专用浏览器初始化、读取动态仓库和字段、验证 SKU/日期/货主/权限；不能称为零浏览器取数。网页客户端发布后协议可能变化，签名比对或参数结构不符会停止。

## 执行过程

1. n8n 领取共享 helper，调用 `plan-direct-http` 创建绑定 execution、实际采集日及销售截止日的计划。
2. 五表共用专用浏览器与现有 DPAPI 登录。仍执行现有查询、字段回读、总数和组合装图片数量确认。无头浏览器固定为 1920×1080，避免复用的小窗口使库龄筛选按钮横向滚出视口。
3. 保留网页的实际校验请求，在最终 `startExcelExport` POST 发出前拦截。核验报表服务、模块、全页选择、双工作表、SKU、销售发货时间及月初至昨天边界。签名字段只存在内存，交接证据只保留业务参数 SHA-256。
4. HTTP 阶段把专用浏览器 context 设为离线，本机唯一会话所有者提交任务、按有界任务窗口轮询，沿用任务 ID、时间、标签、行数和 OSS 域名校验。平台确认提交后才向被拦截的浏览器请求返回结果；浏览器请求不会再次到达导出服务。
5. 全部五表下载后，复用原有文件验证、导入演练、Django importer、内容指纹、精确批次与落库回查。顺序和业务口径不变。

导出顺序：分仓库存、组合装及子件、销售单明细账、库龄、货品 SKU。导入顺序：货品、分仓库存、库龄、销售、组合装。销售按发货时间，截止昨天；每月 1 日沿用上月整月范围。库存及库龄是实际采集日快照。

## 会话与异常处理

- 签名上下文从当前账号的运行时获取，并与网页签名做一次包含中文和特殊字符的等价校验。不能只依据界面 `plate` 名称选择签名；网页还会依据自身 `isInJkyunCef()/signPlate` 分支选择协议。
- HTTP 请求复用浏览器的 Cookie、User-Agent 与 ati；这些值和 access token、refresh token、签名材料、签名 URL 只保存在内存，不写入 n8n、日志或工作流 JSON。DPAPI 账号凭据继续使用现有机制；没有新增明文会话文件。
- 只有可重放的任务查询/校验遇到平台明确的过期错误才按需续期；并发过期合并为一次续期。更新浏览器 Cookie 与 localStorage 前先比较原令牌对，同步写入后回读。刷新或发布结果不确定时封闭当前所有者，要求重新登录，不重试刷新旧令牌。
- HTTP 所有者只在专用浏览器离线且持有既有运行锁的阶段使用。浏览器恢复在线后结束该所有者，下一 HTTP 阶段重新读取当前会话，不共享旧内存令牌。
- 仅任务 GET 查询的传输异常最多重试一次；非 JSON、风控或业务错误不按传输异常重试。导出 POST 不自动重放，提交超时沿用网页的 120 秒预算，并保留网页选择的同步或异步模式。网络异常、权限/二次验证/风控响应或任务不唯一均保留原运行。
- 最终 POST 使用 Playwright Route 拦截。取消、校验失败或浏览器确认失败时先中止被暂停的确切请求，再卸载原页面，最后解除拦截，避免解除拦截释放原 POST 或迟到回调补发导出。
- 进程中断后，只允许对尚无导出意图、尚未下载的模块重新发起只读筛选；保留查询尝试历史，最多五次。已有导出意图的模块继续核对原任务，不能通过重置查询状态再次提交。
- 新传输版本 `web_prepared_http_v1` 与 `web_session_batch_v1` 的计划和 controller 状态隔离。原运行未闭合时不能通过改计划类型新建重复任务。
- 不采用截图里的滚动 45 天范围、5% 错行容忍或强制每 30 分钟刷新。零成本分类及补录政策不属于本次改动。

## 验收与采用

在隔离工作树验证：签名序列化、并发续期、令牌发布失败、风控响应、拦截及迟到回调、日期/双表/SKU 边界、五文件屏障、旧运行隔离、导入回执，以及生产后端边界检查。构建不等于生产发布。

共享 helper 空闲后，以独立运行 ID 验收真实五表导出、原始文件范围及导入演练，再按项目受控发布流程采用 helper 和 n8n 工作流；未完成真实验收不能宣称全链路已经上线，也不能用单次任务 GET 延迟代替整批耗时。

新流程不新增数据库迁移、导入写入源、定时器或成本回填。出现新传输问题时先保留原运行证据；不能在未闭合的相同运行里切换传输并再次导出。

### 2026-09-07 隔离验收记录

运行 `http-acceptance-1788793857188` 已完成五表真实 HTTP 提交、任务绑定、下载哈希及本地导入 dry-run。报表源计数为货品 8,485、分仓库存 25,731、库龄 5,661、销售明细 5,557、组合装母件 1,942；组合装计数不是拆分后的子件导入行数。销售范围是 2026-09-01 至 2026-09-06。库龄阶段修复视口后续跑，前三表沿用原任务，所以本次不作为连续整批速度基准。

本次只读回查的生产销售 revision 为 `16:12`，覆盖截至 2026-09-06；验收没有写入正式业务库，也没有替换生产 n8n 工作流或发布 helper。证据保存在隔离工作树的 `outputs/direct-http-acceptance/validation/http-acceptance-1788793857188/acceptance-summary.json`。更早的试验运行 `http-acceptance-1788792790080` 因销售提交来源未完整确认，不作为五表 HTTP 验收证据。

回归结果：184 项吉客云相关测试通过，构建通过，383 个生产模块边界检查无违规；同步最新 `main`（`8dd246ca`）后，50 项受影响工作流及天猫共享链路测试通过。TypeScript 全量检查仍有 142 项错误，与未改动基线逐项一致，不能表述为全量类型检查通过。候选代码保留在 `codex/jackyun-direct-fetch-20260908`，尚未合入 `main`，因此保留该 worktree；生产采用前仍须按项目门禁处理检查和发布。

### 发布前复核

用户确认进入发布流程后，全量单元测试为 1,979 通过、20 跳过、0 失败；构建产物及渲染契约 20 项通过。修正了一处新增测试的保留变量命名，以及既有财报渲染测试仍在销售页面查找上传扩展名的过期断言：上传入口已在统一数据导入页，测试现在精确核验该页财报入口、API 及 `.xls/.xlsx` 支持，没有修改财务业务实现。

142 项类型诊断与基线完全一致，按既有网页会话版和本机发布记录的无新增标准核验；它不是本次新增故障，先前将其描述为必须全部消除后才能采用候选版本不够准确。共享 Worker/helper 的发布窗口须与其他正在运行的任务串行协调，收到释放通知后才切换版本。

## 2026-09-08 本机正式采用

用户授权发布，且天猫在途执行终态、浏览器关闭、helper 空闲并明确交接后，本机经 Stop → plan → 精确 SHA apply → 原 n8n ID 更新 → Start 完成采用。源码为 `237427e409bbe68c96d601cf2af9ef4bd2801474`，包含吉客云 PR #22/#25 和同期天猫 PR #23/#24；没有覆盖其他任务的新修复。

- Worker/helper release：`20260907T164126Z-c0bafded45a318c6`（上海日期为 9 月 8 日）。
- manifest SHA：`efab585c7498fd18e1f586fe0255e9aa4108d175d15863a24b396864f7fcaf11`。
- plan SHA：`d28687bcdc359cfdd8dce95706dcd4edcf9cbff5a81a27e9b910a7a4dfa9d419`。
- 原 n8n ID `J8kY2mQ5vR7sT4pN`，新 version ID `bfc65dde-b3c5-41c8-8fd9-592bc678ffb8`；11 个节点、连接、设置、标签和项目归属回读一致，仍为 `active=false`、手动触发。
- Status 为 `exact_release`，VerifyStartup 为 `verified`；readiness 200、无不可用服务；helper 为 `ready/busy=false/activeWorkflow=null`，吉客云 profile 为 ready。
- 缺少 execution ID 的 HTTP 计划请求按预期返回 409，未领取执行权或写计划；Django/PostgreSQL 的 24 个监听端口及 PID 前后一致。

本次没有运行新的业务 execution、没有导入正式业务数据、没有迁移数据库或调整成本规则。此前的五表实测是下载与本地 dry-run，不能表述为新版本已完成一次生产导入。其他任务的活动审计、待生成平台任务和浏览器资料均未更改；发布验证后已交还共享窗口。后续恢复仍遵循不可变发布链及现有 Django 单写边界，不通过删除已发布事实或恢复旧 D1 实现回滚。

精简证据见 [`evidence/jackyun-http-release-20260908.json`](evidence/jackyun-http-release-20260908.json)。完整日志、原/新工作流备份及验收文件归档到 `D:\codex-artifacts\jackyun-http-release-20260907`，原始五表文件仍在 `D:\谷歌浏览器\jackyun\http-acceptance-1788793857188`。
