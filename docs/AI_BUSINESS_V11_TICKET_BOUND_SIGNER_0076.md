# 0076 v11 票据绑定签发候选（默认关闭）

0076 精确依赖 0075，仅新建 SQL-only 受保护签发回执表与版本化函数；旧 0068/0070/0073 函数 OID、正文、ACL 不改。它复用 0070 的同一 `teruisi_ai_budget_v11_sign_login` 角色和一次性 ticket/claim，要求 0067 旧证明与 0073 新证明同 run/attempt、同 SHA、同原文。该角色仍初始 NOLOGIN、无密码、无成员；迁移不写 key，不开放 renderer 11 `ready`、下载、Web 入口或正式签发。

新签发接口只能以同一 ticket/claim/run/attempt/owner/report 调用。数据库在 `SECURITY DEFINER` 内重核当前 paused 暂存根，逐 job 对五个固定 Agent 及四张派发/结果账本核 owner、模型/策略、状态、序号、provider-call 归属、规范 JSON、原文字节摘要和容量；每 job 限 1–20 次 provider、1–40 次 tool，单结果 256 KiB、参数 8 KiB。数据库一次计算五 job 页 SHA 链根，逐页只读取该页；签发器读完全部页后独立重算同一根，完整 HTML/gzip/NDJSON 和 XLSX OPC/公式仍由现有 owning preflight 实际重建。数据库再核签发前后的根、同一 0067/0073 证明和活动 key，以新目的域 AES 无关的 HMAC-SHA256 将 ticket、claim、五 job 页链根、每卷文件证明根、报告/owner/版本绑定为一条不可变候选回执。响应未知只用同 claim/receipt SHA 的窄 OUTCOME 查 committed/conflict/absent，三态均不自动重发。

隔离正例暂借现有 `ai_writer` 身份运行全字节 owning preflight，签发 SQL 则使用独立真实非超级 `sign_login` 连接；仅通过合成随机进程内 KeyProvider 注入密钥。候选结果固定 `candidateOnly=true`、`readerProcessIsolated=false`、`readyAuthorized=false`、`releaseAllowed=false`。**同一测试进程暂持 writer 与签发密钥，不能作为生产密钥隔离设计。**正式接线仍需独立受保护 reader 进程、单报告工作许可（特别是 0073 全局 run oracle）、key 托管与轮换、锁内原子发布/结果查询及窄下载栅栏。

目录准入检查新表精确列、唯一/FK/检查约束、owner、表列 ACL、触发器事件及无 WHEN、函数源码/owner/全部非 owner EXECUTE；四账本和私钥表仍不给 signer、reader 整表 SELECT。`PrepareApp`、普通迁移、正式每日备份与恢复在 0076 出现时先拒。

2026-09-26 唯一隔离 55788 真实角色目标 2/2 通过（317.633 秒）：非超级 0073 登录证明与旧 0067 证明同 SHA、0070 单票据/单领取、五 Agent 四账本逐页 SHA 链与现有 owning preflight 的 HTML/XLSX 全字节重建、新目的域合成 key MAC 不可变回执/OUTCOME；跨 run/job、直接表/密钥、ready/下载拒绝，以及 `WHEN false`、同型 CHECK 放宽、`OR true`、第三角色函数授权的目录漂移负例通过。源 `pg_ctl` 停机、55788 无监听，测试库销毁。只证明合成候选；没有生产身份、密钥托管或正式发布。E 归档 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\ai-pg-7d2d5e885870-v11-ticket-signer-role`，`tests.log` SHA256 `30A8161A853B2FEFEBDDBC2B63AA907DEE375F1AFBB64AAE7DAD182B4DDC4200`，`evidence.json` SHA256 `325703EE5AF849B48A7B9ED7E79CB9BE6B163A5C5A48ABBE129AE97A774FC170`。

0075→0076 唯一隔离 55789 升级演练通过：finance.0005 当前共存，旧 14 函数在源库的 OID/正文/ACL/owner、旧 11 张受保护表 owner/有效 ACL、七份 renderer 源码字节未变；升级前后两个独立 custom dump 均恢复并复核，新增总计 12 张受保护表及 9 个函数目录精确，空逆迁移重装通过。事务内注入的合成非空私钥先于明文归档被拒并回滚；正式每日备份在写目标前因 0076 被拒。旧持久文件块为 **0 行**，所以“行字节保持”不证明真实成品字节或非空文件块恢复。源 `pg_ctl` 停机、55789 无监听。E 归档 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\ai-pg-51553e7ecd4e-v11-ticket-sign-upgrade`，evidence SHA256 `23F08BBD41D016ECD7FE773D799C941EA28DB3C15C586A8110E00A7FFC2C9DE7`，before/after dump SHA256 `16A27C7A5332727752561B7FE10C324E68DD367E025FAB311EEB270A0924D0C7`/`1259FE261CB35A5F1733126A3FBAE448ADFB992BB76B8D86E9040B76E701DDE6`。

随后终审补强 `REQUIREMENTS`：同锁核当前 report/flow owner 与 scope、flow 已完成、当前报告快照与 workflow 输入 SHA、当前管理员版本和预算计划根；新回执纳入 workflowVersion。55799 首轮角色 2/3，通过的旧两项仍有效，但新增负例夹具在旧 `ai_immutable_evidence` 上游触发器止步，不能算 0076 RECORD 拒。隔离夹具改为仅在 rollback-only 事务中临时停用具名旧不可变触发器，调用 RECORD 后回滚并核触发器 O/原行。单项 55802 负例 1/1 通过：report owner、snapshot、provider 摘要、tool 跨 job 归属依次被 0076 以精确错误拒，回执仍 0；回滚后同 claim 正例写 1 条。源停机、55802 无监听，E 归档 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\ai-pg-d4722b44eb37-v11-current-root-negative`，`tests.log` SHA256 `F908BB2AE67AD6101704D8E712F258E105EA7ADCDB4292FF2F0E9B9A6126BD48`。

当前根补丁后的唯一隔离 55805 **整类角色 3/3** 通过（553.145 秒），同时覆盖原五 Agent 合成签发、目录漂移和当前根/四账本防御深度负例；源停机、55805 无监听、测试库销毁。E 归档 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\ai-pg-51120f36ee1b-v11-ticket-signer-current-root`，`tests.log` SHA256 `249C2D1087DE7B3EC6C06CBA80FDFD1FFFE73D3B147EBF5BE632769549F04DE1`，`evidence.json` SHA256 `CB446B7949FB0F481EB9EBE60F4C2C37DF7331B82C25788B68B7E21D4A914035`。**修订后 55806 focused 升级已通过**；E 归档 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\ai-pg-a3e527706862-v11-ticket-sign-current-root-upgrade` 的 manifest SHA256 `16C4FBF744B37B94706BA913C0ECB4AA6A6C92A1F1BF51B6BF3A1F9BE7F48C65`，其中当前 SQL SHA256 `D77B2D6A8E2B752A0C018DDD20DED900BC674077761401A7BF265C10AE2B9921`、迁移 SHA256 `08632281D91E88803D6DAAB0B7B90A3EEA7261019AC0493423287663E97BE737` 区分前轮同布尔证据；旧持久文件仍为 0 行，不证明真实成品字节。

纯合同已用最大 5×(20 provider＋40 tool)＝300 页的轻量数据验证页序与页 SHA 链算法；**真实 PostgreSQL 满载 300 页、单页 256 KiB 结果和总 600 秒容量未测**。隔离签发连接与 owning writer 连接均要求非零且不超过 600 秒的 SQL statement_timeout，Python 另有总 600 秒单调钟门禁；这仍不能代替真实极限性能验收。正式独立 reader/key 托管、0073 单报告许可、原子发布与窄下载未实现，候选继续关闭。
