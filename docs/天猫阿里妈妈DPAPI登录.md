# 阿里妈妈 DPAPI 登录保护

2026-09-07：已完成本机受控发布，release `20260907T013343Z-d1f5518ff9392cc3`，源码 `a3c89df6`；服务采用、启动绑定及 Django/PostgreSQL readiness 回读通过。尚未验证真实登录态失效时的 DPAPI 自动提交，不得据此承诺免人工登录。

推广 UI 与直连共用登录保护：优先复用会话，未登录时仅在 HTTPS 淘宝登录来源的唯一密码表单中使用该店既有 Windows DPAPI 凭据。每个浏览器上下文只允许一次自动尝试；提交结果不确定也消耗尝试额度。密码不进入 n8n、参数、日志或文档。

验证码、凭据拒绝、频率限制、未知来源、表单歧义和身份未确认均失败关闭。登录成功必须在阿里妈妈主页面核验完整受控店名，千牛登录成功不能代替此项。平台新增登录来源必须先核验再扩展，不允许宽泛放行域名。

直连推广先完成身份检查，再从只读下载列表捕获请求标识；不会因登录超时重新生成报表。既有活动清单、逐日缺失规划、导入回查与 n8n 编排边界不变。

发布仍需受控构建、服务采用与回读；之后只能从原 n8n 工作流完整试跑。不得为测试强制退出用户会话、清除 Cookie，或直接调用推广业务接口。合并源码不等于线上已启用自动登录。

验证：相关 23 项测试通过；合并最新 main 后全量低并发复验 1,928 通过、23 跳过、0 失败/取消；lint 0 错误、9 个既有警告；后端边界检查、隔离 helper 构建与 git diff --check 通过。默认并发首次运行有三个其他模块浏览器测试超时，单独及低并发复验通过。单元测试未使用真实天猫凭据。

线上完整 n8n 试跑：亿玖 execution `851`，上海时间 09:41:55 至 09:43:10，约 75 秒。A 身份检查成功；B/C 对已覆盖的 2026-08-29 未新增下载导入。P 在登录及店铺身份检查通过后，等待下载列表请求标识约 60 秒失败，错误为“未捕获到阿里妈妈下载列表的 csrfId/loginPointId”。M 未执行，本轮无新增推广导入，不能宣称全链路恢复。原 P/M 活动清单保留；终态 helper ready/idle，受控浏览器监听端口已关闭。后续应核验下载列表实际请求与标识采集契约，不能把此错误归因于密码或自动重新生成报表。
