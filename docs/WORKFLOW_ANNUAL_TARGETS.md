# 上新跟进与店铺年度目标（2026-09-14 开发预览）

状态：已完成独立 worktree 开发和隔离验证，等待用户确认预览效果；未合并 main，未部署生产。
分支：`codex/workflow-annual-targets-20260914`。生产目录和业务数据未修改。

## 已确认需求与行为

1. 运营事务 → 上新跟进 → 编辑产品线：在“吉客云名称（学习关键词）”右侧增加“学习新代码”。使用当前未保存名称/关键词进行只读匹配；候选代码合并到当前草稿，保留手动代码并去重。保存产品线才正式生效，取消不保存。跨产品线命中或已被其他产品线占用的代码列为人工判断项。学习过程禁用相关输入和保存，关闭取消在途读取；超时可重新读取。原页面全局学习按钮保留，去掉打开页面和保存后隐式执行全局学习；ERP 导入时既有学习链路不变。
2. 新品上架顶部“进行中 / 受阻 / 阶段逾期 / 已完成 / 全部项目”支持点击筛选。状态与逾期条件在后端分页前应用，与搜索、负责人、供应商、阶段等条件组合。快捷卡片计数忽略自身状态/逾期条件，清单及阶段统计按当前筛选计算；矩阵与看板使用同一筛选结果。
3. 店铺目标进度移到销售分析 → 目标设置。仅设置每个年份、平台、店铺的整店全年销售目标和全年利润目标，无月度目标入口。已完成额自动累计该年成功导入的月度财报，最新月份为截止月份。销售采用财报净销售额，利润采用既有财报利润指标及其明确回退公式。全年销售/利润完成率分别为对应年累计实际额除以全年目标。

## 年度计算边界

- 平台与店铺组成完整身份，不按同名店铺跨平台合并。
- 只消费 `finance_months.status=completed` 对应的 `summary/shop` 月度事实，既有全局/分组/科目汇总和报表年累计列不重复加入。
- 只对照精确年份、非空平台、空品类的整店年度目标；旧月度、品类、项目目标不参与新的年度进度，也不自动删除或转换。
- 有财报而未设目标的店铺仍显示实际金额；目标为零时显示“未设目标”，没有实际财报时金额/完成率为无数据，不能伪装成零完成。
- 全局缺失月份及各店铺缺失核心金额指标的月份明确标注；已有月份累计金额仍可查看，不能据此宣称全年度覆盖完整。负利润和超过 100% 的进度保留真实文本，进度条宽度限制在 0–100%。
- 目标仍由管理员保存，沿用版本冲突、唯一约束和删除原因审计；隐藏的旧字段编辑时保留。无需新增表或数据库迁移。
- 新读取路径为 `GET /api/finance/targets?view=annual&year=YYYY&page=1&pageSize=100`，旧 `full/items/options` 保持兼容；`items&year=YYYY` 只列该年整店目标。读权限、签名、范围检查和 revision 一致性读取继续生效。
- 代码预览为 `GET /api/workflow/new-product-lines/learn`，使用 reader，最多扫描 100,000 个主数据货品、返回匹配不超过 500 个代码，超限报错而不静默截断；保存继续使用 writer 并校验代码归属和版本。

## 预览

- [年度目标设置](http://127.0.0.1:3100/?module=sales&view=targets)
- [新品上架](http://127.0.0.1:3100/?module=workflow&view=launch)
- [上新跟进](http://127.0.0.1:3100/?module=workflow&view=launch-followup)

预览数据由 `tools/preview/workflow_annual_fixture.py` 在本 worktree 的合成 SQLite 中生成，包含四种项目状态、一条产品线、两家演示店铺与年度目标。预览仅开放查询，可验证学习草稿和取消；持久保存由隔离 PostgreSQL 测试验证，不在只读预览中开放写入。启动/停止流程见 `docs/ISOLATED_PREVIEW.md`。

## 验证记录

- 隔离 PostgreSQL 17.11，独立 55461 端口、随机凭据与合成数据：88 项 workflow/finance 测试通过；完整迁移和 `makemigrations --check --dry-run` 通过，无迁移漂移，临时集群已停止。
- 新增用例覆盖：未保存关键词学习且零写入、代码冲突/超限、分页前状态/逾期筛选、月度目标不影响年累、同名店跨平台、缺失/处理中的财报、亏损、零目标、重导替换不重复累计、年度参数和权限。
- Node 全量：2,164 通过、0 失败、21 跳过。其中一项构建时序导致产物新鲜度检查跳过，最终构建后单独复验相关 5 项全部通过；其余 20 项为原有退役路径跳过。
- 构建、20 项渲染测试、本次文件 ESLint、应用/领域代码 TypeScript 检查及 Django 边界检查通过。全仓 `tsc` 的既有测试代码类型错误不在本次修改范围，应用检查使用隔离配置排除 tests 后通过。
- 浏览器实际点击验证：进行中只剩相应商品、逾期只剩逾期商品、全部项目计数保持；学习新增代码进入草稿，取消后重开仍为原代码；目标页显示演示年累计金额、完成率、缺失月份并可从店铺行打开目标草稿。

本次同时修改 Worker 与 Django 逻辑；用户验收后才合并，生产采用须同时更新对应代码并遵守现有受控发布流程，不能仅替换前端。预览运行中且尚未合并，因此保留分支和 worktree。
