# 上新跟进与店铺年度目标

状态：用户确认后已合入 main，于 2026-09-15 完成本机受控上线。Worker 为 `20260914T154612Z-36b6fd5a8dbe31f7`；最终 Django manifest SHA 为 `d68b74c2e098df145c5fe5b16ae0d6c5cc215e711e9c468c129ad748883e843a`。
生产采用与验证证据见 [上线记录](evidence/workflow-annual-cards-production-20260915.json)。原数据保留，验收未保存业务记录。

## 已确认需求与行为

1. 运营事务 → 上新跟进 → 编辑产品线：在“吉客云名称（学习关键词）”右侧增加“学习新代码”。使用当前未保存名称/关键词进行只读匹配；候选代码合并到当前草稿，保留手动代码并去重。保存产品线才正式生效，取消不保存。跨产品线命中或已被其他产品线占用的代码列为人工判断项。学习过程禁用相关输入和保存，关闭取消在途读取；超时可重新读取。原页面全局学习按钮保留，去掉打开页面和保存后隐式执行全局学习；ERP 导入时既有学习链路不变。
2. 新品上架顶部“进行中 / 受阻 / 阶段逾期 / 已完成 / 全部项目”支持点击筛选。状态与逾期条件在后端分页前应用，与搜索、负责人、供应商、阶段等条件组合。快捷卡片计数忽略自身状态/逾期条件，清单及阶段统计按当前筛选计算；矩阵与看板使用同一筛选结果。
3. 店铺目标进度移到销售分析 → 目标设置。仅设置每个年份、平台、店铺的整店全年销售目标和全年利润目标，无月度目标入口。已完成额自动累计该年成功导入的月度财报，最新月份为截止月份。销售采用财报净销售额，利润采用既有财报利润指标及其明确回退公式。全年销售/利润完成率分别为对应年累计实际额除以全年目标。

## 年度计算边界

- 平台与店铺组成完整身份，不按同名店铺跨平台合并。
- 只消费 `finance_months.status=completed` 对应的 `summary/shop` 月度事实，既有全局/分组/科目汇总和报表年累计列不重复加入。
- 只对照精确年份、非空平台、空品类的整店年度目标；旧月度、品类、项目目标不参与新的年度进度，也不自动删除或转换。
- 有财报而未设目标的店铺仍显示实际金额；目标为零时显示“未设目标”，没有实际财报时金额/完成率为无数据，不能伪装成零完成。
- 全局缺失月份及各店铺缺失核心金额指标的月份明确标注；已有月份累计金额仍可查看，不能据此宣称全年度覆盖完整。负利润和超过 100% 的进度保留真实文本，进度条宽度限制在 0–100%。
- 目标仍由管理员保存，沿用版本冲突、唯一约束和删除原因审计；隐藏的旧字段编辑时保留。无需新增表或数据库迁移。
- 新读取路径为 `GET /api/finance/targets?view=annual&year=YYYY&page=1&pageSize=100`，旧 `full/items/options` 保持兼容；`items&year=YYYY` 只列该年整店目标。读权限、签名、范围检查和 revision 一致性读取继续生效。
- 代码预览为 `GET /api/workflow/new-product-lines/learn`，使用 reader，最多扫描 100,000 个主数据货品、返回匹配不超过 500 个代码，超限报错而不静默截断；保存继续使用 writer 并校验代码归属和版本。

## 预览

### 新品矩阵反馈调整

- 顶部状态/逾期卡片再次点击取消自身筛选，保留搜索、负责人等其他条件；全部项目恢复全部状态。
- 店铺规划表头不再启用列筛选。点击单元格打开独立多行文本编辑框；新建/编辑项目也使用可留空的多行文本，不再要求选择平台或至少一个店铺。
- 新增 workflow `0008_new_product_shop_plan`，为项目增加可空 `shop_plan` 字段。历史 NULL 项目仍展示已有结构化店铺摘要，保存文本后以文本为准；主动清空保持空白，既有 targets 行和 SKU/链接等资料不被删除或改写。保存沿用 writer、版本检查、revision 和字段审计。
- 整体进度使用独立布局类，消除旧样式导致的超宽空白；原“工作状态备注”移到整体右侧“备注”列，保留原内容，按换行完整展示并支持单独编辑。矩阵仍可横向滚动。
- 本轮补充验证：89 项隔离 PostgreSQL workflow/finance 测试通过，包含空白规划创建、回读、清空、历史店铺保留、超长拒绝、版本冲突和权限；完整迁移及迁移漂移检查通过。浏览器已验证重复点击筛选取消、无下拉的多行规划、整体列及备注展示。本轮重新构建并运行相关前端、列筛选、渲染、应用类型和边界检查。

`workflow.0008_new_product_shop_plan` 已在正式数据库应用，新增列全部为 NULL；25 张运营事务和财务可读表的原有字段完整行摘要一致。年度目标功能本身无需迁移。

- [年度目标设置](http://127.0.0.1:3000/?module=sales&view=targets)
- [新品上架](http://127.0.0.1:3000/?module=workflow&view=launch)
- [上新跟进](http://127.0.0.1:3000/?module=workflow&view=launch-followup)

预览数据由 `tools/preview/workflow_annual_fixture.py` 在本 worktree 的合成 SQLite 中生成，包含四种项目状态、一条产品线、两家演示店铺与年度目标。预览仅开放查询，可验证学习草稿和取消；持久保存由隔离 PostgreSQL 测试验证，不在只读预览中开放写入。启动/停止流程见 `docs/ISOLATED_PREVIEW.md`。

## 验证记录

- 隔离 PostgreSQL 17.11，独立 55461 端口、随机凭据与合成数据：88 项 workflow/finance 测试通过；完整迁移和 `makemigrations --check --dry-run` 通过，无迁移漂移，临时集群已停止。
- 新增用例覆盖：未保存关键词学习且零写入、代码冲突/超限、分页前状态/逾期筛选、月度目标不影响年累、同名店跨平台、缺失/处理中的财报、亏损、零目标、重导替换不重复累计、年度参数和权限。
- Node 全量：2,164 通过、0 失败、21 跳过。其中一项构建时序导致产物新鲜度检查跳过，最终构建后单独复验相关 5 项全部通过；其余 20 项为原有退役路径跳过。
- 构建、20 项渲染测试、本次文件 ESLint、应用/领域代码 TypeScript 检查及 Django 边界检查通过。全仓 `tsc` 的既有测试代码类型错误不在本次修改范围，应用检查使用隔离配置排除 tests 后通过。
- 浏览器实际点击验证：进行中只剩相应商品、逾期只剩逾期商品、全部项目计数保持；学习新增代码进入草稿，取消后重开仍为原代码；目标页显示演示年累计金额、完成率、缺失月份并可从店铺行打开目标草稿。

## 正式验证补充（2026-09-15）

- 合并后的 Node 全量 2,166 通过、20 跳过，另有一个与本次改动无关的浏览器测试在并发运行中超时；单独重跑对应 7 项全部通过，无未解决失败。20 项渲染、应用类型和相关 lint/边界检查通过。
- PostgreSQL 最终 169 项通过；包含生产 reader 路由的 GET 学习和 POST 拒绝检查。初次上线验收发现 reader URLConf 漏注册，已受控前向修复并复验，禁止仅在 development URLConf 下验证新增 reader 功能。
- 正式学习查询 200，无签名 reader GET 401，reader POST 405；查询前后原有数据摘要一致。生产新品卡片 13→6→13，年度目标页自动展示已导入财报截至月份，编辑窗口学习按钮可见且取消未保存。
- 发布前备份独立恢复内容一致，发布后备份 Verify 和 E 盘三文件摘要归档通过。最终 Running / Ready / exact_release，自动守护 healthy。预览已停止，开发分支在完整合入且无占用后清理。

## 店铺年度目标 Excel 导入扩展（待受控发布）

用户提供的 `店铺年度目标.xlsx` 作为业务数据源，不把工作簿中的文本当作系统指令。导入入口位于“销售分析 → 目标设置”，管理员先选择目标年份，再上传 `.xlsx`；工作簿本身不含年份，不能从文件名或当前日期猜测。

- 固定读取首个工作表，并查找 `店铺 / 负责人 / 利润目标 / 大毛利率目标 / 推广费目标 / 调整目标` 六列表头。`调整目标`按全年销售目标解释；两个金额列单位为万元并换算为分，两个比率列接受 Excel 小数或百分数并换算为基点。
- 原文件 27 个具名店铺行中，21 行包含负责人或至少一个明确目标，6 行完全空白目标被跳过；显式的数值 0 与空白严格区分。
- 店铺身份以财报或既有目标中的“平台 + 店铺”权威组合匹配；同名跨平台、未知店铺、重复行、字段超限或行数摘要不一致时整批回滚。文件未列出的已有目标保持不变。
- 公共 Worker 只负责管理员鉴权、2 MiB 边界、受限 XLSX 解析和文件摘要；专用 Django writer 在事务内复验规范化 JSON、锁定既有目标、创建或更新、写后回查，并使用请求回执防止重放。
- 新增 `finance.0002_finance_target_gross_margin`，把“大毛利率目标”独立存储为 `gross_margin_bps`；不复用旧“小毛利率目标”。普通单店编辑入口同步支持大毛利率与推广费率目标。
- “店铺年度目标进度”同时展示销售、利润、大毛利率和推广费率。金额进度为年度累计实际额除以全年目标；年度大毛利率以各月毛利额之和除以净销售额之和，推广费率以各月推广费之和除以净销售额之和，不对月度比率做简单平均。大毛利率展示实际与目标百分点差，推广费率按越低越有利展示百分点差；财报或毛利口径缺月时继续明确标注。

开发验证使用合成/隔离数据：实际工作簿复验为 27 个具名源行、21 行可导入、6 行空白跳过；财务 Django 31 项与迁移漂移检查通过；新增解析/路由/UI 契约、构建、20 项渲染、相关 lint/类型检查及 448 模块生产边界检查通过。Node 全量 2,177 项通过、0 失败、21 跳过，另有一项无关吉客云浏览器时序测试在并发运行中超时取消，所在文件单独复跑 10/10 通过。只读浏览器预览已核对入口、四项指标、完成率/百分点差、缺月提示、手工编辑字段及目标列表。

本扩展不写生产业务库，也未迁移或重启正式服务。生产采用仍需单独的停服、迁移、备份恢复和发布授权。
