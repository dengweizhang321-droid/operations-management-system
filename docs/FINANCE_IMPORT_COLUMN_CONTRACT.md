# 财报导入内部列号字段修复

2026-09-14，用户要求导入志高事业部 2026 年 8 月财报。正式公开导入口返回 HTTP 400：`财务明细行字段与 finance-normalized-v1 不一致`。写后只读回查确认没有新增 8 月批次，已有财务数据截至 2026 年 7 月。

原因是 `parser.ts` 把包含 `columnIndex` 的 Excel 维度对象直接展开到财务明细。TypeScript 的结构类型没有删除额外属性，JSON 序列化将内部列号发送到 Django，后端精确字段集合验证正确拒绝了请求。

修复在汇总和金蝶明细两个构造点显式投影 `scopeKey/scopeType/scopeName/groupName`。保留后端严格契约、鉴权、金额转换、费用合计、幂等和审计，不扩大写入权限。

验证：新增 XLSX 与 BIFF8 两种格式回归，覆盖规范化边界的精确字段集合、月份、事业部金额和店铺费用。财务相关测试 59 通过、0 失败、15 跳过；隔离生产构建通过。对用户工作簿全部已识别月份逐条比较，除删除内部列号外，金额、比率、身份、来源计数与警告完全一致。真实财报内容不提交源码仓库。

本次用户只授权 8 月导入。原文件包含 1—8 月，继续操作须仅选择 `26.8` 工作表，在内存中生成上传内容，并复验规范化行与原文件 8 月完全一致，然后经公开 `/api/imports/finance` 提交。不能把全年文件直接提交而更新其他月份，也不能绕过公开入口直接写数据库。

全量 Node 回归最终 2160 通过、0 失败、20 跳过；真实隔离 PostgreSQL 财务测试 20 项通过，原报表 8 月规范化结果通过 Django 精确契约校验。

用户明确授权发布后，修复已合入 main，并采用 Worker/helper `20260914T094506Z-81d2b9e6d317f82b`，manifest SHA `100bb15aaa4f442847e6308cadb929b2c92699bcf24b17eb8470030315dd0a15`。暂停唯一运行中的市场计划，两次确认全部在途领取为零及 helper 空闲后停止 Worker；后端继续运行，上线后恢复该计划并观察到自然推进。系统启动回查 Running/Ready/exact_release，启动绑定通过。

2026-09-14 17:53:35（上海）正式导入口返回 HTTP 201，完成批次 `b940e603b3c66c1cc381492917161f2742ddf205df964ec76f858675cd0174b3`，仅导入 2026-08，2294 条、30 店铺、81 科目、0 警告。独立回查完成批次、9 项主要金额与报表一致，原有 19 个月份记录不变。财务覆盖更新至 2026-08。

发布前备份独立恢复摘要一致。导入后复用另一受控维护任务生成的共享库备份 `daily-20260914T095432Z-1ebee0e98715`，其快照晚于本次入库，独立 Verify 通过，三份文件已复制到 `E:\运营管理系统业务数据` 并逐文件核对 SHA。无数据库迁移或 n8n 重启。详见 `docs/evidence/finance-import-column-production-20260914.json`。
