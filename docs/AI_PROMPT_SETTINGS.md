# AI 对话布局与配置设置

2026-09-13：第一阶段在 `codex/ai-chat-settings` 独立 worktree 开发，等待用户预览验收；尚未合并或上线。生产 AI 迁移仍以原采用记录为准。本次没有生产写入、服务重启、真实模型调用或消息发送。

## 已实现范围

- 顶部新增独立“AI 对话”，位于“运营事务”左侧。沿用 `?module=ai` / `view=assistant`，不改变个人会话归属与旧链接。原“AI 助理”入口进入 Agent 工作流，并保留记忆、AI 空间、AI 管理、AI定时任务与新增配置设置。
- 独立对话占满顶部导航以下高度，隐藏原 AI 子导航与页脚占位，压缩输入区；正文最大宽度 1120px，输入区固定在聊天面板底部、消息独立滚动。桌面历史栏可收起，760px 以下以覆盖侧栏打开，板块抽屉保持紧凑布局。
- AI 助理 → 配置设置（`?module=ai&view=configuration`）：全局提示词、通用业务规则、领域规则、草稿预览、版本记录与恢复。领域规则可维护名称、适用场景、关联领域、正文、来源及启停；列表顺序即规则顺序。
- 会话工作能力第二阶段暂缓：未新增持久工作目录、跨轮文件系统、长任务执行器或更宽的沙箱权限。现有 JSON/pandas 分析工具继续使用现行边界。

## 配置与生效规则

全局提示词用于角色、回答风格与工作顺序。通用规则每次提问采用；具体业务规则按销售、库存、网店、市场、客服、财务六类维护。内置文案根据本仓库的业务规则整理，同事系统仅作为交互参考，没有导入其配置或业务数据。

领域选择由当前问题的内置领域关键词、页面范围及本次已调用工具共同决定，并与当前真实工具目录所含领域取交集。自定义“适用场景”用于说明规则用途，当前不作为可编程触发条件；同一领域被选中时采用该领域全部启用规则。不是知识库全文检索，也没有自动推断任意工具组。后续若需更细粒度触发，可在独立阶段加入可预览的明确条件。

聊天服务每次提问固定一个配置快照。保存从下一次提问生效；同一回答的后续工具轮次仍使用原快照。适用于共用 `chat.answer` 的网页、板块对话、钉钉问数和 AI 定时任务；独立 Agent/DAG、图像任务和模型连接测试不接入这套配置。

系统权限、只读边界、工具参数与后端计算公式保持代码权威；配置只能指导回答与解释，不能授权操作或改写公式。模型专属要求仍在 AI 管理维护，不应与全局提示词重复或冲突。

回答的 `execution.guidance` 保留版本、配置摘要及所采用规则的 ID、名称和来源；网页执行详情与对话详情显示版本与名称。历史消息没有该字段时展示“暂无配置版本记录”。快捷帮助、上下文重置没有模型轮次，不生成口径版本证据。

## 存储、权限与恢复

新接口 `/api/ai/prompt-settings`：GET 查询当前/指定版本以及每页最多20条历史元数据，POST 保存或恢复。边缘与 Django 均要求当前启用的、数据范围不受限的管理员；写入经过同源校验、签名请求、AI 写回执、幂等与审计。数据库权限与业务管理员权限分别校验。

迁移 `ai_assistant.0011_prompt_settings` 在 PostgreSQL 新增 `ai_prompt_settings_revisions`，AI 自有表由51张增至52张，不改历史对话、模型、任务和业务表。每次保存校验 `expectedVersion`，在 AI mutation 锁中追加新版本，冲突返回409。恢复创建新版本并记录 `restored_from`，不覆盖或删除历史。无记录时读取虚拟版本0的代码默认配置；全局提示词留空也采用代码默认值，通用规则留空则为空。

版本0不是不可变数据库记录，其具体默认内容随代码版本变化；回答摘要用于区分。要求完全冻结默认内容时，应将其保存成显式配置版本。

单字段/总量限制：全局8000字符、通用4000字符、最多24条规则，每条正文4000字符，整个配置最多64 KiB。数据库保留大小、版本约束以及 authority fencing；reader 只读，writer 只有 SELECT/INSERT，无 UPDATE/DELETE/TRUNCATE。健康检查、权限清单、备份及独立恢复清单均包含新表，并继续兼容迁移前备份。

配置不进入通用数据集。此次覆盖检查同时补齐既有定时任务模型和执行参数等字段的明确排除项，不增加公开数据列或读取权限。

## 隔离验证与预览

运行 `tools/ai-postgres-rehearsal.py --tests-only --prompt-settings-upgrade --port 55449` 可创建并最终停止独立临时 PostgreSQL 集群。它拒绝生产根目录和生产端口，仅使用合成记录；验证0010→0011、旧会话与消息字段保持一致、重复迁移无动作、迁移 dry-run、真实 reader/writer 健康、无凭据写入拒绝、追加权限、禁止业务写入，以及52张表的 dump/独立恢复摘要。再运行全部 AI 与系统数据目录测试，模型响应由测试替身提供。

2026-09-13 已验证：同步最新 main 后，PostgreSQL 202项测试中199通过、3跳过；全量 Node 单元测试2137通过、0失败、21跳过。后端边界检查、隔离构建、20项构建产物检查通过；网页预览 smoke 通过。Lint 为0错误、11条既有警告。另有51项最终相关回归通过（含新增导航渲染检查）。全仓 TypeScript 检查有145条既有诊断，与未修改的基线逐行一致，未新增诊断；因此不宣称全仓类型检查通过。实际生产发布、真实付费模型和钉钉投递未验收。

预览使用 `npm run preview:isolated`，地址 `http://127.0.0.1:3100/?module=ai`。新增一段明确标记的合成长回复，用于检查表格、输入区和配置证据。预览只允许 GET/HEAD：可以查看配置、编辑本地草稿，不能持久保存或调用模型；保存与恢复写流程由隔离 PostgreSQL 测试验证。

验证记录见 [候选版本证据](evidence/ai-chat-settings-candidate-20260913.json)。

## 下一步采用门槛

用户确认预览效果后，复核最新 main、完成合并；生产采用须另行获得明确授权。发布必须同步 Worker/Django、新迁移、AI 角色授权与健康检查；按现行受控流程做发布前后备份、独立恢复与真实 principal 的正负向接口验收。新代码依赖配置表，不可只更新 Worker 或跳过迁移/授权。生产故障使用 PostgreSQL 备份和兼容代码的受控前向恢复，不恢复旧 D1 路径，不删除配置历史。
