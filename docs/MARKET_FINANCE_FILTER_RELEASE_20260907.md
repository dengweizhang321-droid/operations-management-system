# 市场任务与销售筛选受控发布记录

2026-09-07 经用户明确授权完成本机受控发布。最终 Worker 源码为 `126bb6a93d4fae18b182b266915e5c0ef04d061c`，版本为 `20260907T101721Z-f9782c6c5b04226f`。Django 应用来自首轮已验证提交 `c953d8fca8f7eb52f3b91cfd570793c393d77a1a`，manifest SHA 为 `8475bf740abc98a82bac4e641cfe4e6148a05d29122a5e26b270054b19cfd8c7`；第二轮没有修改 Django 业务实现。

## 修复结果

- 市场分析 → 系统和 AI 设置 → AI 标注与批量入库：已创建的三级类目任务读取进度不再触发只读 reader 写数据库，原任务连续读取返回 200。生产验证没有新建或消费 AI 任务。
- 销售分析 → 财务分析：请求刷新、成功、失败和空结果期间保留月份选择器，支持继续多选月份和店铺；维持请求版本校验，避免显示旧筛选结果。
- 商品经营明细：前端和服务端查询上限统一为 1000 字符，去除只处理前 8 个关键词的隐藏截断。
- 销售总览“货品编码或名称”：支持逗号或换行粘贴多个代码，总长度 1000 字符，最多 100 项，单项仍保留 200 字限制；URL 刷新恢复不会在第 500 字截断。

首轮线上边界回查发现 100 个独立 productQuery 参数叠加其他条件会超过 Django 的 100 个字段限制。最终版本将代码列表合并为一个参数，并在公开 summary 路由兼容归一化重复参数；没有提高全局请求字段限额。实际 100 个代码、1000 字符的两种请求形状均返回 200，1001 字符返回 400。

## 验证与恢复依据

- 全量单测 1937 通过、20 跳过、0 失败；构建、相关 lint 和后端边界检查通过。
- 独立 PostgreSQL 的市场、商品、销售相关 65 项测试通过，迁移检查无变化。
- 隔离镜像的 23 服务启动及重启、16 项公开接口检查通过，业务数据摘要不变。
- 浏览器交互回归覆盖多选期间请求挂起、成功、失败、空结果、刷新恢复和过期响应；新增路由回归覆盖 100 个重复参数压缩及其他筛选与请求头保留。
- 线上 14 项只读回查全部满足预期，最终总控为 Running / Ready / exact_release。
- 发布前最新备份 `daily-20260907T094550Z-a336ad76addd` 及发布后备份 `daily-20260907T102703Z-8c0b92286d41` 均通过完整性复验和独立恢复；恢复摘要与对应备份完全一致。
- 初始类型检查与当时 main 基线均为 142 项既有诊断，无新增；不将全仓类型检查描述为通过。

本次没有应用数据库迁移或为验证写入业务数据；保留前一项库存钉钉同步修复、权限、authority、备份、旧 release 和 D1 退役证据。Worker 两轮激活均经精确计划 SHA 的 successor 流程及启动绑定回读；第二轮仅切换 Worker，Django/PostgreSQL 保持运行。恢复遵循现行 PostgreSQL 架构和兼容代码前向修复，不使用旧 D1 路径。

详细版本、摘要、检查和备份证据见 [发布证据](evidence/market-finance-filter-release-20260907.json)。
