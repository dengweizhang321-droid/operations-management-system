# AI 经营分析受控暂停与首版交接（2026-09-27）

**状态：旧五阶段扩展路线已暂停。** 不再启动影子库演练、增加协议/迁移或部署。整合分支 `codex/ai-business-current-integration` 在 `6f006a2a751fb732430e02d22116e817d3c2a65e`，已推送且工作树干净，未合入 main。冻结 0073 的未验收实验代码单独保存在 `codex/ai-shadow-0073-paused-unverified` 的 `a28d98bfaf1331396bab0bdc4e70478681862073`，已推送且工作树干净；**最后一次 ACL 规范化/分节诊断编辑未运行测试**。旧分支与证据保留，不删除或当成正式可用能力。

| 类别 | 首版处理 |
| --- | --- |
| 可复用 | 既有网店/推广查询与店铺选择、管理员权限和店铺隔离、结构化诊断动作、HTML/XLSX writer 与分卷下载、人工复核；已有志高店真实推广逐日回执及 7 天双格式**来源预览**。优先直接用这些现成能力，不把隔离候选迁移作为首报前置。 |
| 首版必要 | 固定**一家店、一段本期、一项业务问题、一份报告**；先对准当前系统的权威店铺身份与来源批次，核本期/基期口径和金额/点击，再形成可人工复核的原因假设、行动建议、停止/回退条件；报告 HTML/XLSX 同数、可打开。保留幂等、当前权限、来源缺口披露和每报告费用上限。真实模型调用只在具体模型、计费账号和人民币上限明确后进行。 |
| 延期 | 五 Agent 必须并行、市场/ERP/B 端/财报全域同根、同比与店铺去重 UV、57.5 万行全量首报、独立签发/多代 proof/attestation/ledger、未部署协议的长期兼容，以及正式发布。它们保留为长期目标，逐项凭业务需要再纳入，不做首份报告自动前置。 |

**可核首报候选。** 京东“志高商用设备旗舰店”，由现有店铺选择器选精确系统身份，不能仅用参考文件中的 `701455` 或相似店名推断。先用本期 **2026-08-16—08-22**、对照 **2026-07-17—07-23**，回答“推广花费增加而点击下降时，优先核查哪些投放对象与口径，如何小步调整并观察回退？”历史来源回执显示本期 61,543 行、花费 8,984.99 元、点击 4,832；对照 71,200 行、7,526.10 元、点击 5,382。来源是留存的真实历史提取，**不等于当前生产修订**；当前页面同店 30 天花费口径与参考逐行来源已有差异，首报必须先选定同一个权威批次并复算。现有 7 天 renderer 6 三卷 HTML/XLSX 已证明来源行同数，但没有模型诊断、人审或原生 Excel 验收，不能当作已交付报告。见 [志高试用验收](AI_BUSINESS_ZHIGAO_TRIAL_ACCEPTANCE_20260925.md)。

最短依赖路径：① 用现有选择器和查询锁定该店、上述窗口及**一个**可复算的当前来源批次；若 7 天全量超过旧 64 MiB 限额，首报只取已核聚合与明确分页明细，不声称全量扫描。② 用已有诊断/报表流程形成单次分析与人工复核，数值引用回查来源，缺少的 B 端/财报/市场或店铺 UV 明示未纳入。③ 在明确模型与费用上限后做一次真实模型验收，人工核行动建议。④ 输出一份同数 HTML/XLSX，打开核对关键值、来源、日期和缺口，再决定是否扩大范围。首版完成标准是**真实数值可对账、建议可执行、文件可打开**，不是协议或 Agent 数量。

当前阻断：两家店原始财报的稳定店铺映射、京东店铺 UV 官方字段、真实模型/计费账号与每报告人民币上限仍未提供；这些不妨碍先做上述单店推广 MVP，但相应维度和付费调用保持关闭。既有 0077–0079 仅为隔离候选，五个 job 仍暂停且零模型/已读；0078 publication 不开放正式 ready 或 Web 下载。正式数据库迁移、加密备份与异集群恢复也未获采用许可。

收尾状态：本任务没有影子 PG、`pg_ctl`、归档 Python 或相关端口在运行，正式服务未触碰。已开始的合成 runroot 源端 SHA 在**搬运前**精确停止；`D:\.codex\worktrees\ai-shadow-0073-snapshot\运营管理系统\.runtime\ai-pg-f51724c4bfd5` 保留，E 对应完整目录不存在。此前五个完整失败 runroot 已逐文件 SHA 移 E，清单为 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\shadow-0073-full-runroots\archive-manifest.json`。另一个已停的 0079 隔离 runroot `D:\.codex\worktrees\ai-market-human-cap\运营管理系统\.runtime\ai-pg-7bc08ea28a22` 仍有随机测试 `password.txt`；精确删除命令被自动策略以 `blocked by policy` 拒绝，未绕过，需后续受控清理。它不是正式凭据，数据库已停、端口无监听。
