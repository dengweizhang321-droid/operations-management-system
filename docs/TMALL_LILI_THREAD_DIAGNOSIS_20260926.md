# 丽力货品线程异常：诊断版本已采用，原 M 异常仍待复现

## 2026-09-26 受控采用与一次真实复现

用户明确授权采用诊断版本，并从原 n8n 丽力工作流完整运行一次。源码 `fc8661b4` 已合入 main；Worker/helper `20260926T151320Z-78a1701b451cdc4a`、manifest `197201c1bd4c4e52b5988d1184f99da7f77d0230511e12c2fdb4ddd880f9c020` 已通过原在线准备、精确 SHA apply 和唯一控制器启动。此次仅 Worker/helper 停止并启动，数据库与 n8n 原 PID 保持，Django 部署清单及 18 条 n8n 定义不变。

新完整 execution **4594** 于上海时间 23:29:01–23:29:16 在 **A** 阶段失败：`browserType.connectOverCDP: Timeout 10000ms exceeded`。WebSocket 已连接，但 Playwright 浏览器初始化未完成；业务请求返回 500，浏览器关闭回执 `closed`，helper 正常释放、没有隔离。B/C/P/M 未执行，未新增下载或导入，**未捕获原 M 线程崩溃，不把此次连接超时冒充原根因**。这类超时在诊断版发布前的只读探针也曾出现。

原错误流程 **4595** 按既有规则等待至 **9 月 27 日 00:29:16** 后完整重试，等待不占 helper；未新增手动运行、取消重试或修改错误分类。丽力商品日报/推广日报仍分别为 9 月 25 日 77/44 行，货品仍为 9 月 25 日快照 2,488 行。原 9 月 26 日货品计划保留 `planned`、空批次，节奏未推进。

发布前备份 `daily-20260926T151330Z-5018e630cf26` Verify 通过，独立恢复 `6b5675e74853` 内容一致并完成隔离清理；发布后备份 `daily-20260926T153137Z-1f09eb83ae96` Verify 通过，两份均三文件摘要匹配归档 E。D 盘空间不足以容纳独立恢复，33 份已有 E 副本的旧备份经逐文件名称、大小和 SHA 核验后，将 D 原件移至 E 本次归档目录并再次核验，共 99 文件、24,684,182,278 字节；无备份或业务数据删除。

最终 Running / Ready / exact_release，12 组件、启动绑定、看门狗自然健康检查通过，9325 关闭。完整证据见 [生产诊断记录](evidence/tmall-lili-diagnostic-production-20260926.json)。此次授权不延伸至新的修改发布、浏览器配置重置或额外手动重放；下方为采用前的诊断与测试记录。

2026-09-26 用户要求定位货品节点线程异常。本轮只读检查和隔离测试没有触发新的正式 execution、服务重启、平台导出或数据导入。

## 已证实的现场事实

- 正式 helper 为 `20260925T213040Z-e45b1332207bc4cf`。构建回执中 CDP、Playwright 适配器、货品直连、货品登录、隔离器和流水线六个源文件 SHA 与当前源码一致。
- 原定时 4439 在 M 阶段约 2.993 秒后返回 `helper_slot_unavailable/503`。经上一轮授权清理并重启 Worker/helper 后，原工作流新完整 4583 仍在 M 阶段约 362 毫秒后发生同样错误。
- 4583 的 A 登录和店铺身份通过；商品日、推广日截至 9 月 25 日无缺口，B/C/P 按覆盖正常跳过。货品审计的更新时间到达本轮，但仍为 `planned`、空 `batches`，没有进入商品列表完成后的批次阶段。
- 隔离器把未知退出保留为 quarantined。旧实现的 Worker error 回调丢弃原 Error，且在 ready Promise 已 resolve 后调用 reject 无法把错误交付上层；exit 回调也忽略退出码，线程 stdout/stderr 被直接消耗。这解释了为什么 n8n 只能收到泛化 503，但不是线程异常的底层原因。

## 本轮只读浏览器探针

- 仅连接当前丽力 9325，未导航、点击、读取凭据、提交导出或导入。
- 浏览器级 `Browser.getVersion` 和 `Target.getTargets` 正常返回。浏览器为 Chromium 151.0.7922.109，有两个千牛 page target。
- 在独立 512 MiB Worker 中，用与正式相同版本的 Playwright 1.61.1 建立 CDP 连接，连续两次均在 10 秒初始化上限返回 TimeoutError。其中一个页面的 Page.enable、Page.getFrameTree、Runtime.enable 等初始化命令未返回，另一页面有正常协议响应。
- 探针通过 catch 返回错误并正常退出，**没有复现原线程崩溃**。故不能把现场连接超时、页面未响应或 512 MiB 限额直接认作原根因，也不能排除它们只是线程退出后的遗留状态。原已丢弃的异常不能事后恢复。

## 诊断候选的范围

- 在线程 error 和非 clean exit 时记录固定事件名、原 workflow/store/execution 归属、最后固定阶段、ready 状态和退出码。
- Error 只输出受限的错误类别、错误名称和已知源码文件的行列位置。不得输出 message、URL、堆栈函数名、完整路径、Cookie、Token、页面正文或任意自定义错误属性；阶段只接受固定白名单。
- 货品阶段增加计划、认证、CDP 连接、列表请求捕获和分页读取的固定阶段标记。
- 通过现有 supervisor 的 stderr 日志保留。未改变内存限额、超时、导出逻辑、隔离、自动重试、资源接管或服务生命周期；这不是恢复修复，也不会自动解除当前隔离。
- 合成真实线程测试覆盖 ready 后异常、显式异常退出、正常收尾、非法阶段和敏感内容过滤，以及诊断回调失败仍保持原收尾契约。原 6 店真实编译 helper 镜像通过，无业务动作。

## 下一步门槛

候选测试结果另附。采用此诊断版本需要当前明确的 Worker/helper 发布维护授权；再次真实复现只能经原 n8n 完整工作流，并单独限定一次。不能把此前一次 Stop/Start 和执行 4583 的授权延伸到本次发布及新执行。获取新的脱敏原始异常后，才能确定修复位置；没有证据前不升级浏览器、不提高内存、不删除活动清单。

## 候选验证

- 线程诊断及原隔离定向测试：12/12 通过。
- 原生产形状的编译 helper 镜像：六店独立领取、同店互斥、跨店身份拒绝、禁用店拒绝及越序拒绝通过，业务动作 0。
- 完整单元测试集合以 `--test-concurrency=4` 执行：2,281 项，2,256 通过、23 跳过、2 失败。失败为既有财务路由静态断言和缺失 `color-surface-subtle` 样式变量；将 HEAD 的测试和全部被读取文件复制到独立临时基线后，这两项均独立复现。不能宣称全量全通过。
- 全库 lint：0 错误、11 项既有警告；`git diff --check` 通过。
- 本轮没有生产构建、版本切换、维护或新的 n8n 业务执行。源码候选不代表真实异常已定位或服务已恢复。
