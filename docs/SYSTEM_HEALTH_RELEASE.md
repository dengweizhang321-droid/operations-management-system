# 系统体检第一、二批正式采用

2026-09-16，用户明确要求“合并到主线并发布上线”。两批已合入并推送 main，按本机受控维护流程完成发布；无需数据库迁移、新增索引或权限扩展。

## 采用版本

| 项目 | 版本 |
|---|---|
| 网页、Worker/helper 源码 | `dd0f4271befdb3c035a6d2e163dcf9416be2095b`，包含同期早班配置 |
| 最终 Django 源码 | `c84bb034154bc8327f252fe34fd0fbd6ea7e55c8` |
| Worker/helper | `20260916T020314Z-21945e0e18c57de1` |
| Worker manifest SHA-256 | `8ac5442f1c561c5c2ee8f0c71b1ece8ded7f094b0145ab42b03704c36be94a43` |
| Django manifest SHA-256 | `23274b9c1d44b69c1327179240a11b56e35f6a92b2ff9be549b2d5145d5d639f` |

第一批包括客服保存失败/重复点击保护、市场统计口径和通用数据目录字段。第二批包括主数据数据库内分页、当前页批量关联、榜单请求内聚合复用和筛选项合并读取。页面布局和公开契约保持原样。

## 正式验收

来源为经授权的本机只读 API，销售数据覆盖截至 **2026-09-15**。市场 revision 为 `1006:401937025c73`：主数据 48,205 个身份，榜单全范围 321,102 条。每个变体只读一次，默认页另复读一次；不是压力测试或生产 P95。

| 请求 | 最终首次 | 重复/其他页 | 结果 |
|---|---:|---:|---|
| 主数据默认页 | 1.371 秒 | 重读 0.560 秒；第二页 0.432 秒 | 200，分页正确 |
| 榜单默认页 | 7.996 秒 | 重读 5.447 秒；第二页 4.980 秒 | 200，首页完整响应摘要与旧版完全一致 |
| 榜单“未确认价格” | 5.182 秒 | — | 200，240,678 条 |
| 主数据空结果 | 0.211 秒 | — | 200，零条 |
| 市场统计卡片 | 2.093 秒 | — | 200 |

客服列表、主页正常；非法 view 返回 400。四份正式前端资源与不可变 release 字节一致。最终 Running / Ready / exact_release、12 组件、启动绑定、Django 守护和钉钉 connected 均复验。

发布前旧主数据仍在 30 秒后 503；旧榜单本轮为 16.137 秒，但与独立恢复同时运行，不能用该数字计算严格提速比例。原历史正式榜单约 12–13 秒；隔离同库旧/新对比详见第二批报告。**榜单 3 秒 P95 目标仍未达到，长期内存增长仍未在本批解决。**

## 首轮问题、修正与验证边界

首轮新主数据正常，但三个榜单变体均因单条 SQL 超过原 6 秒限制返回 503。只读 EXPLAIN 显示：有效价格条件高度相关，哈希长度与字符表达式过滤使价格侧估算降到 1 行，产生无索引的 Nested Loop / Materialize，合成库未覆盖该执行计划。

补丁保持状态、类型和正价过滤，把哈希校验改为结果表达式：无效哈希对应 NULL 正式价。唯一快照身份保证原语义和条数不变；没有放宽超时、修改数据库统计或生产配置。相同正式统计下的 EXPLAIN 改为 Hash Join，价格侧估计 1,999 行。第二次受控切换后榜单恢复，首页完整响应 SHA-256 与发布前一致。

补充验证为隔离 PostgreSQL 29 项通过，覆盖 10 万行关联条件、25 万行以上分页、结果等价、价格边界、主数据及签名/权限契约；SQLite 7 通过、2 个 PostgreSQL 场景跳过；迁移 dry-run 无变化。新增关联条件样本在原实现上也能通过，不能声称它稳定复现了正式慢计划；正式 EXPLAIN 与最终接口回读是此次修正的直接证据。原候选的 199 项集成测试等记录保留，不把它们冒充补丁后的全量重跑。

## 维护与备份

两轮切换前均核查市场、AI、图片任务无在途领取且 helper 空闲；原市场计划均已结束/删除，未新增暂停或恢复业务操作。使用精确维护 ID、DeployApp/HardenAcl、不可变 successor 和唯一总控，既有 Django 守护通过受保护 runtime 隐藏恢复；没有新建监控。

发布前备份 `daily-20260916T015058Z-c3339d8dd7d9` 独立恢复内容摘要一致，端口 55453 的临时 cluster 已停止清理，生产库未被恢复演练改动。前后备份均复验并以三个文件逐项摘要归档到 E 盘；D 盘原备份保留。完整摘要见 [脱敏生产证据](evidence/health-fixes-production-20260916.json)。

没有重启 n8n、手动补跑下载、触发付费模型或真实发送。维护期间周报检查 `2452/2455/2456/2459` 失败，随后 `2457/2458/2460` 自然成功；10:30 市场任务 `2461` 自然成功。10:00 京东四店 `2453` 因维护失败，既有安全重试 `2454` 在最终检查时仍 waiting，业务恢复终验交原有调度及监控，不能表述为京东四店当天已成功。
