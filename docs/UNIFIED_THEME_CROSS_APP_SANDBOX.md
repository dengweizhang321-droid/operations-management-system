# 统一主题与跨模块沙箱计算（候选）

2026-09-12 在 `codex/unified-green-cross-app-sandbox` 隔离工作树实现。尚未合并或生产采用；本机 3100 预览仅使用合成数据，AI 写请求在预览中关闭。

## 界面

以 AI 对话原有深绿 `#293f32`、浅绿 `#f3f7f4`、正文 `#33453a` 为全系统基础。`app/styles/tokens.css` 是共享色值源；`shared-theme.css` 统一导航、Tab、公共筛选、日历、视图切换、导入卡片和深色横幅的交互状态；既有全局样式的界面蓝色字面量改为主题变量，保留风险、涨跌和图表维度的业务颜色。

用户预览反馈已纳入：自动化中心横幅减少内边距、标题尺寸和标签间距；商品经营同步按钮使用白字，并统一商品经营、市场 AI 标注横幅；市场与系统设置共用的大图/列表、卡片/表格切换，以及导入卡片图标、边框和说明文字同步改色。

删除 `app/ai-sandbox-view.tsx` 及“分析沙箱”Tab，并同步导航目录与 AI 页面上下文。旧 `?module=ai&view=sandbox` 规范化为 AI 对话。保留旧 JSON 沙箱后端/API、数据、审计和中央工具；pandas 计算继续通过 AI 对话、原有 Agent/钉钉工具链调用。

## 计算修复

- 原沙箱只识别数组父级的 `truncated/hasMore`，导致分页元数据位于 `pagination` 的完整业务结果被拒绝。现在对明确列出的库存、财务目标、网店、客服和运营事务数据集按 `page/limit` 连续读取，逐页验证页号、页大小、返回条数、总数与截断状态；广东入仓使用其实际 `totalPages` 契约。未知分页协议仍拒绝。
- 查询必须从第一页开始；后续页总行数变化、短页、重复游标、字段截断和上限超出时拒绝整个计算，不能拿最后一页或部分数据当全量。
- 每个输入可提供 `columns`，仅从已授权响应选取明确存在的标量字段，避免嵌套对象阻断容器输入。不会补造字段、放宽源角色或展开私有对象。
- 数据集描述增加 `pandasExport` 提示，要求先查询确认数组和字段；销售品类趋势使用 `trend.items`。跨 app 指系统内的业务领域；仍只允许最多 3 个授权数据集、共 2000 行/2 MiB、每源最多 20 页、导出 8 秒、容器计算 8 秒。
- pandas 结果列允许“销售额（元）”“毛利率 (%)”等单位标签；表格生成、保存回读与 CSV 校验共用此规则。其他工具保持既有列名规则，敏感字段屏蔽和 CSV 公式防护继续生效。

## 验证及限制

使用独立 PostgreSQL 55449 和独立测试 broker 18121，随机测试通道密钥、独立防重账本和容器标签；测试复用已核验的本地固定镜像，不读取生产通道密钥或更改正式 broker。成功后停止测试 broker、验证零残留，并停止独立 PostgreSQL。

真实三领域测试在镜像库写入合成销售、ERP 和库存记录，通过真实领域 SQL reader 连续导出，HMAC 调用真实 Linux/rootless 容器：销售 `10000 - 2500` 分得到 `75` 元，净销量 `2 - 1 = 1`，关联库存 `8`；验证三域来源、4 页源查询审计和结果 receipt。边缘网络目录/转发使用受控测试替身，容器和数据库执行为真实。模型协议测试另行覆盖 OpenAI/Anthropic 工具回传、中文单位列、私有表格和 CSV 下载、其他用户拒绝。

最终检查计数见 `docs/evidence/unified-theme-cross-app-sandbox-candidate-20260912.json`。没有业务迁移、真实业务写入、付费模型请求、钉钉发送或正式服务切换。真实模型自动选择数据集与代码的自然语言端到端成功率尚未验收；计算完整性也不代表跨域原子快照或日期覆盖完整。

## 后续采用

按仓库规范先验收 3100 界面，再合并；生产采用另行授权，走既有备份、独立恢复、Django/Worker 受控部署及回查流程。本变更不修改 broker 包或镜像，无需修改其固定镜像配置；不能以候选验证代替正式聊天链验收。未合并分支和预览工作树需保留。
