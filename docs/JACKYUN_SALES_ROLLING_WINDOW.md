# 吉客云销售明细最近 45 天滚动同步

状态：2026-09-23 已按用户明确短暂维护授权，同步采用 Worker/helper 与 Django 销售写侧。后续新 API 计划使用最近 45 天；旧计划保持原范围。本次未触发真实 45 天下载/导入。

## 业务口径

销售单明细账沿用发货时间及既有下单时间缺失回退口径，每次完整同步截至上海业务日期的昨天，包含昨天在内共 45 个自然日。结束日为运行日减 1 天，开始日为结束日减 44 天。2026-09-23 执行时为 **2026-08-09 00:00:00 至 2026-09-22 23:59:59**；2026-09-01 执行时为 7 月 18 日至 8 月 31 日。跨月、跨年、闰日和月初均按同一规则，不再按月截断。

每次成功采集、完整校验并发布的新快照更新该范围的明细：金额、数量等变化采用本次新值，窗口内本次不再出现的旧明细按原完整范围替换规则移除；其他日期与渠道的历史保留。同范围同内容沿用原幂等规则，不把多次导入的数量和金额累加。新同步仍按原全局 helper/吉客云锁串行，未决导出或未闭合运行不得跳过。

“最新”在这里指本自动链路本次新获取且成功验证发布的完整快照，不以文件名或文件修改时间排序。本改动不增加任意历史手工文件、不同执行器迟到文件的来源水位协议，也不改变发货时间为取数范围的口径。

货品、分仓库存、库龄与组合装四表的日期/范围保持原规则。销售库存成本匹配、零成本清洗、仓库过滤、渠道白名单、来源行数校验及五表全部校验后才导入的门禁保持原规则。

## 日期如何贯通

新 `plan-api` 创建 v2 计划，显式保存 `salesStartDate` 和原 `asOfDate`；同一 execution 重试不重算日期。v2 缺少起始日、不是完整 45 天或运输协议不匹配均拒绝。历史 v1 计划仍保留原月度含义，不能把旧月度文件解释为 45 天数据。

共同的 `lib/jackyun/sales-period.ts` 用于计划、商家 API 查询/导出条件、交接日期读回、销售预处理、元数据与分片上传范围、导入演练和独立批次回查。控制器保存并比对起始日；输入摘要为携带起始日的销售合同使用 v4，未携带该字段的旧合同仍按原 v3 字节算法核验。处理子流程返回的日期范围也必须与父运行一致，原始和处理后文件仍核对完整摘要。

API 任务续跑保留原起始日并比较控制状态，月度控制器不能接管滚动计划。既有页面异常的零业务关闭 operator 支持 v2，仍执行原身份、日期、摘要、helper 空闲和业务效果不存在门禁；不会自动关闭任何失败运行。

## 订单更新与滚动边界

复用 Django/PostgreSQL 原有全销售写入基域锁、输入归属、状态 token、内容指纹、事务内 upsert 和范围内旧行清理，没有新增业务表或迁移。

补齐一个已在隔离库复现的边界：当窗口移动但明细恰好不变时，旧代码会把它当成缺少元数据的历史兼容数据，返回旧范围及旧文件回执。现在已具备完整范围元数据的现代批次走正常新范围发布，事实行不重复，归属与新范围/新原文件一致。同范围同内容仍返回 duplicate；真正缺少范围元数据的历史兼容去重保留。

## 验证和采用

候选验证覆盖 45 天边界、月初、跨年、闰日、上海零点、旧计划保留、范围篡改、控制器跨范围拒绝、交接和输入摘要、预处理保留上月/边界行、排除当天与范围外行、原子关闭及续跑兼容。独立 PostgreSQL 检查覆盖金额/数量更新、重复不累计、窗口移出后的历史保留、其他渠道隔离、发布失败回滚，以及范围改变但内容不变时的新回执。详见 [候选证据](evidence/jackyun-sales-rolling-45d-candidate-20260923.json)。

采用步骤：准备干净候选和 Django 应用收据；发布前备份及独立恢复；确认业务空闲后按明确维护授权进入持久维护，采用 Worker/helper 与 Django 销售写侧，保留原权限/启动/备份门禁。没有新增迁移。然后回读服务、版本绑定和原数据，在线更新原 n8n 主流程说明文字为 45 天；业务节点、目标、调度及共享重试流程无需改动。n8n 无需重启。

实际 45 天业务下载/导入与真实批次回查尚未验收，不能用夹具或构建通过冒充上线。采用后的新业务测试仍从原 n8n 完整入口启动。回退不能删除 v2 起始日或把 v2 改成 v1，需保留已创建计划的兼容解析和原始审计证据。

## 2026-09-23 正式采用

源码 `0ac4a4653b5c5338e4f7df637bd6a704ffad2871`；Worker/helper `20260923T035640Z-3d8e2a24509c3c03`，manifest `5c1c3aa32daaf61bd57ed864815d4ee166d3062d920e146a1404559918834be6`。Django 通过绑定候选收据采用，app fingerprint `645dfe7fb06a8ba33a4db8b286d07b23a1d21d1330f26487cd180cd65e906882`；受保护销售写侧文件与候选固定源逐字节相同。

首次 Django 在线预置因新固定工作树未安装锁定的 Wrangler 依赖而失败，正式 app 未变化；按 package-lock 安装依赖后，原完整 PrepareApp 门禁通过，没有放宽自检或改依赖版本。之后完成发布前备份的独立恢复与 E 盘归档，保留 PostgreSQL 进入应用维护，依次 DeployApp/HardenAcl、Worker apply、退出维护及唯一 Start；后台守护通过原受保护 Run 补齐。

最终 Running/Ready/exact_release、12 组件、启动绑定、15 资源、守护 healthy、钉钉 connected、两轮独立看门狗及发布后备份复验/E 盘逐文件归档通过。62 条迁移清单不变，销售 revision `35:34` 和 4121 批次、库存批次及旧运行计划/active/n8n 执行记录未变。

原 n8n 吉客云工作流仅在线更新范围说明，发布版本 `91d35933-8b9f-4ae8-a444-cbd74ee64381` 与 current 一致。编辑器对齐位置、省略 Wait v1.1 的默认 5 分钟/timeInterval 与 Webhook 的 onReceived 默认参数、生成等待节点 webhookId，并补充仅交换 JSON 流程的 binaryMode；已核对原业务参数、连线、00:10 调度及另 17 条流程未变。没有重启 n8n。原其他流程等待重试 4133（12:50）保留，未停止、补跑或接管。

本次正式验收证明新代码和 45 天日期计算已经采用，未宣称真实 45 天数据已更新；下一次完整执行才会新建 v2 计划并同步该范围。详见 [正式证据](evidence/jackyun-sales-rolling-45d-production-20260923.json)。本次授权不延伸至未来停服或手动补跑。
