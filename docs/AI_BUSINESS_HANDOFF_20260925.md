# AI 经营分析推广专项：小阶段交接（2026-09-25）

本清单只描述隔离分支 `codex/ai-business-current-integration`；未部署生产、未迁移正式数据库、未调用付费模型或发送报告。用户要求本小阶段完成后暂停，并保存交接、提交、推送。

## 本阶段已接

- 推广专项 renderer 9 采用显式 `promotionTrialVolumes` 创建，保留 renderer 7 与旧 1–6 的协议和文件。仅在五个 Agent 节点完成、六节点工作流输出一致、人工明确批准及来源重新核验后，可将完整暂存卷发布为可下载文件；父任务取消后的文件可安全暂停或取消，不能继续发布。
- 同一份批准内容生成行动建议、来源时间范围、对账与缺口、推广明细和完整卷目录；每卷 HTML/XLSX 以及完整 JSON 清单持久化并按块下载，客户端重算文件 SHA-256、复验账号和发布状态。预算没有接入生成链路，文件和页面明确标为“未交付”，不会捏造分配金额。
- 页面保留原推广报告文件入口，并增加独立的“生成推广专项试用版”。市场 v2 仍是默认关闭的有界只读预览；不会冒充正式市场 Agent 已读取。

## 已核验的证据

- renderer 9 纯渲染合同 37 项、行动/范围等纯合同回归及客户端分卷下载 35 项通过。
- 新文件数据库门禁与实际持久交付的隔离 PostgreSQL 12 项通过：`.runtime/ai-pg-7ac69af248eb/tests.log`。标题漂移阻断、父流程取消后文件安全收尾在其中。
- 备份纯校验 22 项通过；页面 `npm run build` 通过。所有测试使用隔离工作树和数据库。
- 0045→0046 升级、旧 1–7 文件字节与权限、迁移前后独立恢复及空数据回退通过：`.runtime/ai-pg-bd27e4980c95/business-promotion-trial-file-upgrade-evidence.json`。该升级演练不执行发布器；发布/下载由上面的目标数据库测试覆盖。

## 明确未关闭

- 本阶段是推广专项的可试用交付路径，不等于原五阶段全部完成。真实店铺/市场/ERP/B 端/财务的多期组合、真实模型判断、参考报告业务深度、全量 575,095 行规模与原生 Excel 开启/复算尚待验收。
- UV 没有可靠的店铺每日唯一去重来源；不把通用店铺总览或跨天 UV 求和当作区间 UV。预算仍未进入试用版 HTML/XLSX。市场 v2 缺正式 Agent 持久读取，财务 v3 未成为正式报告文件。
- 当前完整清单固定表键、标题、行列数与行摘要；列名和表说明未纳入持久清单的版本化证明。本阶段不宣称覆盖此边界。

## 下次接续顺序

1. 先检查本清单对应的升级演练最终摘要、分支 HEAD、`git status` 与远端推送结果；不得用中途通过的日志替代最终摘要。
2. 在当前来源版本上补真实跨源数据覆盖与数值对账，优先解决来源身份、UV、预算、市场 Agent/财务正式接线；缺源继续显式留空。
3. 在受控真实业务与 Office 条件满足后验收模型质量、大规模资源、完整 HTML/XLSX 和旧版本兼容。正式服务采用另行走现有维护/备份/回退门禁。
