# 库存管理板块说明

## 1. 目标与入口

库存管理用于把库存快照、销售需求、库龄、固定成本、在途与运营执行事项放在同一条链路中。页面包含：

- 库存总览：保留库存健康分布、优先补货建议和按货品、仓别展开的“库存健康明细（近30天）”；不再展示顶部库存 KPI 卡片、数据质量暂停横幅或重复的“销量近30天”表格。库存健康分布只统计京东仓、天猫履约仓（系统仓库分类 `cainiao`）、精确广东仓和自营仓，排除工厂代发、售后、样品、海外等其他仓别。
- 库龄分析：10 个精细库龄区间、数量占比、货值占比与明细。
- 备货计划：建议加入草稿、确认、完成或取消；支持下载标准 Excel 模板后批量导入多个规格，草稿可多选后一键确认并提交钉钉“备货管理”，已确认计划可批量重试同步、按采购/工厂发送群消息，也可生成采购执行事项。
- 滞销清理：高库龄、低动销与滞销候选；可生成清理执行事项。
- 京东入仓监控：RDC/DC 区域库存、在途、正向出库、计算周转、供应商与长库龄风险。
- 广东入仓监控：人工型号清单、广东仓库存与出库、供应商完整到仓周期、风险健康分布及 Excel 导入导出。上线须采用包含库存迁移 `0007_guangdong_monitor` 的版本。

所有库存、销售和成本分析继续排除 `刷刷仓`。

库存后端已完成 Django/PostgreSQL 正式单写切换：Worker 负责真实鉴权、XLSX 解析、HMAC 和有界传输，库存 reader/writer 固定使用 `127.0.0.1:8051/8052`。实现、迁移证据和运维边界见 [`DJANGO_INVENTORY_MIGRATION.md`](DJANGO_INVENTORY_MIGRATION.md)。

## 2. 公共筛选与跨 Tab 持久化

库存管理的六个 Tab 共用一套顶部筛选组件。货品编码或名称、仓库、品牌、品类属于公共业务范围，切换库存总览、库龄分析、备货计划、滞销清理或京东入仓监控时会保留并同步应用；各 Tab 的专属条件仍独立保留，例如总览的库存类型和健康状态、库龄页的风险状态和库龄区间、备货计划状态、入仓供应商。广东入仓监控固定使用精确名称“广东仓”，忽略其他页面遗留的仓库筛选，并以独立 URL 参数 `inventoryGuangdongRisk` 保存风险分类。

筛选状态写入当前页面 URL，浏览器前进、后退、刷新及复制链接后均可恢复。所有公共维度由各接口在服务端筛选，页面不加载全量数据后再做本地过滤；“库存健康明细（近30天）”同样只聚合筛选后的库存范围。库存总览不跟随全局自定义日期改变销量口径，固定以销售数据最新截止日向前 30 个自然日计算。筛选变化会重置对应分页、取消旧请求，并通过请求代次门禁阻止迟到结果覆盖当前范围。品牌和品类选项以各接口当前库存或库龄批次的未筛选全集为准，当前筛选没有结果时也不会把已选条件清空。

## 3. 数据来源与口径

| 数据 | 来源 | 主要用途 |
| --- | --- | --- |
| 分仓库存 | 最新完成的库存导入批次 | 可用、实盘、锁定、在途、库龄、固定成本、规格默认供应商 |
| 库龄报表 | 最新完成的吉客云库龄批次 | 库龄、7 日销量、30 日销量；存在时优先用于库龄分析 |
| 销售明细 | 最新截止日向前 30 个自然日的销售订单行 | 同货品、同规范化仓库的正向销量，以及货品近 30 天总销量；退款不冲减备货需求 |
| ERP 货品档案 | 货品主数据 | 品名、固定成本回退，以及老快照缺少规格供应商时的供应商回退 |
| 运营设置 | 系统设置 | 补货阈值、销量窗口、负库存、自动补货和预警开关 |

金额按人民币分保存。库存货值仅统计有可信固定成本且可用库存为正的部分；成本不完整时页面必须标注覆盖率，不得把缺失成本按零元解释。

`config/inventory-warehouse-mapping.json` 固化用户提供的《仓库类型映射.xlsx》：283 个仓库无重复，其中代发仓 219 个、京东仓 45 个、菜鸟仓 3 个，另含广东、售后、样品、海外、虚拟和异常仓；65 个仓库标记“计入库存=是”，218 个标记为“否”。导入时 Worker 与 Django 同时核验仓库分类和计入标记；总览库存、健康和备货计划只使用标记为“是”的仓库，近 30 天工作台仍展示代发仓的供应商库存、销量、周转和在途。未出现在配置中的新仓库沿用受控名称识别并默认计入，避免静默丢失新仓数据。

京东入仓仓库优先使用受控映射中的“京东仓”，同时兼容吉客云历史快照中的区域平台仓命名，例如 `上海常温C平台仓9号库-CHN`。兼容识别只影响查询分类，不改写历史事实。

## 4. 数据质量门禁

导入前拒绝以下高风险数据：

- 合计、总计、小计、汇总或 `total` 行。
- 系统未允许负库存时的负实盘或负可用库存。
- 单行数量绝对值超过 1,000 万。
- 库龄小于 0 或大于 3,650 天，避免把 Excel 日期序号误当库龄。
- 单件成本超过 100 万元或单行库存货值超过 10 亿元。

总览计算后还会执行全局门禁：

- 同货品、同仓库销量匹配率低于 60% 时，暂停精确补货数量、平均可售天数和新增备货计划。
- 自动补货设置关闭时，暂停自动建议。
- 库存快照超过 3 天时提示数据过期。
- 平均库存数量或平均库存货值明显异常时，暂停精确建议。

筛选或定位单一货品不能绕过全局质量门禁。页面不再单独展示质量暂停横幅，但创建计划的服务端门禁仍保持有效。“库存健康明细（近30天）”按近 30 天总销量降序最多返回 50 个货品，并按京东、供应商（映射分类为代发仓）、售后、广东、样品、菜鸟和自营仓展示库存、销量、计算周转与在途，同时汇总计入库存范围内的总库存、已覆盖库存金额、总在途、货品近 30 天正向总销量、总周转、建议补货和预警。`刷刷仓` 始终排除。

## 5. 库龄结构

精细库龄区间为：`0–7`、`8–15`、`16–30`、`31–60`、`61–90`、`91–120`、`121–150`、`151–180`、`181–360`、`1 年以上`。

每个区间同时展示：

- SKU × 仓库记录数；
- 可用库存数量与数量占比；
- 已覆盖库存货值与货值占比。

页面可同时选择多个库龄区间，并可按品牌、品类、仓库和风险状态多选；筛选在服务端应用，并同步更新指标、分布图和明细。品牌优先取 ERP 货品档案，库龄快照没有对应货品档案时明确显示“未设置品牌”；品类沿用当前库龄或库存快照，空值统一归为“未分类”。分布图使用“库存数量柱状图 + 库存金额折线图”的双纵轴表达；库龄金额统一按“正向可用库存 × 固定成本价”计算，不采用报表中的零售价或其他金额字段。

库龄分析明细同时展示品牌、库龄分布区间、库龄天数、库存数和库存金额；当前页 CSV 使用相同字段与口径，库存金额以人民币元导出。

数量、货值占比只以库龄已识别且可用库存大于 0 的记录为分母。缺失库龄的库存单独披露，不进入区间占比。旧四区间响应仍保留，避免现有消费者失效。

## 6. 执行闭环与权限

- `viewer`、`analyst`：只读查看。
- `operator`、`admin`：维护备货计划、从已确认计划创建采购事项、创建滞销清理事项。
- `operator`、`admin`：可把已确认计划同步到配置绑定的钉钉多维表；受限数据范围账号不得执行。
- `operator`、`admin`：可多选已确认且已填写采购、工厂的计划，先预览，再确认发送到系统设置绑定的钉钉群；受限数据范围账号不得执行。
- `admin`：另外允许同步库存文件。

库存总览可从“库存健康明细（近30天）”打开“创建备货计划”。货品名称、品牌、供应商、现有库存、近 30 天正向总销量和预计消耗周期由当前权威数据回填，其中预计消耗周期允许人工调整为 0–3,650 天、最多一位小数；入库库房只能来自映射表标记为“计入库存=是”的当前库存行。部门预填“志高项目组”且允许清空；人工填写的备货数量、对应采购、对应运营、部门、备货类型、下单日期、预计到货日、验货和备注随计划持久化。没有精确系统建议时仍可创建人工计划，但必须显式填写正数备货量并保留“人工创建”原因；这不会把低可信建议伪装成系统建议。

备货计划页可下载《备货计划导入模板.xlsx》，填写货品编码、入库库房、备货数量及可选的采购、运营、部门、类型、日期、预计消耗周期、状态、验货和备注后一次导入。货品编码按文本读取以保留前导零；单次最多 200 行、文件不超过 4 MiB，不接受公式、额外列或重复的“货品编码 + 入库库房”。状态留空默认“已确认”，部门留空默认“志高项目组”，验货留空默认“否”。导入会一次性回读最新权威库存快照并校验每个规格与仓库，任一行无效则整批不落库；过期快照必须由用户明确确认。系统从库存权威数据回填货品、供应商、库存、销量与建议，只把表中人工字段写入计划。导入本身不会自动调用钉钉，避免未复核数据直接产生外部写入。

采购事项只能来自已确认的备货计划，并记录计划类型、供应商、入仓仓库、计划数量和预计到货日。滞销清理事项在保存前会重新核验最新库龄快照，只接受当前仍属于高库龄、低动销或滞销的货品。执行事项进入“运营事务”并关联来源对象；不会自动修改、删除或重新导入库存事实。

通过页面单条表单保存为“已确认”或把草稿确认时，页面会在本地确认成功后立即把同一计划提交到 `config/dingtalk-replenishment.json` 绑定的《备货计划进销存表》/“备货管理”。草稿支持当前页多选并一次确认后顺序执行“本地确认 → 钉钉幂等提交与写后回查”；每条草稿使用页面当前备货数量，完整成功项取消勾选，本地确认或钉钉提交未完成的项目保留勾选。混合选择草稿和已确认计划时，两个批量按钮只处理各自状态，不互相丢失选择；批量导入仍须在复核后显式操作。历史未同步计划或同步失败计划保留手工重试按钮。同步前回读表、字段和单选项契约。对应采购、对应运营和部门按钉钉真实 ID 精确解析；重名、无法匹配或单选项缺失时失败关闭。每条记录的备注包含 `[运营管理系统备货计划ID:<id>]` 标记；查询仍兼容历史 `[TERUISI备货计划ID:<id>]`，下一次受控同步会更新原记录而不是新增重复行。写后逐字段回查；系统同时保存钉钉记录 ID、当前格式摘要和同步状态，因此重复点击或边缘请求重放不会静默新增重复行。钉钉不可用只影响本次外部同步，不改变已完成的本地确认，页面会明确提示并保留勾选以便重试。

多个已确认规格可在同一页勾选后点击“批量提交钉钉表”。浏览器按顺序复用单条计划的幂等同步、计划 ID 标记和写后回查链路，最多处理当前页 50 条；成功项自动取消勾选，失败项保留勾选并显示首个错误，便于修正人员映射、权限或单选项后只重试失败记录。该操作只更新钉钉“备货管理”表，不等同于发送群消息；群消息仍要求采购和工厂完整，并经过预览与再次确认。

备货计划群发只允许选择已确认、且对应采购和工厂均完整的计划。库存域固定使用“志高助手”发送到“志高/特睿思备货计划群”，与新品周报的通知目标相互独立；预览与正式发送都会动态精确核验目标群、机器人及入群关系，并把采购姓名解析为唯一钉钉用户 ID。正式消息先 @ 对应采购，再按工厂使用“**▸ 工厂（N 条）**”分组，每行使用“货品编码 货品名称，× 数量台”；供应商分组和货品行通过 Markdown 硬换行稳定展示。确认发送前页面完整展示目标和正文；正式发送前会再次同步所选多维表记录。群消息按 DWS `im.batch-write.v1` 逐目标账本确认成功，并保存去重账本；已成功、发送中或结果不明确的同一批内容不会自动重复发送，账本只保存回执摘要，不保存原始外部回执或凭据。

同步适配器复用当前 Windows 用户已安装的 DWS 与其钉钉 OAuth profile；仓库配置只保存稳定的组织/用户 profile 标识、Base/Table/Field ID 和字段契约，不保存 access token、密码或应用密钥。部署前须以同一 Windows 用户完成 DWS 登录并对目标 AI 表格保有写权限；授权失效时按钮明确失败，不回退到群机器人 Webhook、浏览器自动化或其他账号。

同步异常处理增强（已于 2026-09-07 在本机受控发布）：DWS 非零退出先检查标准输出和标准错误中的已知错误信号，再解析成功响应。授权失效、访问权限不足、限流、确认门禁和网络异常使用固定的可操作提示；未知失败不误报为授权失效。页面在计划状态列持续展示已保存的失败原因，日志仅记录命令阶段、错误类别和退出码，不保存原始 CLI 输出、账号或令牌。查询既有记录缺少记录列表、返回无标识记录或达到分页边界时停止提交，不能按“没有记录”继续创建；写入超时不自动重试，人工核对后重试同一计划，由原标记查询核对已有记录。

本次采用源码 `a1eff0bf1fd29db7541d775b3030aa19fb909c59`，Worker release 为 `20260907T093433Z-45b22a6f2cfced84`，Django 部署清单 SHA-256 为 `6a5a84fd8aa7570e37b86172ee22b9e8aa89de2f8ed39df2a5376c32dfd77b11`。发布前通过独立 PostgreSQL 镜像、23 服务冷启动和重启、迁移检查及 Worker API 验证；发布后原失败计划重试成功，本地状态为 `synced`，DWS 独立回查唯一记录及字段一致。发布前后备份已验证，发布后备份独立恢复摘要一致；本次无需数据库迁移。脱敏证据见 [本机发布记录](evidence/inventory-dingtalk-release-20260907.json)。

遇到“登录授权已失效”，管理员应在运行库存服务的同一 Windows 用户下，按配置中的精确 profile 完成 DWS 重新授权，并只读核验目标表；不能只凭 profile 列表的 active 状态判定同步已恢复。随后重试原计划并验证钉钉唯一记录和本地 synced 状态，不重新创建本地计划。重新授权本身不发送记录或群消息。

## 7. 京东入仓监控边界

当前版本提供的是可复核的第一阶段监控：

- 只统计京东 RDC/DC 与受控规则识别的京东区域平台仓。
- 7/30/90 日出库只统计正向销量；退款不计为出库。
- 计算周转天数为 `当前可用库存 / 近 30 日平均正向出库`。
- 库存货值使用系统固定成本；不是京东供应价或结算价。
- 供应商优先来自吉客云分仓库存的“规格默认供应商”，老快照缺失时回退 ERP 货品档案，页面明确标记来源；两者都缺失时标记待补映射。

京东原生库存、原生周转、供应价/结算价尚未接入，因此系统不得输出“与京东完全一致”、原生差异、残差 SKU 或对账通过结论。后续接入原生数据后，应保留原始指标与系统计算指标两套口径再做对比。

## 8. 接口与验证

### 广东入仓监控

待受控发布：手动设为“库存健康”的型号，在后续成功发布的当前库存快照中，精确广东仓实物库存 `on_hand_quantity` 较上一当前快照增加或减少时，自动清空手工风险及原因，恢复“系统自动判定”，同时结束该型号既有备货健康跟进周期，防止旧计划继续覆盖系统风险。其他型号设置、备货数量、计划更新时间不变；后续新增备货或增加备货量仍可开启新跟进周期。实物数量未变、仅可用/在途变化、重复导入、历史补传和其他仓/型号不触发；缺失记录不视为零，重新出现先建立可比较基线。非健康的人工风险保持原规则。重置与库存发布、revision 和变更前后审计同事务完成，失败整体回滚；无需新增迁移，现有手动健康设置在下一次可比较的库存变化时适用。

2026-09-11 已在本机生产采用备货健康规则（库存迁移 `0010_replenishment_health`）：同规格、精确“广东仓”的最新非取消备货计划新增正数备货量或增加数量后，风险分布、型号明细和原因栏统一标为“健康 / 备货跟进”。沿用当前备货数量来源，草稿、已确认和已完成计划均可触发；取消或数量归零不享受该状态。跟进期间优先显示备货原因，系统原判及人工设置仍保留；首次增加后恢复原有系统/人工风险判定。发布与备份恢复见 [生产证据](evidence/guangdong-replenishment-health-production-20260911.json)。

“首次增加”指成功发布的当前库存快照中，实物库存 `on_hand_quantity` 较上一份已观察到的快照增加；先降后升也算首次增加。仅可用数量解锁、在途增加、其他仓/规格、历史补传、重复批次和缺失记录不会解除；未取得首次基线时先建立基线。状态在库存发布事务中持久化，解除后库存再次下降不会被旧计划重新标健康；再次增加备货量或新增计划才开启新周期，确认状态和备注修改不重置。迁移将存量非取消、正数广东备货计划以迁移当时的库存建立基线，不追认历史到货，也不修改备货数量或计划更新时间。

库存总览对全部精确“广东仓”记录同步同一套供应商周期、启用型号覆盖、30日正向出库、180天低周转边界、数据缺失/过期、人工风险及备货跟进规则，不受监控清单成员范围限制。总览与广东监控统一展示六类状态：无库存可用、紧急补货、补货预警、积压风险、低周转、库存健康；广东风险键继续保持既有兼容值，展示和总览映射使用上述统一名称。

广东监控只读取最新库存快照中仓名精确为“广东仓”的记录，不包含广东其他站点子仓。清单以吉客云货品编码为唯一身份，初始为空；只有启用型号进入风险分布。缺库存记录保留为缺数，不推定零库存。供应商优先使用该库存行的规格默认供应商，再回退 ERP 档案。

在“监控清单”下载 Excel 模板并填写货品编码、监控状态（启用/暂停）、备注；编码列预设文本格式，避免丢失前导零。支持上传单工作表 XLSX、直接粘贴多行或搜索主数据选定编码。每次及清单总数最多5000个型号，文件最大4MiB；拒绝公式、未知编码、冲突重复行、额外字段及超限文件。预览后确认仅新增或更新本次编码，其他型号不变；暂停项可在清单中查看和恢复。导入以规范化业务内容幂等处理，重复内容不重复修改 revision；并发版本冲突要求重新预览。操作与导入审计保留摘要、来源、操作人及变更前后内容。

“供应商备货周期”维护完整下单至可用入库的生产周期（1–365天）和安全天数（0–365天，默认7天），同供应商共用，只作用于广东监控。每个监控型号可在明细中单独覆盖生产周期、安全天数、运营负责人、采购负责人、风险及原因说明；型号周期与安全天数必须同时填写或同时留空，手工风险与原因说明也必须同时填写或同时留空。未设置型号周期时继承当前供应商周期/安全天数，供应商发生变化后自动重新匹配；未设置型号负责人时继承该型号广东仓最新一笔未取消备货计划中的负责人；风险与原因均留空时恢复系统判定。缺失周期设置不能判断为健康。

日均需求为广东仓近30日正向出库除以30，库存周转为非负可用库存除以日均需求；在途没有可靠到货日，不抵减缺货预警。可用库存小于等于0属于无库存可用；库存周转严格大于180天属于低周转，等于180天仍落在健康边界；库存周转不超过生产周期属于紧急补货；大于生产周期且小于“生产周期+安全天数”属于补货预警；达到“生产周期+安全天数”且不超过180天属于库存健康。未匹配销量、窗口不足、近30日零出库或周期缺失时归积压风险并保留具体数据原因，不能把无法计算伪装成180天周转。最晚下单日期按库存快照日加上向下取整的“库存周转−生产周期−安全天数”估算，并以本机服务器上海业务日作为下限，不返回过去日期；快照超过3天时提示待更新且不标健康。

健康分布卡片按货品搜索、品牌、品类和供应商范围展示型号数及型号占比，不再展示卡片底部的库存件数、货值及其占比；点击风险分类筛选明细。广东监控顶部的广东仓可用库存、在途库存和已覆盖库存货值三张汇总卡不再展示，API、统计说明和导出健康分布仍保留原统计口径。型号明细展示商品、规格编码、供应商、运营负责人、采购负责人、库存/在途、7/15/30日出库、销售周转/库龄、生产周期/安全天数、最晚下单、备货数量/最新下单时间、风险原因和型号编辑入口，不再展示成本/货值列。列表同时标明周期、负责人和风险的实际来源；手工风险生效时仍展示系统原判及其依据，避免覆盖后丢失自动计算证据。库龄只取最新吉客云库龄表格中仓名精确为“广东仓”的行；备货数量和最新下单日期取“备货计划”中同货品、同广东仓的最新非取消计划，运营负责人和采购负责人在没有型号覆盖值时取该计划。清单导出可回导；监控导出包含同口径的全部筛选明细、统计说明及健康分布，并绑定同一库存/销售/ERP/配置版本；版本变化时拒绝交付混合文件。

新增 `/api/inventory/guangdong-monitor` 及 `/watchlist`、`/products`、`/preview`、`/import`、`/suppliers`、`/export`、`/template`、`/file-preview` 子入口。查看和导出允许 viewer/analyst/operator/admin，维护允许 operator/admin，受限scope账号保持拒绝。中央AI工具 `get_inventory_page_data` 增加 `view=guangdong`，只读且最多20行。没有定时外部通知或自动创建备货计划。

库存迁移 `0007_guangdong_monitor` 新增三张广东清单、供应商周期和审计表，`0008_guangdong_item_overrides` 在监控型号表增加周期、安全天数及负责人覆盖字段和成对约束，`0009_guangdong_item_risk_overrides` 增加风险及原因说明覆盖字段和成对约束；reader只读业务配置，writer按最小权限维护配置并追加审计。部署脚本、readiness和日常备份清单同步包含现有表。验证在独立工作树执行 `python tools/verify-guangdong-monitor.py`（独立55458端口PostgreSQL、合成数据、最小权限、备份恢复）及 `node tools/verify-guangdong-ui.mjs`（独立3108端口、浏览器交互、模拟API）。正式迁移与发布仍使用受控发布流程，不由开发验证自动执行。

### 广东监控本机生产采用（2026-09-08）

经用户明确授权，广东监控已随提交 `f39d8d6887a57812ac36f41bc7ada0e7b2141ab7` 完成本机受控发布。Worker effective release 为 `20260908T103235Z-6e9bfb8baea02174`；Django 部署清单 SHA-256 为 `d86d6a007bfb95ca36cd0b0dcd0d9529ff06e8dca7cc78defa475d5530b76c0b`。已应用 `inventory.0007_guangdong_monitor`，只为库存 reader/writer 新增三张广东表所需的10条权限，其他角色、BI、列权限及数据库权限保持一致。

入口为“库存管理 → 广东入仓监控”。上线回查时清单、供应商周期及审计表均为0条，没有导入测试货品；使用时先添加吉客云货品编码，再设置对应供应商完整到仓周期和缓冲天数。生产浏览器、两类完整 Excel 导出、版本冲突拒绝及原有京东监控回归均已通过。

发布使用独立工作树和生产备份的独立 PostgreSQL 镜像；镜像完成新增迁移、最小权限配置、23服务启动/重启及20项接口检查。发布前后正式备份均保留，恢复仅遵循现行 PostgreSQL 备份恢复或兼容代码前向修复，不恢复旧 D1 路径。具体计划摘要、服务状态、回查和恢复证据见 [广东监控生产发布证据](evidence/guangdong-monitor-production-20260908.json)。

### 既有接口

- `GET /api/inventory/overview`：库存总览与备货计划投影。
- `GET /api/inventory/age-analysis`：库龄指标、筛选、分页与精细分布。
- `GET /api/inventory/inbound-monitor`：京东入仓监控。
- `POST /api/inventory/work-items`：生成采购或清理执行事项。
- `POST/PATCH /api/inventory/replenishment`：新增或更新备货计划。
- `GET /api/inventory/replenishment/import`：下载备货计划 Excel 导入模板。
- `POST /api/inventory/replenishment/import`：按最新库存快照原子导入 1–200 条备货计划；导入不自动写钉钉。
- `POST /api/inventory/replenishment/dingtalk`：把一条已确认计划幂等同步到钉钉“备货管理”。
- `POST /api/inventory/replenishment/dingtalk/group`：预览或确认发送 1–50 条已确认备货计划的钉钉群消息。

所有列表均在服务端筛选、分页并返回总数；页面切换筛选时取消旧请求，防止迟到响应覆盖新状态。CSV 只导出当前页并使用 UTF-8 BOM，同时防护公式注入。页面“问问小特”只带入有界页面上下文，不自动发送、不绕过中央 AI 工具和权限。

Django 终态下，分仓库存、库龄、备货计划、运营设置、原始分片、导入审计和 revision 全部属于库存域；销售与 ERP 只作为最小权限只读依赖。商品经营库存投影、系统成本、AI 和全局搜索通过有界库存 consumer 读取，库存 reader/writer 不可用时失败关闭，不回查旧 D1/R2。自动补货默认关闭，迁移不会因旧设置缺失而静默开启。
