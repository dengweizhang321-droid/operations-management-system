# 市场 AI 标注调度与复核修复

本修复已于 2026-09-13 合入 main 并在本机正式采用，源码为 `2a1867c244fc1c8d3b4cb55675b8dd71d11044a7`，Worker/helper 为 `20260913T024434Z-bc351d47b7209e2b`。入口为“市场分析 → 系统和 AI 设置 → AI 标注与批量入库”。初次采用及运行回查后的前向修复过程见 [发布证据](evidence/market-annotation-reliability-production-20260913.json)。

## 已确认原因

2026-09-13 从本机同源公开 API 只读检查任务状态（运营 MCP 不可用，使用本机替代来源）：5 条计划的云端运行状态均为 running，其中一条总数 9,330、失败数 5,386，抽样失败原因为“账号或数据权限已变化”。这些是检查时的任务状态，不能视为实时数据覆盖或所有失败类型的全量统计。

- 旧后台使用固定的市场调度身份获取 AI 模型配置；AI 域对真实账号权限复验时拒绝该身份，错误被记成图片失败后仍继续领取。
- 定时器一次只识别一张图，设置并发没有转成实际派发；无指定任务的领取只检查最早任务，满载或无候选时阻塞其他计划。
- 复核资格依赖前端最近 50 个任务及最近 300 个 Prompt，与全部历史结果的跨任务查询不一致；大任务还没完全结束时，已完成图片也被禁用。筛选、全选和入库的服务端规则存在差异，批量入库缺少前端依赖的 500 条分页回执。

## 修复后的执行方式

1. Django 返回有界可运行计划及其真实创建人；后台先实时复验创建人权限，模型调用前再次复验。身份或模型配置失效立即暂停，显示可操作原因，不连续消耗其他图片的尝试次数。
2. 每轮公平选择最多 5 条计划，每条按当前可用槽分配；总计最多 50 个并发通道。权限与协调租约准备各自有请求时限，准备完成后开启最多 10 秒的新图领取窗口、最多 200 次领取，随后只等待已启动的请求完成。准备较慢不再消耗全部领取窗口，其余计划在后续定时唤醒中轮转。并发配置、冷却、协调租约和单图租约以 PostgreSQL 为权威，浏览器只负责控制和轮询。
3. 普通超时/网络错误降低并发并冷却对应通道；供应商限流在相同模型的计划间共同退避，其他模型继续运行。连续成功后逐步恢复配置并发。
4. 同任务内相同类目、口径、SKU、榜单维度和图片哈希的跨月候选共用一次模型结果，成功和扩散在同一事务完成。完成写入使用固定请求身份重试回执，不重新调用模型；云端租约过期且结果未知时整组隔离、暂停并要求人工核验，不能把未知结果当作未执行。
5. 单次模型 HTTP 最多 90 秒，模型请求还受本次租约剩余时间限制，预留结果落库时间。模型成功与数据库回执失败分开处理；回执丢失不得把成功结果改记为模型失败。
6. 过期租约清理先检查是否存在过期推理；没有时直接返回，避免在同模型锁内扫描整个候选积压。2026-09-13 正式只读等价查询发现，4,919 条待处理候选的空过期相关扫描耗时 6,922 ms，而过期存在性检查不足 1 ms。该数据仅证明查询瓶颈，不能推算模型提速倍数。

本机部署要求运营系统持续运行；关闭浏览器可继续，关闭电脑或停止系统后不能继续执行。提高并发不能补偿权限失效、模型限流或服务离线。

## 复核与批量入库

- 已识别完成的单条候选即可复核，不必等待整条大计划结束。服务端返回该候选的有效枚举及资格，历史任务列表截断不再影响可选性。
- 列表、全选和入库使用一致的类目、细分品类、识别来源及入库状态规则；不跨出当前筛选。无可选项、未加载完成或权限限制会显示原因。
- 全选前先保存当前页未保存的编辑；重复全选不反复增加候选版本。每次入库最多 500 条，通过 `hasMore` 继续处理，回执幂等且保留审计；任务仍有待识别项时继续运行。
- 并发领取、人工编辑和入库统一锁顺序，并保留候选版本、当前图片快照和正式价类型校验。

## 验证与采用

使用独立工作树和合成 PostgreSQL 集群，运行 `tools/market-annotation-postgres-rehearsal.py`，覆盖多计划、公平领取、配置动态变更、通道冷却、同模型限流、跨月去重、租约过期、提交回滚恢复、跨页筛选、重复选择及 500+1 入库。镜像同时验证当前市场 reader/writer 最小权限。

TypeScript 行为测试覆盖并发总量、任务公平分配、失权拒绝、调度预算、回执错误不再推理和选择资格；另运行市场现有测试、全量单元测试、lint、生产边界检查与隔离构建。未进行受控的真实模型吞吐量基准或供应商限流注入，不以合成模型延时或运行快照推算真实提速倍数。

开发验证使用独立合成数据，没有生产写入或付费模型调用。用户授权“发布上线”后，已受控采用 Django 与 Worker 并重启相关系统服务；既有启用计划自然恢复包含真实模型调用和结果写入。此次无数据库结构迁移、n8n 重启、人工新增模型试调用、复核勾选或批量入库；旧失败记录保留，不自动重置封顶项，不重放未知结果。

最终源码验证：真实 PostgreSQL 市场测试 52/52、定向测试 76/76、HTML 渲染测试 20/20、运行器与派发复核测试 13/13 通过；全量单元测试共 2,158 项，其中 2,138 通过、0 失败、20 跳过。lint 为 0 错误、11 项其他文件的现有警告，隔离构建和 430 模块后端边界检查通过。整仓 `tsc --noEmit` 仍报告未触及的历史测试/工具类型错误，未标记为通过。构建与读取 `dist` 的测试必须顺序运行：中途并发执行曾导致产物被替换而出现 ENOENT，构建稳定后已完整重跑通过。初版候选结果保留在 [开发验证记录](evidence/market-annotation-reliability-validation-20260913.json)，最终结果以 [发布证据](evidence/market-annotation-reliability-production-20260913.json) 为准。

正式回查确认系统为 Running / Ready / exact_release，首页及 17 个实际 JS/CSS 资源与不可变 release 字节一致，钉钉接收器自动恢复 connected。发布前备份已在独立 PostgreSQL 集群恢复且摘要一致；发布后备份及独立复验通过，未对最终发布后备份再次恢复。

2026-09-13 10:59:21 的只读快照显示，5 条原有计划相较本次增量停机前合计新增 255 条完成结果，各计划均推进，观察窗口未新增失败；该计数包含同图跨月扩散，不等同于 255 次模型调用。正式复核页面的“全选筛选结果”已启用，UI 当时显示跨页 1,789 条；这是动态水位，验证未点击勾选或触发入库。

## 单独记录的列表超时

发布回查另发现“主数据与价格”默认列表 `GET /api/market/master?view=database_primary&page=1&pageSize=30` 在约 30,078 ms 后返回 503；同次 `system_kpis` 查询在 515 ms 内返回 200。该列表调用链相关文件与发布前 main 一致，但没有在旧运行版本复现，不能断言问题早已存在。本次标注修复未解决该列表超时，也不据此宣称市场所有页面性能已验收。

## 标注页面读取失败后的恢复

2026-09-13 后续修复已在本机受控上线（源码 `8f93ec2f5beea4475656da514f4d60546e73c5e9`），处理标注页面红色“Django 市场服务暂时不可用”提示在读取恢复后仍残留的问题。旧轮询将进度/复核读取错误放进共用操作错误，而成功轮询没有清除它；进度请求也没有前端超时或任务切换取消。诊断时，本机同源只读 `workspace_fast`、`progress`、`review`、`candidate_counts` 均返回 200，分别约 1,133、340、534、1,001 ms。截图所对应的最初 503 未复现，不能据此认定原上游失败原因或宣称服务容量问题已经修复。

当前实现把进度和复核读取分成独立错误通道，仅同通道最新请求成功后清除错误，仍显示上次成功数据并提供只读重试按钮。请求及响应体读取限定 30 秒，任务/筛选切换和组件卸载取消旧请求；迟到成功、迟到失败均不得更新当前数据或错误。复核读取失败时，运行中轮询在下一轮继续尝试，即使完成计数未变化。后台刷新不得清除保存/入库错误，也不得重放写请求、任务创建或模型调用。

本次只涉及前端读取及展示，不改变后台队列、权限、数据库或服务配置。“主数据与价格”列表另在本次只读检查中约 30,047 ms 返回 503，仍是独立未解决问题。验证与交付状态见 [候选验证记录](evidence/market-annotation-refresh-recovery-validation-20260913.json)。

生产采用版本为 `20260913T143708Z-cd14607ae3bd71e3`，包含 AI 回复正文、标题、表格和底部文字的黑色样式。正式 Running / Ready / exact_release、16 个实际 JS/CSS 资源字节、标注只读接口与启动绑定均通过；发布前备份独立恢复及发布后备份复验通过。无数据库迁移、Django/n8n 重启或人工模型调用。完整采用证据见 [发布记录](evidence/market-annotation-refresh-production-20260913.json)。

## 入库更新已有标注的日期序列化

2026-09-13 用户再次报告 AI 标注页面写操作后出现通用“Django 市场服务暂时不可用”。本机市场 writer 日志确认 21:32 的三次及 22:57:58 的一次 `commit_selected` 均在 `_commit` 保存 `MarketAnnotationCommitReceipt.before_json` 时触发 `TypeError: Object of type datetime is not JSON serializable`。该异常被公开适配层转换成 503。此前读取恢复修复没有覆盖此入库异常；读取接口健康也不能作为写操作验收。

当同一商品、同一图片已有正式标注，或者同图跨月份候选在一批内依次更新同一标注时，旧值审计快照包含 ORM 原生日期。修复在入库事务内将日期转换为带时区、保留微秒精度的 ISO 字符串，其余字段、空值、金额和版本保持原类型，不删除审计字段、不放宽 JSON 存储、不新增迁移。新候选首次入库继续保存空的旧值快照。

独立 PostgreSQL 用签名 API 覆盖已有标注、同图跨月、两层幂等回放、第二条审计写入失败后的整批回滚及同请求重试、非管理员拒绝；同时使用与正式环境相同的市场 reader/writer 授权契约验证领取、完成、复核和覆盖已有标注的入库。原代码已在同一合成场景复现 500，修复后市场 55 项测试及最小权限验证通过。候选验证不会对生产执行入库，也不会自动重放用户失败的写请求。

本次正式日志另有 HTTP 连接数上限告警；它没有出现在上述入库异常堆栈中，不能据此替代已确认的日期序列化原因。该容量现象与主数据列表超时单独保留，不在本修复中改变服务并发配置。

上述修复已受控采用，源码 `0b96b86cd23f44f40ab6d557fedc0819a4069d1c`，Django manifest SHA `bb1d44779115d8b0fdc5b8caceae9b3b14a7f9e725c2039bb4bd2a650927354d`，Worker/helper 保持 `20260913T143708Z-cd14607ae3bd71e3`。受控整栈停启、正式 Running/Ready/exact_release、源码字节、只读接口、启动绑定、发布前备份独立恢复和发布后备份复验通过；未执行生产入库验收或数据库迁移。验证细节见 [候选记录](evidence/market-annotation-commit-json-validation-20260913.json) 和 [生产记录](evidence/market-annotation-commit-json-production-20260913.json)。

切换前 23:16:25 的只读快照为 4 条计划均无有效推理租约、helper 空闲；此快照未锁住之后的定时派发。受控停止期间又有领取，恢复后 45 条候选被自动标为结果未知，4 条计划暂停。已通过生产 market reader 的只读事务核验隔离状态和 `attempt_count>=3`，没有重放这些图片、重置失败项或人工补写结果；恢复其余未处理图片已单独向用户请求确认。后续发布不能把一次空闲快照视为排空证明，应先通过受控机制停止新派发，再等待已有推理结束后进入停机。

用户随后明确要求“恢复其余图 片”。23:40:37 经正式公开 API 对上述 4 条原计划逐条执行 `set_cloud_run_state=running`，4 次均返回 200；23:41:17 回查均为 running，完成候选分别新增 13、20、24、12 条，已有定时调度自然继续推理和写入。恢复前及自然推进后只读核验，全部 74 条历史隔离候选的完整行摘要一致；已完成的第 5 条计划云端记录未变。没有重置隔离项、创建新计划、人工调用模型或入库，也没有服务重启。完成候选增量可包含跨月复用，不等于付费调用次数。见 [恢复核验](evidence/market-annotation-remaining-resume-20260913.json)。

## 图片已变化时的批量入库

2026-09-13 23:48 的生产只读核对确认，5 条计划已勾选候选中有 184 条与当前图片哈希不同，分别为净水 15、绞切肉 25、开水器 60、洗碗机 19、炒菜机 65 条；没有缺失月份快照或榜单身份。Django 复核列表与跨页选择原先只检查状态、任务 Prompt 和字典，最终入库才检查图片，一个失效项因此使整批回滚。此问题独立于前述日期 JSON 异常。

复核行、数量、跨页选择及最终入库现在共用精确类目、scope、SKU/SPU 维度、SKU、月份和当前图片哈希校验；ready 缓存有非空哈希时优先使用缓存，否则使用快照哈希。失效候选不可在页面勾选或编辑，原已选记录单独提示并保留。入库先筛出最多 500 条有效候选，持有快照行锁再次校验；选择后又失效的候选也跳过，其他有效项继续。回执保存 `skippedStaleCount`，全部失效时有界返回零入库，重复请求不会循环或重复发布。

前端不再调用 Django 尚未实现的 `rebuild_stale_selected` / `rebuild_stale_item`。本次入库保留失效候选及其人工复核记录，不自动改写成新图、不重放模型、不触及结果未知的隔离候选。旧图候选须基于当前图片重新识别并人工复核；它们不再阻止其余有效项入库。

镜像 PostgreSQL 覆盖混合候选、全部失效、月份/身份缺失、缓存哈希优先级、写入时图片再次变化、501 条跨批入库、幂等回执及整批失败回滚。真实 React 浏览器验收使用隔离合成 HTTP 响应，验证旧图禁选、跨页选择、编辑保存、连续两批入库和跳过提示；数据库写流程另由签名 API 和真实 PostgreSQL reader/writer 角色验证，未以合成浏览器响应替代后端验收。

2026-09-14 已受控采用源码 `75c73f748de00eb2dfec9fbe45ecdab92fefdfac`，Worker/helper 为 `20260913T162304Z-3733181f1543a083`，Django manifest SHA 为 `c5651c760123f4652301b3bceaa55b2e0b6ee1bb6cfd3f980bd96ca8c2943411`。正式新浏览器只读验收显示 184 条旧图排除提示，批量入库按钮数量为 16,554，接口、实际 JS 字节和后端源码与发布包一致，整栈 Running/Ready/exact_release。正式验收没有代用户入库；写流程以 61 项隔离 PostgreSQL 测试验证，全量 Node 2147 项通过、20 跳过。

本次先通过正式 API 暂停 4 条原运行计划，00:14:29 与 00:14:49 两次确认包括过期领取在内的在途数为零、helper 空闲，再执行受控停止。恢复后 00:32:52 逐条恢复上述计划；00:34:09 回查自然新增完成 24、23、12、12 条，合计 71 条。全部 74 条既有隔离记录的完整行摘要、已完成第 5 条云端计划记录保持一致。没有重置隔离项、人工模型调用或 n8n 重启。

发布前备份已在独立 55484 端口完整恢复并核对内容摘要，发布后备份及归档摘要复验通过。Django staging 的两次 local R2 自检在上传完成后因 CLI 未退出而超时，均发生在应用原子替换之前；沿用项目已记录的进程级 `WRANGLER_HIDE_BANNER=true` 后，原 operator 的完整 put/get/hash/delete/缺失检查通过，没有扩大时间、修改依赖或跳过门禁。完整证据见 [候选检查](evidence/market-stale-candidates-validation-20260914.json) 与 [生产核验](evidence/market-stale-candidates-production-20260914.json)。
