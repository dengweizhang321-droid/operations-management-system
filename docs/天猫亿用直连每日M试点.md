# 亿用 P/M 直连与每日 M 试点

## 批准范围与当前状态

用户批准先试点亿用，同时把亿用 M 从每三天调整为每天。丽力、拓丰、炊之王、马思图不切换，M 仍每三天；亿玖现行直连及每日 M 不变。

本分支准备代码、注册表和默认停用的候选模板；**未发布、未迁移本机节奏、未通过真实 n8n execution 验收**。注册表的目标 intervalDays=1 不代表当前运行服务已经采用；切换完成前不得通知“已启用”或“已导入”。

| 项目 | 目标 |
| --- | --- |
| 工作流 ID | 原 `TmallYiyongDaily2026`，禁止新建第二条 ID |
| 候选模板 | `automation/n8n/tmall-yiyong-direct-pm-candidate.workflow.json` |
| 店铺与日调度 | `tmall-yiyong`，Asia/Shanghai，每天 14:20 |
| 顺序 | 原子认领 → A → B → C → P → M |
| P/M 协议头 | `yiyong-direct-pm-v1`，不得使用亿玖协议头 |
| M 节奏 | 持久 intervalDays=1，实际成功上海日 +1，失败不推进 |

直连复用 [亿玖接口契约](天猫亿玖P-M直连候选工作流.md) 的单日四场景商品+计划报表、唯一 taskId、固定 MTOP 分批导出、文件验证、单次导入和精确回查。所有浏览器、凭据、taskId、活动清单与批次仍绑定亿用自己的注册项。直连不绕过平台登录、安全验证或风控。

## 采用前不可跳过的门禁

1. 核验该店无在途 execution，helper 空闲，其他任务无冲突；保全原 ID 的 live/已发布版本和本店节奏状态。
2. 检查原 UI P 与 M 活动清单。旧 M 任何活动槽未清退时直连都拒绝接管；已确认任务不能以超时为由自动作废。需先安全完成原任务，或由用户明确确认该精确旧任务作废，再使用既有受控归档入口；不删除文件、不倒退阶段。旧 P 的未决/已提交/下载/导入动作同样禁止跨协议接管。
3. 通过现行受控发布流程采用包含亿用协议的 helper；不可修改不可变 release，不在运行目录原地构建。需要本机服务维护时先取得当前明确授权。不得先发布 n8n 让它命中旧 helper 的店铺拒绝门禁。
4. 无未决 M 后，用 `tmall-product-master-cadence-admin.ts` 的 `migrate-interval` 入口迁移 3→1；实时读取并精确确认最后成功日。保留 lastSuccessDate/lastSnapshotDate，nextDueDate 按最后成功日 +1 重算，不伪造当天成功。旧间隔/成功日不符、重复迁移或未决活动槽均停止，不删除状态文件重建。
5. 将候选作为**同一 ID**的新版本受控发布，核验当前及已发布的 nodes/connections/settings，确认六店每店只有一条 active 日调度。`tmall-yiyong-sycm-cookie-daily.workflow.json` 是生成基线，不与候选并行激活。
6. 仅通过 n8n 创建新完整 execution；不单独执行 P/M，不直接调用 helper 业务接口。验证码/登录/任务歧义时失败关闭，保留证据。
7. 验收 C、P 的文件/店铺/日期/哈希/精确批次/覆盖，以及 M 的分批商品集合、合并文件、精确导入批次和次日 nextDueDate；确认亿用受控 Chromium 已关闭且 helper 释放。节点绿色或下载成功不是导入完成。
8. 真实验收通过后再更新 README/AGENTS、运维 skill/手册与现有监控自动化的“亿用每日 M + 候选为现行模板”状态。采用前保持其生产现状描述，不能提前声称实测成功。

## 验证

聚焦测试覆盖独立协议白名单、跨店/错版本/多值头拒绝、A/B/C 与调度不变、确定性生成、每日 cadence 迁移/重试/跨日/不推进失败、原已确认 M 拦截且证据不变，以及 not_due 无业务动作并关闭浏览器。需同时执行全量单元测试、lint 和 diff 检查；不得在真实业务存储中运行测试。

开发验证：34 项聚焦测试通过；全量单元测试 1854 通过、0 失败、23 跳过（总计 1877）；lint 0 错误，9 个既有警告，改动文件 lint 无警告；生产依赖边界检查 366 模块、0 违规；隔离 helper 打包及 `git diff --check` 通过。这些证据不替代真实 n8n/落库验收。

新策略一旦发生业务动作，不得切回旧版重建任务；保留新版本活动清单，以前向修复和新完整 n8n execution 安全续接。
