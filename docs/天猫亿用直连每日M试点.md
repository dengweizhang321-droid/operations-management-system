# 亿用 P/M 直连与每日 M 试点

## 批准范围与当前状态

用户批准先试点亿用，同时把亿用 M 从每三天调整为每天。丽力、拓丰、炊之王、马思图不切换，M 仍每三天；亿玖现行直连及每日 M 不变。

2026-09-06 已按用户明确授权归档旧管家活动清单、通过受控 successor 发布配套 helper、迁移本机节奏 3→1，并在同一 n8n ID 发布直连版本。仓库模板仍固定 `active=false`，本机发布状态单独核验；完整试跑 execution 840 已成功，文件、精确批次、覆盖与收尾证据见下文。这是一次真实试跑通过，不代表已证明长期无故障。

| 项目 | 目标 |
| --- | --- |
| 工作流 ID | 原 `TmallYiyongDaily2026`，禁止新建第二条 ID |
| 候选模板 | `automation/n8n/tmall-yiyong-direct-pm-candidate.workflow.json` |
| 店铺与日调度 | `tmall-yiyong`，Asia/Shanghai，每天 11:50 |
| 顺序 | 原子认领 → A → B → C → P → M |
| P/M 协议头 | `yiyong-direct-pm-v1`，不得使用亿玖协议头 |
| M 节奏 | 持久 intervalDays=1，实际成功上海日 +1，失败不推进 |

直连复用 [亿玖接口契约](天猫亿玖P-M直连候选工作流.md) 的单日四场景商品+计划报表、唯一 taskId、固定 MTOP 分批导出、文件验证、单次导入和精确回查。所有浏览器、凭据、taskId、活动清单与批次仍绑定亿用自己的注册项。直连不绕过平台登录、安全验证或风控。

## 采用前不可跳过的门禁

1. 核验该店无在途 execution，helper 空闲，其他任务无冲突；保全原 ID 的 live/已发布版本和本店节奏状态。
2. 检查原 UI P 与 M 活动清单。旧 M 任何活动槽未清退时直连都拒绝接管；已确认任务不能以超时为由自动作废。需先安全完成原任务，或由用户明确确认该精确旧任务作废，再使用既有受控归档入口；不删除文件、不倒退阶段。旧 P 的未决/已提交/下载/导入动作同样禁止跨协议接管。
3. 通过现行受控发布流程采用包含亿用协议的 helper；不可修改不可变 release，不在运行目录原地构建。需要本机服务维护时先取得当前明确授权。不得先发布 n8n 让它命中旧 helper 的店铺拒绝门禁。
4. 无未决 M 后，用 `tmall-product-master-cadence-admin.ts` 的 `migrate-interval` 入口迁移 3→1；实时读取并精确确认最后成功日。保留 lastSuccessDate/lastSnapshotDate，nextDueDate 按最后成功日 +1 重算，不伪造当天成功。旧间隔/成功日不符、重复迁移或未决活动槽均停止，不删除状态文件重建。
5. 将候选作为**同一 ID**的新版本受控发布，核验当前及已发布的 nodes/connections/settings，确认六店每店只有一条 active 日调度。`tmall-yiyong-sycm-cookie-daily.workflow.json` 是生成基线，不与候选并行激活。
6. 仅通过 n8n 创建新完整 execution；不单独执行 P/M，不直接调用 helper 业务接口。验证码/登录/任务歧义时失败关闭，保留证据。
7. 验收 C、P 的文件/店铺/日期/哈希/精确批次/覆盖，以及 M 的分批商品集合、合并文件、精确导入批次和次日 nextDueDate；确认亿用受控 Chromium 已关闭且 helper 释放。节点绿色或下载成功不是导入完成。
8. 真实验收通过后再更新 README/AGENTS、运维 skill/手册与现有监控自动化的“亿用每日 M + 候选为现行模板”状态。采用前保持其生产现状描述，不能提前声称实测成功。

## 验证

聚焦测试覆盖独立协议白名单、跨店/错版本/多值头拒绝、A/B/C 与调度不变、确定性生成、每日 cadence 迁移/重试/跨日/不推进失败、原已确认 M 拦截且证据不变，以及 not_due 无业务动作并关闭浏览器。需同时执行全量单元测试、lint 和 diff 检查；不得在真实业务存储中运行测试。

开发验证：34 项聚焦测试通过；全量单元测试 1854 通过、0 失败、23 跳过（总计 1877）；lint 0 错误，9 个既有警告，改动文件 lint 无警告；生产依赖边界检查 366 模块、0 违规；隔离 helper 打包及 `git diff --check` 通过。这些证据不替代真实 n8n/落库验收。

新策略一旦发生业务动作，不得切回旧版重建任务；保留新版本活动清单，以前向修复和新完整 n8n execution 安全续接。

## 2026-09-06 本机采用与真实验收

- 旧 M：仅将用户授权的 2026-09-04 快照、run `5aeeaeaf-4a35-4a91-9d59-8b6090f7b7c4` 的 `export_confirmed` 活动清单保全后归档作废；未删除原证据、未倒退阶段、未取消平台任务，也未重置任何已导入事实。原清单 SHA-256 为 `f3ef302e45543ea39c480fa02d88a1411586f6098522edc477ef0b621d00589b`。
- 采用代码 `3308226c`；本机受控 release `20260906T071555Z-f197eb1e1b92e1db`，manifest SHA-256 `d5fb74660c564e0a95c336ab544a3fe4210c8d557c873bc8a5463736ba92644c`。仅维护所需 Worker/helper，未重启 Django 服务、未部署远程生产或迁移业务库。
- 同一 n8n ID 的 live/current 与 published version 均为 `33c05b8d-3e38-458c-bbea-b0fb1fe6001d`，nodes/connections/settings 与直连模板一致；六店各一条 active 日调度。通过 UI 卸载旧发布、官方 CLI 导入、UI 发布注册新版本，不需要重启 n8n。
- 完整 n8n execution **840**（manual）：上海时间 **15:24:57.842–15:35:18.400**，耗时 **10 分 20.558 秒**，A/B/C/P/M 全部 success，未单独触发辅助业务接口。P 节点约 12.893 秒，M 节点约 600.245 秒；M 用时主要为 10 批平台导出等待，不是未释放的卡死。

| 事项 | 日期 | 精确验收结果 |
| --- | --- | --- |
| 商品日 C | 2026-09-05 | 131 行，复用同内容的已完成批次（摘要 `a8f47396f1b0`），不是新增重复事实；文件哈希、日期与落库覆盖通过；1 条商品未匹配旧主数据告警 |
| 推广日 P | 2026-09-05 | imported，64 行，批次摘要 `b1fe4ca09355`，告警 0；唯一任务、文件、SHA-256、精确批次 completed 与同日覆盖一致 |
| 货品 M | 2026-09-06 | due_completed / imported，191 个商品分 10 批，合并 2986 行、2799 个非空唯一 SKU；批次摘要 `15f958146777` completed，店铺/快照/行数/文件哈希与回查一致 |

M 的 10 个分批文件及合并文件 SHA-256 均独立复验通过；合并文件 SHA-256 为 `5b2c23f3501c5f45ee6136f11474b4839ff8d045ec2bb73788ee3c2fc02a23ac`。M 有 3 类源数据告警：187 行缺 SKU ID、921 行缺 SKU 商家编码、1100 行商家编码重复；按既有隔离键/业务映射契约处理，不能把它们声称为已补齐字段。C 的既有未匹配告警也不据本次 M 成功自动宣布消除。

本次真实 M 成功后，持久 `intervalDays=1`、`lastSuccessDate=2026-09-06`、`lastSnapshotDate=2026-09-06`、`nextDueDate=2026-09-07`。本店受控浏览器端口已不监听，helper 为 ready、busy=false、无 owner，Worker 的 Django/PostgreSQL readiness 正常。C/P/M 三事项分别向核验后的“志高助手 → 测试群聊”投递完成通知；现有 `11-n8n` 监控保留原计划/通知策略/目标任务，仅同步亿用直连现行模板和每日 M，其他店铺不变。
