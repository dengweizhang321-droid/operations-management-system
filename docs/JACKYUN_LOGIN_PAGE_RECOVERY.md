# 吉客云登录页面就绪与失败恢复

2026-09-23：已按用户“按照建议执行”的授权正式采用，4102 已核验关闭，旧等待重试 4117 已在 n8n 界面取消。登录页面检查、错误分类和这一类零业务效果的 operator 关闭规则均已采用；五表业务成功仍待原自然执行验收。

## 故障边界

4098 已正常关闭，02:10 的新执行 4102 到达 B 后再次报 `API_LOGIN_PAGE_NOT_UNIQUE`，留下空导出计划。后续执行在 A 因 4102 未闭合而停止；旧共享错误分类遗漏这两条错误，造成无效小时重试。08:35 左右另有应用服务异常；本轮调用原总控 Start 时，已回读 Running/Ready/exact_release，未重复启动已就绪组件。

旧代码只等 Chrome 调试接口可用，然后立即要求候选页面数量为 1，没有等待页面集合稳定，也没有保存当时数量。不能据 `Crashed` profile 标记推断一定出现多个页面。本轮在核验进程、用户、profile、端口并持有原全局锁后，只读观察专用浏览器 16 次，每次均为 1 个吉客云页面，未重现历史故障；未提交登录或请求导出。候选随后通过独立的仅登录验证，未进入业务导出回调。

## 变更

- 浏览器绑定验证后最多等待 15 秒；同一个候选页面及 URL 连续两次观察一致后才能使用，默认间隔 250 毫秒。只接受原精确吉客云 origin 或 about:blank，继续使用原登录、租户和安全验证规则，不猜选多页、不接管已打开的浏览器。
- 超时错误只记录总数、候选数、空白页数、吉客云页数和其他页数，单项最大 999999；不记录完整 URL、查询参数、fragment、页面内容或凭据。无 browser context 也使用同一明确错误类，并进入已拥有浏览器的原清理路径。
- 共享小时重试将 `API_LOGIN_PAGE_NOT_UNIQUE` 及精确吉客云 `原运行 n8n-export-first-<id> 尚未闭合` 错误归为人工处理终态。瞬态网络错误仍按原规则重试；其他工作流节点、连线、目标身份、计划和一小时等待不变。已处于等待中的旧 execution 携带旧定义，发布不能假装自动更新其快照，须单独核验与收尾。
- 原 preflight plan/apply operator 扩展为可关闭这一类 API 页面选择失败，接受旧精确错误或新版固定格式的纯计数错误。仍要求 B 节点、固定请求地址、精确节点顺序、失败状态、无成功重试或活跃执行、session_api_v1 空 exports、无 exportIntent、无未知计划字段、日期/active 匹配及四类业务效果路径全不存在。闭合须由 operator 明确执行，保留原全局锁、helper 空闲、摘要复验和 create-only 回执；并非自动清空任何失败任务。

## 验收和发布

测试覆盖零页、持续多页、同 URL 的页面对象被替换、脱敏计数、错误分类、旧精确恢复回归、通用关闭的错误/身份/阶段/摘要/并发/业务效果/迟到文件拒绝。候选真实登录成功只证明当前登录路径，不等于五表导入成功，也不能证明历史页面异常的底层原因已复现。

正式发布使用原在线准备、备份独立恢复、应用维护及唯一 Start 路径；共享 n8n 错误流程应基于实时定义只替换分类器代码，保留 owner、active、其他节点及全部目标流程。发布后复验新 helper 和原 4102 证据，再通过原 operator 生成新计划、按精确摘要关闭并回读。业务验收由原 n8n 完整执行完成，禁止直接调用导出或导入接口代替。

## 正式采用记录

最终源码 `d1f5a44ce1538c17704993ae31a5b87a443590ca`，Worker/helper `20260923T015424Z-b8038ff004984110`，manifest `231e879d95c7d8cc964ac9b4e9383af009f8336b1b213d0b8e4d04bbf5b8fbd5`。前一份候选未激活；最终候选包含空 exports 必须为对象的补充校验。

共享错误工作流 `TeruisiHourlyRetry2026` 已在线发布版本 `f18ba655-9da5-4d66-a1a6-47ecdb5c07b7`，current/active 版本相同。正式分类代码与生成器字节一致；从正式代码取出的隔离输入验证显示，两类已知错误输出为空，503 仍输出原安全重试。编辑器自动对齐位置、省略 Code/Wait 的默认参数、生成 Wait webhookId 并添加 `binaryMode=separate`；已从已安装节点定义核验默认运行模式及时间等待，保持所有节点的业务参数、连线、目标、同 owner 调用策略和时区。该分类器只传 JSON，不生成文件。

旧等待 4117 携带旧定义，在核实全实例仅此一条等待执行后，通过原工作流界面只选择 Waiting 停止；UI 显示停止 1 条，数据库回读 canceled，未删除历史或创建手动执行。吉客云主工作流及 00:10 调度未改。10:00 的京东 4118 于 10:06:37 自然成功后才进入应用维护，未中断它。

4102 create-only 关闭回执 SHA-256 为 `cb57486f1394aef6986c0a6b0ac5dc89d717bbb84b45fcfd18e44e9f0f01c91a`；正式部署的恢复校验器接受回执，原计划/active/n8n error 摘要不变，四类业务产物路径仍不存在。Django 未重新部署，62 条迁移及原库存批次不变。应用恢复后单独检查到 Django 守护尚未运行，按已部署 operator 的 Run 和 singleton 约束补齐，回读 running/healthy；没有再次重启业务组件。

最终定向检查 40 项通过。全量基于 `bb1fcf3f` 为 2239 通过、20 跳过、2 项既有失败；最终空对象类型收紧后重新通过相关 40 项及正式完整构建。页面 20 项、456 模块后端边界通过，lint 0 错误/11 既有警告；全库类型检查仍有既有诊断，本次改动文件没有诊断。新关闭规则不是自动清理，也不覆盖已导出/下载/部分导入或结果未知的失败。

前备份独立恢复、前后备份复验与 E 盘三文件逐项归档、最终服务及看门狗证据见 [正式记录](evidence/jackyun-login-recovery-production-20260923.json)。本轮没有手动业务补跑、迁移数据库或重启 n8n。下一次原自然吉客云计划是 2026-09-24 00:10，今日五表尚未因本任务重新导入；当前登录验证成功不能替代该次真实五表验收。此记录不授予未来维护、关闭或补跑授权。
