# 多 Agent 深度经营分析：分阶段实施

## 目标与阶段

经用户确认，按以下顺序开发。目标是按经营问题分析、诊断、规划并交付 HTML/XLSX，参考报告只是质量样本，不固定店铺、章节或工作表数量。

| 阶段 | 范围 | 放行条件 |
| --- | --- | --- |
| 一：数据与计算基础 | 来源、身份、粒度、覆盖、同比环比、完整分页、确定性计算、共享证据结构；及早核验市场/B 端历史可用性 | 来源金额与分组核对；缺失和映射歧义明确；店铺销售/推广关联与跨源证据具备实测依据 |
| 二：最小多 Agent 闭环 | 协调、店铺商品、推广搜索、独立复核；有界并发、持久共享证据、局部失败；基础双文件 | 至少两个专业 Agent 与独立复核真实协作；两个店铺、两个周期；冲突与失败用例；完整授权明细可取得 |
| 三：完整诊断与规划 | 市场、B 端、店铺销售/利润及全部维度；原因下钻、预算情景、近期/中期计划 | 每条建议有对象、证据、动作、前提、指标、观察期及回退条件；有数据/缺失分别验收 |
| 四：工程级文件与规模 | HTML 搜索/筛选/排序/分页/图表/导出；XLSX 格式、公式、情景、分片；交互与视觉检查 | 两格式同源同数，参数可复算，完整明细不截断，接近参考报告规模验收 |
| 五：工作台与正式验收 | 自然语言入口、进度、取消/恢复、证据追问、版本、成本和并发限制 | 多店、多周期、多问题真实端到端；权限变化、重复提交和模型未知结果安全处理；受控采用 |

阶段开发不代表每阶段必须生产发布。第二阶段的试用闭环不等于最终需求完成。正式启停、迁移与采用仍走现有维护流程。

## 当前交付边界

**已完成数据、证据、市场样本和专业协作的多批候选代码，尚未达到任一完整阶段的全部放行条件；尚未合入 main 或生产采用。用户已要求持续开发至五阶段完成，后续批次无需再次确认开发意图。**

第十五批接通首次经营分析工作台；第十六至二十批接通 v2 目录、采集封存、轻量多 Agent 报告及持久多卷交付。第二十一批首次工作台显式使用 v2 完整范围预览，封存后由用户手动启动分析，旧请求继续按原协议恢复。第二十二至二十四批接通 v2 固定预算存储、报告 API、独立多 Agent 执行、文件交付及首次目标选择/试算工作台。当前范围仍由用户显式选择，不是自动理解问题并匹配真实店铺身份。Excel 原生参数联动与 HTML 离线试算保留；完整容量、自动范围解析、市场与店铺真实来源及多店多周期业务验收继续开发，不能标记完整阶段完成。

已实现：

- 网店 owning reader 的精确平台/店铺规范明细端点，Worker 薄转发、中央 AI 工具、权限与既有审计链。
- 京东推广、SKU、原生 SPU、B2B、主数据；天猫推广、原生 SPU、主数据的现有规范事实适配。适配器存在不证明正式数据已导入或原始 B2B 文件口径已经验收。
- 本期、前一等长期间、去年同期夹闰日；人民币分、缺失 `null`、零/负基期状态、比率百分点。
- 连续签名游标、页摘要、来源版本、完整行数与控制金额核对；数据变化拒绝拼页。
- 流式分组、按汇总分子分母计算比率、跨店隔离、原生 SPU 与 SKU 身份分离、商家编码歧义不扇出销售事实。
- 可复用的 `PageReconciler`、`VerifiedAnalysis` 结构化核对结果。
- 第二批补齐 ERP 正向、退款、净销售、成本及数量规范页；平台/店铺/渠道精确隔离，ERP 网店规格编码与网店当前主数据精确关联，歧义和未匹配金额单独保留。
- 第二批补齐持久证据任务、不可改写的来源分块、版本检查点、取消/封存、所属用户权限和只读 AI 证据工具；来源读取、页核验、持久化及写审计形成可恢复的受控路径。
- 第三批提供统一分析表协议：从封存证据生成店铺、品类、SPU、SKU、关键词、搜索词、逐日分组，稳定行引用及同口径同比/环比；完整重验后分页返回。后续 Agent 和双文件共同使用该结构，不由模型各自计算。

阶段一仍需完成：

1. 店铺自身总览及跨源指标口径对齐；商品日访客不能代替店铺去重 UV。ERP 适配器与合成对账通过不等于正式店铺业务对账完成。
2. 扩充跨域只读来源清单，验收真实身份映射覆盖，建立统一的诊断、建议和双文件报告结果协议；现有持久证据协议仅记录原始规范页及核对结果。
3. 市场区间销量及 TOP 榜样本边界、正式 B 端来源、去年同期历史的实数覆盖验收；没有记录时同时核验真实店铺枚举和源覆盖，不能直接断言没有经营活动。
4. 正式规模的只读查询计划与容量验证。十万行合成计算验证不代表正式数据库查询性能通过。

后续阶段保持上表完整范围，不能用提高聊天字数或放宽沙箱行数替代这些工作。

## 数据接口与口径

`GET /api/netshop/analysis-records`

参数：`platform`、`shop`、`dataset`、`startDate`、`endDate`，可选 `window=current|previous|yearAgo`、`limit`、`cursor`。日期输入含首尾，数据库谓词使用左闭右开区间。窗口 1—93 天，后端每页 1—100 行；不同源/平台组合不支持时返回 422。禁止重复/未知参数。

仅无范围限制管理员可用；普通用户和有范围限制的管理员均拒绝。端点仅注册在 netshop reader（开发环境兼容路由除外），没有 writer 路由、SQL 输入、文件路径输入或任意代码执行。

AI 工具 `get_netshop_analysis_records` 在中央注册，单页最多 20 行、默认 10 行，单请求最多 8 次；超过 38,000 字符拒绝返回并要求减小页长从头读取，避免通用工具结果裁剪损坏核对链。未接入钉钉自动调用。单请求限额内无法读完时必须披露未完成，不能当作全量诊断。后续大数据执行器应直接使用受控分块服务，不把全部明细塞入模型上下文。

首页返回 `control`（完整行数、已有 typed 指标总额）、`coverage`、`availableDates`；每页都有 `sourceRef`、`sourceRevision`、`pageEvidence` 和 `pagination`。依次将未改写的页面交给 `VerifiedAnalysis.consume(page, request_cursor=...)`，仅在 `result()` 成功后发布该分组结果。

- 游标绑定参数、页长、网店 revision 和主数据批次，1 小时有效；版本变化/过期须重启该来源收集，不能混合旧页。
- 页内读前读后核对 revision。这是单来源版本围栏，**不是跨领域数据库快照**。第二批保存各来源版本、水位和采集时刻；自动持续执行、跨源更新决策属于后续工作。
- `dates_present` 只说明每天有记录，不说明平台已结算；缺日不能补零。
- 京东 `reportedGmvCents` 是平台总订单归因金额；天猫是源净成交口径；两者均不是 ERP 净销售或利润。归因窗口未验证时标记 unknown。
- 订单行不保证跨商品去重，商品×日访客不能当店铺 UV；部分缺失的分子/分母不计算比率。
- 主数据仅用最新已完成、带快照日期的批次。即使查询去年同期，也明确是当前快照，不冒充历史身份映射。
- 商品名、关键词、搜索词等来源文本只作为数据，不能充当 Agent 指令。
- 累计数值超出 JavaScript 无损整数范围时失败；内存分组最多 25,000 组，超限拒绝结果，不截断。超大词×商品组合的持久分区尚未实现。

## 第二批：ERP 与持久共享证据

ERP 通过既有 sales owning reader 的签名 consumer 操作 `analysis_records` 读取，AI 中央工具为 `get_sales_analysis_records`。参数为精确 `platform/shop/channel`、`startDate/endDate`，以及可选 `window/limit/cursor`；窗口、分页、摘要及金额单位沿用前述规范。后端每页最多 100 行，AI 工具每页最多 20 行、默认 10 行，单请求最多 8 次。没有客户、订单号或任意 SQL 输出。

ERP 日期为发货业务日 `business_date`。正向销售与退款按分摊金额符号拆分，净销售保留配件及补差价，排除刷刷仓；数量遵守原 `is_net_quantity_row` 投影。成本保留源符号，计算毛利为净销售减成本，不扣费用；源报告毛利、费用和排除配件的销售额另列。没有数据时返回精确平台/店铺的有界历史渠道枚举供核验，不自动换渠道，也不把空结果解释为零经营。

商品关联只用 ERP `online_spec_code` 对网店当前主数据 `merchantCode`，不回退到 ERP `product_code`。唯一 SKU/SPU 配对才计入匹配组；重复编码对应多个商品时不扇出金额，未匹配和歧义金额仍参加源总额核对。当前主数据不能证明历史映射；该结果不能证明推广归因成交等于 ERP 销售，更不能用于虚构关键词利润。

证据任务接口均走现有 AI reader/writer、实时权限及审计链：

| 接口 | 行为 |
| --- | --- |
| `POST /api/ai/business-evidence` | `clientRequestId` 与最多 12 个固定来源，重复标识只允许同一计划 |
| `GET /api/ai/business-evidence/{id}` | 计划、来源进度、水位、版本、核对结果 |
| `POST /api/ai/business-evidence/{id}/collect` | `sourceKey/expectedVersion`，每次读取并提交一页 |
| `POST /api/ai/business-evidence/{id}/finish` | `expectedVersion/action`，`seal` 须全部完整；`cancel` 保留已有证据 |
| `GET /api/ai/business-evidence/{id}/chunks/{sourceKey}?sequence=1` | 单块规范页与摘要 |
| `GET /api/ai/business-evidence/{id}/mapping?sales=源键&master=源键` | 仅对封存证据重验全页并作商品关联 |
| `GET /api/ai/business-evidence/{id}/analysis?sourceKey=源键&dimension=shop` | 统一分析表，支持 `baselineKey`、`offset`、`limit` |

来源格式为 `{key, domain: sales|netshop, query}`。query 只接受固定身份、数据集/渠道、日期及比较窗口，不接受调用方提供的事实页面。中央只读工具 `get_business_analysis_evidence` 可读取本人任务摘要或单块，未接入钉钉。不同专业 Agent 未来可在同一发起人权限下引用相同证据；本批没有自动派发 Agent、付费推理或报告生成。

取数在 AI 写锁外执行，提交时重新核对身份、状态和版本。分块、检查点、写回执及审计同事务提交；取消后的迟到结果或审计失败不会留下新分块。中断后可从已提交检查点继续逐页请求，但游标过期或源 revision 变化须重新创建收集任务；不得把旧页和新页拼接封存。封存仅表示来源完整核对，`modelAnalysisCompleted` 仍为 false，不表示诊断复核通过。

当前有界容量：每人最多 4 个未完成任务；每任务最多 2,000 页、64 MiB，每人累计 256 MiB，全局累计 2 GiB 和 10,000 个任务；每页固定请求 10 行。商品关联每个来源最多 5,000 行、输出最多 1.5 MB。达到上限明确失败并保留检查点，绝不截断后声明完成。尚无历史证据清理或大型分区执行能力，不能据此宣称达到参考报告规模。

新增 AI 迁移 `0014_business_evidence`，自有表从 56 张增至 58 张。分块只增不改，任务身份和计划不可改，封存/取消后为终态；数据库触发器、最小 reader/writer grants、健康检查、历史备份清单及恢复校验同步维护。本批未迁移正式数据库。

## 第三批：统一分析表

`business-result-table-v1` 保存来源核对摘要、筛选/日期、每组值与缺失数、加权比率、比较结果及稳定行 ID。`dimension` 限 `shop/category/spu/sku/keyword/searchTerm/daily`；`baselineKey` 仅接受同来源、同店铺、同原始查询日期的 `previous/yearAgo`，不将不同业务口径作增长比较。日表禁止直接按日期字符串匹配跨期。

当前或基期不存在的分组不补零；缺日或字段缺失不计算增长率；零/负基期保留差额并标记状态。CTR/转化率变化为百分点，CPC 和 ROAS 不误用百分点。空维度单列并标记，商品访客不冒充店铺去重 UV。一个表只表达所选来源，未混加推广成交、ERP 净销售和 B2B 金额。

服务端只从本人封存证据重算，逐块核验摘要和完整控制汇总，不重新查询业务源或调用模型；每次 API 返回最多 100 组，中央工具 `get_business_analysis_table` 默认 10、最多 20 组。沿 `offset/limit` 可读取完整组表，读取一页不能声称全量分析。大型跨源分区和导出仍属后续工作。

第三批验证：隔离 PostgreSQL 27 项通过，中央工具 7 项通过，构建及修改的 TypeScript lint 通过。首次 API 测试把查询串错误地签入 path，改为已有支持独立 query 签名的测试工厂后通过；没有放宽身份或签名校验。日志为 `.runtime/ai-pg-0556957c8f0c/tests.log`、`.runtime/batch3-tools-final.log`、`.runtime/batch3-build.log`、`.runtime/batch3-lint.log`。

## 第四批：证据绑定的专业分析队列

`POST /api/ai/business-reports` 接受 `clientRequestId/evidenceRunId/question/dryRun`，只从本人已封存、具有相同原始比较区间的证据创建报告；问题最多 1000 字。复用既有报告表和持久 DAG，不新增迁移。生成三类专业分析（店铺商品、推广搜索、市场/B端），随后独立 Agent 复核、整合报告、人工确认；现有同一 DAG 仍串行运行，尚未扩展到专业节点并行。

这类子任务向模型只提供共享证据和分析表两个工具，执行前再限制 `runId` 必须是本次封存任务；中央工具桥仍校验完整目录摘要。普通 Agent 保持原能力。当前权限、模型版本、调用预算、租约、取消、回执和未知结果不重发沿用既有队列。专业结果及整合正文设置字节上限，避免依赖交接超出 24 KiB；完整数值明细通过证据交接，不复制进模型文字。

`business-diagnosis-v1` 区分观察、假设、动作、缺口。结构化引用必须指定来源、维度、行位置、稳定行 ID、指标及值类型，服务端重新计算并解析真实数值，不接受模型自报的数值替代。每份最多 12 条结论、32 个引用；调整动作必须含对象、具体改动、前提、责任角色、预算影响、观察期、衡量指标及回退条件。引用核验不能证明自由文字及因果解释正确，仍须独立和人工复核。

独立复核有冲突或任一专业/复核 Agent 没有成功的共享证据读取回执时，不能批准正式报告。草稿与阶段结果保留；模型未知结果不得自动重放。当前文件下载对新报告显式拒绝，待后续专用构建器接通，避免把旧 2000 行静态导出冒充完整交付。现有模板报告行为保留。

第四批验证：AI 域与纯计算隔离 PostgreSQL 共 263 项（261 通过、2 跳过），后续针对引用、精度与动作字段的复测通过；构建和后端边界通过。类型检查仍为 153 条既有诊断，与第二批基线无新增差异。端到端队列演练实际经过五个独立 Agent job、十次模拟 provider 派发、五个持久工具回执和人工复核，未调用真实模型；这不是真实分析效果验收。日志见 `.runtime/ai-pg-b8bc1cb6c55f/tests.log`、`.runtime/ai-pg-426da1da527a/tests.log`、`.runtime/batch4-build.log`、`.runtime/batch4-typecheck.log`。

## 第五批：市场样本证据接通（仍非第五阶段完成）

新增 `get_market_analysis_records` 与市场 owning reader 的 `analysis_records`。来源固定京东、类目、榜单范围、SKU/SPU 和价格筛选，只收集单日 TOP 榜；重叠周/月榜排除并计数。原生区间上下界、缺失值和日覆盖保留，不暴露估计中点作为精确成交；市场不绑定用户店铺，也不提供全行业规模或份额。

证据计划现在接受 `domain: market`，封存后支持品类、品牌及原生 SKU/SPU 表。完整可比样本可计算当前下界/基期上界至当前上界/基期下界的变化区间，点估计增长率保持不可用；样本进出榜影响必须随表说明。没有榜单数据不解释为零市场。

验证：市场、既有市场消费查询、纯计算及证据/报告相关隔离 PostgreSQL 41 项通过；补充市场证据封存回读后 15 项通过。中央分析工具 8 项通过，生产构建及修改 TypeScript lint 通过。日志 `.runtime/ai-pg-0c9d36213485/tests.log`、`.runtime/ai-pg-a1205a5e1a35/tests.log`、`.runtime/batch5-tools.log`、`.runtime/batch5-build.log`。没有正式取数、付费模型或生产发布。

## 第六批：后台批量取数入口与证据切片

创建证据可显式指定 `collectionMode: bulk`，不可变计划固定采集器版本、专用 `business_collection` 入口及每页 100 行上限。旧任务默认标准模式，继续每页 10 行，不能在续传中切换页长。新入口只向无范围限制管理员的内部签名采集调用开放，经中央目录摘要、执行权限和审计，再到各域 owning reader；不会投影给模型、聊天、钉钉或 Codex MCP。普通工具 40k 字符上限不变，独立采集页同时受 128 KiB 字节上限约束。

三类来源按 UTF-8 字节选择完整行前缀，超过容量只减小本页行数，游标绑定实际末行；单行过大明确失败。不会截断文本后宣称完整。`get_business_analysis_evidence` 读取分块时默认每次 10 行，支持 `rowOffset/rowLimit`；返回完整页摘要、切片范围与明确的部分读取标记，不能将切片摘要冒充整页核对。

本批没有提高任务 64 MiB/2000 页、聚合 25k 分组或映射 5000 行的限制，也尚未接入自动后台轮询；大规模持久分区、自动采集及完整文件交付仍待完成。批量入口不是全量规模验收。

验证：隔离 PostgreSQL 78 项通过，分块切片及报告补充 15 项通过；工具与契约 46 项通过。全量 Node 2221 项中 2200 通过、1 项 Windows 进程谱系变化失败、20 跳过；该受保护 venv 启动器测试独立复测 powershell.exe/pwsh.exe 两项均通过，未改进程安全规则。最终构建、lint、后端边界通过；类型检查仍为原有 153 条诊断，无新增。日志 `.runtime/ai-pg-18bc467a5bb0/tests.log`、`.runtime/ai-pg-4479e23eef23/tests.log`、`.runtime/batch6-tools-final.log`、`.runtime/batch6-unit.log`、`.runtime/batch6-process-retest.log`、`.runtime/batch6-build-final.log`、`.runtime/batch6-typecheck-slices.log`。

## 第七批：专业节点实际并行与局部失败

新经营报告固定 `executionMode: parallel-v1`，最多三个无依赖的专业节点同时执行；独立复核、整合和人工复核继续按依赖推进。旧报告和普通 DAG 的串行行为不变。节点创建仍在 AI 域事务内，受全局 64、每人 8 个活动 Agent 名额约束；名额不足时保留待执行节点。

原签名 `agent` 调度入口现在先取得 PostgreSQL 会话互斥，再按最早待执行任务选择。普通任务仍推进一个微步，经营任务最多三个独立微步并行；每个微步仍只有一次 provider HTTP 或一次工具读取。数据库互斥只覆盖调度批次，外部调用不持业务事务锁。线程使用独立 Django 连接，并在结束时关闭；租约、权限、预算和派发回执继续逐项校验。

一个专业节点失败不清除已完成结果，也不提前跳过因容量不足尚未运行的独立节点。全部独立结果收齐后，可安全重试的失败进入暂停，人工恢复只重启原失败 job；其他输出不变。任何未知模型结果继续禁止自动重放，下游复核和整合不启动。整任务取消使所有在途租约失效，迟到结果不能写入。

验证：AI 域及计算隔离 PostgreSQL 270 项中 268 通过、2 跳过；并行测试通过屏障验证三个 provider 调用同时处于执行中，并检查重复调度互斥、实际局部恢复和三条在途结果取消。工具/Worker 入口 18 项、构建及后端边界通过。补充单条未知结果保留另外两条成功结果及互斥异常释放后的最终 21 项通过。所有 provider 均为合成替身，实际付费模型效果、正式连接容量和多店多周期仍未验收。日志 `.runtime/ai-pg-1aeb07821a78/tests.log`、`.runtime/ai-pg-63714473a4e9/tests.log`、`.runtime/batch7-node.log`、`.runtime/batch7-build.log`。

## 持续开发的剩余验收清单

2026-09-16 只读检查参考 XLSX 的工作表 XML：文件 42,715,563 字节、30 个工作表；含表头的行号上界分别为词货关联 149,842、市场日榜 84,006、搜索词诊断 38,091、推广日 SKU 23,985、销售明细 7,396。它们是规模参考，不能将含表头的行号当业务记录数，也不能以这些示例数据替代系统来源。

- 数据：接通店铺总览/真正 UV 的可用性说明、市场 TOP 样本及区间口径；实数核验 B 端和同期历史；跨源映射与来源枚举不误判空数据。
- 执行：后台取数的批量受控通道、检查点和分区计算；专业节点有界并行及局部失败；多店、多周期和真实模型效果验收。
- 交付：专用 HTML/XLSX 构建器接通报告下载；分析、原始规范明细、引用、行动、情景参数同源；大文件/大分组采用明确的分片或附件方案，不能被普通聊天 38k 字符、20 行、25k 分组上限截断。
- 工作台：自然语言问题与范围确认、来源缺口、后台进度、取消恢复、复核冲突、下载/分片和证据追问入口；保留原模板报告能力。
- 完整验收：接近以上规模的合成压力样本、独立数值核对、HTML 交互、Excel 重算及视觉检查；真实只读查询计划与授权范围的真实模型验收；最后再按受控发布要求准备正式迁移/采用。

## 第八批：后台自动采集与暂停恢复

创建批量证据任务可指定 `autoCollect: true`，由 Worker 现有定时维护入口唤醒 Django `evidence` 队列；不再依赖浏览器逐页调用。采集与模型队列使用独立 PostgreSQL 互斥，每轮最多 16 页、25 秒内开始下一页，每次来源调用另有 30 秒预算。每页领取、检查点、完整性核验和审计持久化；完成所有来源后自动封存。

`POST /api/ai/business-evidence/{id}/control` 接受 `expectedVersion` 与 `pause/resume`。暂停、取消或版本变化后迟到页不得提交；暂时服务失败按 30/60 秒退避，连续第三次失败暂停，源版本/游标问题不自动换源续接。创建人停用或失去管理员权限立即停止采集。审计失败回滚同页证据与封存状态，恢复只从已提交检查点继续。

迁移 `0015_business_collection` 为原证据任务增加采集状态、下次时间和错误计数；旧任务默认 `manual`，不会意外启动。AI 自有表仍为 58 张。本批不自动触发付费模型，也不改变 64 MiB/2000 页等容量；完整文件交付和工作台尚待接通。

验证：最终隔离升级演练包含 AI 261 项（259 通过、2 跳过）、0013→0014→0015、旧 56 表摘要保留、0014 历史任务字段保留、真实 reader/writer 权限及独立 dump/restore，日志 `.runtime/ai-pg-7ae41aac3115/tests.log`、`.runtime/batch8-upgrade-final2.log`。此前 AI 与纯计算联合 279 项（277 通过、2 跳过）；最新授权降级用例已包含在最终 AI 演练。Node 12 项通过，构建、lint、后端边界通过，类型检查仍为 153 条既有诊断。升级脚本旧 SQL 插入缺少新增列导致首次演练失败，修正夹具后通过；未放宽数据库约束。仅候选分支开发，未采用到正式系统。

## 第九批：大分组分页计算

分析 API 改为逐页核验来源、使用有界临时文件归并分组，再只加载请求的最多 100 行结果。当前/基期及其并集分别最多 250,000 组；临时 SQLite 文件仅作本次派生排序计算，位于随机临时目录，用完或异常后删除，不是业务事实库、持久缓存或恢复检查点，PostgreSQL 封存证据仍是唯一权威。固定 SQL 不接受模型代码，原页摘要、全部行数、各金额与缺失计数必须先核对，才允许读取任何结果页。达到 256 MiB 临时文件上限明确失败，禁止截断。

分组数学与既有算法共用，稳定行 ID、排序、同比/环比、市场样本区间、加权比率和缺失含义保持一致。分析工具及诊断引用同步支持新行号范围；旧非分页纯计算函数仍保持原 25,000 组上限。来源采集 64 MiB/2000 页、关联 5000 行限制尚未改变，也尚无持久派生表；每次请求仍重验来源，不能据此宣称全量工程文件或总体吞吐验收。

验证：隔离 AI 与纯计算 285 项（283 通过、2 跳过），日志 `.runtime/ai-pg-746c361ba11c/tests.log`；新增逐维度双窗口对照、Unicode 排序、30001 分组末页、晚到坏页、整数溢出/容量事务回滚和临时文件清理测试。149841 行/同数量分组的独立合成演练核对金额合计 11226237561 分，末页 100 行与缺失数全部一致；Python tracemalloc 峰值 667475 字节，不含 SQLite 原生缓存、系统缓存和进程 RSS。启用 tracemalloc 耗时 52.797 秒，不启用时 18.563 秒；这些是本机纯计算测量，未含正式数据库读取或并发请求。日志 `.runtime/batch9-scale.log`、`.runtime/batch9-scale-time.log`，复验脚本 `tools/business-analysis-scale-rehearsal.py`。Node 10 项、构建、lint、后端边界通过；未查询正式数据、调用模型或部署。

## 第十批：同源流式 HTML/XLSX 构建器

新增 `business_export` 从报告固定的封存证据版本准备范围、诊断、调整规划、结构化引用、来源覆盖、全部规范明细及各维度本期/同比/环比表。导出只读本人持久证据，复验分块与来源核对，不查询实时业务源或调用模型；正式文件必须已有人工复核，草稿明确标识。原始基期明细显示实际比较日期，金额保留字段的分单位，不把原生市场区间改成点值。

完整分析表通过分区排序的一次扫描导出，与分页 API 共用数值、缺失含义及稳定行 ID。临时表在最多 512 MiB 的随机 SQLite 文件中准备，只用于此次派生计算；逐表结束和异常后清理。双文件随后只遍历每张表一次，使用完全相同的有序行数据与 SHA-256 摘要。Excel 带冻结、筛选、列宽、数字格式、可重算的已知值合计和比率公式；超过 Excel 15 位有效数字的整数保留为文本，来源文本不会转成公式。HTML 使用嵌入式数据、离线搜索、排序、缺失筛选、100 行分页、数值前十图和筛选 CSV 导出，不加载外部资源。

本批构建器支持最多 120 表、每表 100 万行/160 列、每文件 256 MiB；超限或晚到坏行明确失败，不返回完整文件回执。可用构建器不等于系统已能下载：经营报告原下载路由仍拒绝旧式导出，后台构建任务、持久文件、分块下载、完整工作台、自动分片及预算情景尚待接通。本批未新增迁移或生产采用。

验证：隔离 AI 与纯计算 293 项（291 通过、2 跳过），日志 `.runtime/ai-pg-653f9fa73b54/tests.log`；最终导出与分区复测见 `.runtime/ai-pg-e93adcba23e0/tests.log`。覆盖全部规范明细、维度 API 行 ID 对照、快照/权限/复核、坏分块、单次遍历、行数不足/超出、文本注入、超精度整数和错误比率缓存。修改文件 lint、后端边界与差异检查通过。

独立合成压力文件有 149841 行/12 列明细：XLSX 58575964 字节、HTML 84123608 字节，生成约 21.579 秒。逐行读取实际 XLSX、重新计算行摘要和文件摘要均通过，128435 条明细比率公式存在；不是参考报告全部 30 张表或真实分析质量验收。Chrome 验证完整末页搜索、排序、缺失筛选、图表、CSV 文本保护、手机宽度及零外部请求，日志 `.runtime/batch10-wide-verify.log`、`.runtime/batch10-wide-browser.log`。另用 bundled artifact-tool 导入小型合成文件，独立重算点击率 5%/3% 和合计 600/22 并渲染检查；没有改写原文件，也没有将个人 bundled runtime 引入生产依赖。复验工具为 `tools/business-files-rehearsal.py`、`tools/business-files-verify.py` 和 `tools/business-files-browser-rehearsal.mjs`。

## 第十一批：持久双文件任务及报告页下载

迁移 `0016_business_files` 新增文件任务和不可变二进制分块，AI 自有表增至 60 张，同步 reader/writer 权限、健康检查、旧备份版本识别和恢复清单。任务固定报告、证据、专业节点输出、草稿/复核身份及渲染版本；草稿通过人工复核后仍为草稿。创建幂等，完整文件就绪或取消后不能改写。

Worker 独立唤醒 `files` 队列，与取数和 Agent 并行等待；Django PostgreSQL 全局互斥保证单个文件构建器。每轮构建预算 600 秒、租约 650 秒，最多五次构建；生成过程定期校验权限、版本和取消，文件写入以每 512 KiB 分块、最多 16 块一批提交。取消或暂停后的迟到写入拒绝，审计失败回滚本批内容和字节计数。已完整保存的双文件在发布前失败时，可恢复核验并交付，不重复构建、不调用模型。未完整保存的尝试继续保留，恢复重新构建，不能宣称从任意行断点续写。

`/api/ai/reports/{id}/files` 创建和列出任务，`/api/ai/business-files/{id}` 读取单任务，`/control` 使用版本控制，`/chunks/html|xlsx` 读取已就绪分块。每次读写及下载都校验实际身份；列表只返回紧凑进度，单个清单按需读取。每文件 256 MiB、每任务含旧尝试共 1 GiB、每人 2 GiB、全局 8 GiB，未完成每人两项、总计最多 1000 项；现阶段没有自动归档清理，超额暂停并保留证据。

经营报告详情显示草稿/已复核双文件生成、进度、暂停/恢复/重新构建/取消。关闭网页后由后台继续处理。浏览器逐块核对身份、尝试、序号、长度和 SHA-256，再校验完整摘要，并在创建下载 Blob 前重新验证权限；普通 API 的原有体积上限不放宽。下载取消只取消本次读取。浏览器暂存完整文件，内存占用仍随文件大小增加；服务端规模验收不代表低内存终端验收。

验证：隔离 AI 与计算 300 项（298 通过、2 跳过），`.runtime/ai-pg-4a41bfa281ad/tests.log`；最终文件列表改动再测 7 项通过，`.runtime/ai-pg-e1f43b3c3ced/tests.log`。升级 `0013→0016`、旧 56 表摘要保持、真实限制角色、写入栅栏、二次迁移无操作及独立 dump/restore 通过，`.runtime/batch11-upgrade.log`；另验证 524288 字节二进制分块恢复后逐字节与摘要一致，此为传输夹具，非业务报告验收。下载、API、调度和备份相关 Node 31 项、Python 备份清单 8 项通过。构建和后端边界通过；类型检查仍有 153 条原基线错误，修改路径无新增错误。

Chrome 合成组件演练通过严格模式挂载、旧读取与保存交错、按版本暂停/恢复、读取成功不清除写错误、实际文件下载字节一致和手机宽度检查，`.runtime/batch11-ui3.log`；截图 `.runtime/business-file-ui/files.png`、`mobile.png`。可复验 `node tools/business-file-ui-rehearsal.mjs`。该测试使用独立静态构建、模拟 API 和临时浏览器，不是正式页面/付费模型/全链路验收。

全量 Node 首轮 2232 项：2211 通过、20 原有跳过、1 条旧调度返回结构断言失败；修正断言以包含 evidence/files 两队列后，该文件及下载合计 24 项复测全通过，未再重复无变化的全库长测。日志 `.runtime/batch11-all-node.log`、`.runtime/batch11-node-corrected.log`。全库 `npm run lint` 通过，`.runtime/batch11-all-lint.log`。

本批未改变采集 64 MiB/2000 页和身份映射 5000 行上限，未接通自然语言任务创建、自动文件拆分和预算情景。没有合入 main、生产迁移、启停服务、真实取数或外部发送。五阶段仍保持开发中。

## 第十二批：固定证据预算情景与在线版本试算

报告创建可携带 `budgetPlan`：预算总上限、预留、规划/观察天数、提前复盘花费占比、样本门槛，以及最多 100 个预算对象和 5 个情景。每个对象固定来源、聚合维度、分析行位置和稳定行 ID，带权重、预算上下限、责任角色和最低归因产出比。同一店铺不能同时按 SKU 和关键词重复分配；基数只允许同区间本期推广，不能用 ERP 销售或市场样本替代。逐组完整重验封存来源后才采用选中对象，不接受调用方提交事实数值。

金额以整数分保存，约束分配采用精确分数运算和稳定余数分配，最低预算不可满足时拒绝；达到对象上限后保留未分配余额。点击成本、订单效率和订单金额乘数显式来自输入；结果是历史归因效率下的假设情景，不承诺增量收益。缺日、缺少基数或无效基数不生成预测；低样本单独标识，样本门槛不冒充统计置信度。不同平台归因口径不直接合计；未知贡献率不计算扣推广余额，已知假设贡献也不称为实际利润。

新增只读工具 `get_business_budget_scenarios`，只能读取当前报告绑定的证据、参数摘要及分页对象，模型不能通过该工具修改预算。带预算的报告增加推广和独立复核 Agent 全对象读取门禁，缺少分页回执、参数版本不一致或复核冲突时不能正式批准。原无预算报告保留既有工具范围。

报告详情可修改参数并调用 `POST /api/ai/reports/{id}/budget-preview` 在 AI reader 试算；同源和本人权限继续核验，不写数据库或调用模型。保存新版本创建带 `previousReportId` 的新任务，固定新参数并重新分析/复核，原报告不覆盖。保存结果未知时锁定该次输入，重试复用同一请求 ID 和内容，不自动重复创建。关闭组件取消本次读取，旧响应不写入新报告。

双文件新增预算约束、对象、情景假设、汇总和逐对象测算表，HTML 与实际 XLSX 单元格逐值对照，包括单位、空值、负贡献和来源行身份。本批的文件仍是报告快照，尚无下载文件内的预算参数编辑和联动公式；在线试算通过不代表该项完成。无新增迁移，AI 自有表仍为 60 张。

验证：AI 与计算隔离 PostgreSQL 311 项（309 通过、2 跳过），`.runtime/ai-pg-aced4913f125/tests.log`；加强双文件实际单元格检查后预算相关 17 项通过，`.runtime/ai-pg-187c6b5b25a4/tests.log`。首轮发现 Python 局部导入 `read` 遮蔽其他接口读取函数，改为独立别名后全套通过。Chrome 合成组件演练通过参数重算、未知贡献率、手机宽度和保存丢回执后的同一提交确认，`.runtime/batch12-ui.log`；使用真实纯计算引擎和模拟接口，未执行付费模型。复验工具为 `tools/business-budget-rehearsal.py`、`tools/business-budget-ui-rehearsal.mjs`。构建、全库 lint 和后端边界通过；类型检查仍有 153 条原基线诊断，修改路径无新增。其余验证见 `docs/evidence/ai-business-budget-candidate.json`。

全量 Node 2233 项：2212 通过、0 失败、21 跳过；其中 20 项原有跳过，另 1 项构建产物时序检查在最终构建后连同相关文件重新测试，共 5 项全通过。日志 `.runtime/batch12-full-node.log`、`.runtime/batch12-bundle-final.log`。未查询正式经营数据、迁移、启停服务、部署或发送通知；五阶段继续开发。

## 第十三批：HTML 离线预算重算与渲染版本兼容

下载 HTML 增加独立离线试算区，可修改总上限、预留、规划/观察天数、复盘花费占比、样本门槛、对象权重/上下限/最低产出比，以及情景成本、订单效率、客单和贡献率假设。使用 BigInt 中间计算保留整数分分配、稳定余数和四舍五入；初值必须与后端逐项核对，超过离线精确数值容量或不一致时禁用试算，原报告仍可阅读。所有运算均离线，不加载外部资源、调用模型或执行投放。

编辑后立即清除旧试算，非法参数不保留旧结果。新结果显示本地版本及未复核状态，可下载包含原报告 ID、原参数摘要、新参数和结果的 JSON；此版本不是系统报告版本，刷新不会自动保存。恢复原参数回到服务端初值。来源商品名和情景文字使用惰性 JSON 与纯文本 DOM 展示，不作为脚本执行。原 HTML 数据表与 XLSX 不因试算改变；本批没有 XLSX 参数联动，也没有将离线 JSON 自动导回系统。

文件任务新建使用渲染版本 2；迁移 `0017_business_file_renderer` 仅将约束扩至版本 1/2，保留不可变身份、额度、状态和全部旧数据，不增加表。旧任务仍使用渲染器 1，已存完整旧文件可按旧清单恢复。新清单固定渲染版本，不能用旧清单恢复新版任务；有 v2 任务时拒绝回退约束，避免伪装兼容旧版。新旧任务不会互相覆盖。

验证：158 组合成参数的完整分配、状态和情景数值与 Python 引擎逐项一致，另 5 组非法/超精度输入拒绝；覆盖 100 对象、最大总预算、连续触顶、余数并列、缺日、缺基数、低样本、零订单、未知贡献和不同平台口径。真实 Chrome 打开生成的离线 HTML，检查分币输入、权重修改、本地版本下载、非法参数清空、原表保全、恶意来源文本、初值被篡改、手机布局及零外部请求通过；桌面/手机渲染已查看。日志 `.runtime/batch13-offline-final.log`，复验工具 `tools/business-budget-offline-rehearsal.py` 和 `tools/business-budget-offline-browser.mjs`。

纯计算及文件 12 项、Node 下载与 API 28 项、全库 lint（0 错误，11 项既有警告）、后端边界通过。隔离升级 `0013→0017` 的旧 56 表摘要、真实限制角色、幂等迁移、独立 dump/restore、512 KiB 二进制原文及新旧渲染版本均通过，`.runtime/batch13-upgrade-final.log`。升级后首轮 AI 测试有一条已有会话激活排序失败；本批不修改该会话逻辑，保留为第五阶段稳定性核验项，最终复测结果见 `docs/evidence/ai-business-offline-budget-candidate.json`。

最终 AI 与计算共 315 项，312 通过、2 跳过，1 条新增文件测试错误假设同创建时刻的任务按渲染版本排队；修正为逐个核验所领取任务的版本及两版本均运行，文件与会话 15 项复测通过。日志 `.runtime/ai-pg-41c0c3bd2af1/failure.log`、`.runtime/ai-pg-81286da19b5b/tests.log`。此前会话排序用例在这两次验证均通过，但未据此宣称其稳定性问题已修复。无生产查询、迁移、服务启停、部署、真实模型调用或外部发送。自然语言首次创建、Excel 参数重算、完整多来源容量和真实业务验收仍未完成，五阶段不能标记完成。

## 第十四批：Excel 原生参数联动与三版本文件兼容

新版含预算的 XLSX 在原报告后附“预算试算_可编辑”“预算分配计算”“预算情景计算”三表。参数、计算、结果有明确归属；蓝字浅黄色格可改总预算、预留、对象权重和上下限、样本门槛、观察期及情景假设。一次选择一个活动情景，各计算表显示相同选择；原报告中的全部情景快照保持原样。单元格保护用于防误改，不是权限安全边界，不使用宏或外部数据连接。

预算分配使用整数商和余数，按稳定对象身份处理分币尾差。原生公式先约分再计算金额、产出比、假设贡献扣推广余额、复盘花费、等天数基期花费及预算差额；超过约分后 14 位整数积范围时相关结果留空并提示高精度复算，可转用 HTML。该精度限制比 Python/BigInt 更保守，不能声称全部合法参数都能在 Excel 中预测。原报告保留点击/订单预测等完整情景，新增联动页目前不重算点击/订单两列。不同成交口径、未知贡献率、零贡献率和缺日分别处理，合计缺失或超过精确范围时不展示伪精确总额。试算为未复核本地结果，不修改经营建议、系统版本或投放。

新文件任务使用渲染版本 3；迁移 `0018_business_excel_renderer` 只允许既有约束中的版本 1/2/3，不增加 AI 表。旧版本仍使用原生成行为，有 v3 任务时拒绝回退约束。文件内清单与持久交付清单记录试算页身份、原报告和参数摘要；完整暂存文件仍按原版本校验恢复，不因新增公式调用来源或模型。

独立电子表格引擎实际导入 XLSX、修改单元格并重新计算，共 46 组、1215 个逐值比较通过，覆盖 100 对象、最高总预算、连续触顶、随机权重、分币尾差、情景切换、空/零贡献率、缺失、混合成交口径、超精度和 8 组非法输入；检查原表不变，并渲染查看主试算页。该验证不是 Microsoft Excel/WPS 客户端实机验收。工具为 `tools/business-budget-excel-rehearsal.py` 与 `tools/business-budget-excel-recalculate.mjs`，日志 `.runtime/batch14-excel-all.log`。初次独立重算发现余额表达式括号及空贡献率的 INDEX 空值判断问题，均已修正；精度合计使用通用 SUMIF，避免评估器对 ABS 数组的兼容差异。

候选业务文件与计算隔离 PostgreSQL 66 项通过，升级 `0013→0018` 后 AI 279 项（277 通过、2 原有跳过），旧 56 表摘要、真实限制角色、二次迁移无操作、独立 dump/restore、512 KiB 原文及三个渲染版本恢复通过。Node 下载/API 7 项、修改工具 lint 和后端边界通过。最终联合回归 319 项：316 通过、2 跳过，既有会话激活排序失败再次出现；本批 Excel 和 v3 持久交付全部通过，排序问题转入后续专项修复，不以重跑掩盖。详见 `docs/evidence/ai-business-excel-budget-candidate.json`。未合入 main、生产迁移、启停服务、部署、真实取数、付费模型调用或外部发送；五阶段继续开发。

第十四批独立 Agent 复审另核验 8 组边界，未发现阻断缺陷，补充修正等天数基期超精度时的可见提示。正常归因预测继续显示，基期对比单独标记需高精度复算；49 组实际重算、1253 个数值比较及主表渲染通过，10 项纯计算/边界测试通过。详见上述候选证据中的独立复审记录。

## 工作台稳定性补充：会话激活顺序

此前第十三、十四批暴露的既有排序失败已稳定复现并修复：Windows 时钟返回相同时间或时间回退时，仅用打开时间排序再按旧更新时间打破平局，会把刚打开的旧会话排在后面。现在沿用 AI 写事务互斥，在同一创建人、同一板块内分配单调递增的打开排序时间；旧 AI 会话按原更新时间参与比较，其他用户和板块不参与。保留业务更新时间、归属、上下文和原权限，不新增迁移。

固定时钟测试在修复前必现，修复后等时、倒退、归属隔离及 AI/经营分析联合 321 项（319 通过、2 原有跳过）全部通过。证据 `docs/evidence/ai-workspace-clock-order-candidate.json`。该修复属于工作台稳定性补充，不代表第五阶段完整验收或正式采用。

## 第十五批：首次任务工作台与原请求恢复

AI 流水线页增加经营分析工作台。用户填写经营问题、日期、精确店铺/ERP 渠道、比较窗口及市场条件，先调用只读预览生成完整来源矩阵，确认后保存后台证据任务；封存前不能启动分析，封存后手动选择模拟或调用配置模型的多 Agent 分析，关联报告进入既有复核、预算和文件面板。本批没有调用真实模型。

预览复用实际网店来源支持表及市场查询校验器，不读取经营事实。所有请求来源保留在矩阵；不支持、超过 12 来源、计划 16000 UTF-8 字节或当前 DAG 输入 8000 字节上限时停止提交。主数据只安排本期快照；每个事实来源必须完整安排同一原始区间的请求窗口。计划中的维度、窗口及“分页采集完成”均不证明业务日期或字段已经齐全。旧版无问题上下文的证据请求保持原计划字节、摘要及幂等重放，包括原本允许的独立窗口和大于 8000 字节的旧证据计划。

任务列表服务端分页、按实际创建人和 scope 隔离，详情只返回最近 10 份关联报告并提示更多历史。新建证据和报告在当前标签页按账号保存完整请求、UUID 与已发送未知状态，刷新不会自动重放；首次结果未知后，即使重试返回 403，仍保留原编号。每次提交先读取当前账号，服务端再验证冻结的 `expectedPrincipalKey`，防止预检与提交之间切换账号。读取、预览和控制请求相互隔离迟到响应；历史手动采集任务仅允许取消，不展示后台暂停/恢复按钮。

本批由后端、前端与独立复审 Agent 并行完成，主线程集成验证。独立复审修复未知回执误清、错页显示、精确身份误翻译及旧手动任务按钮四项问题。合成 PostgreSQL 工作台 11 项与 Chrome 13 项通过，浏览器涵盖刷新后恢复、权限拒绝、预检后换账号、迟到响应、容量完整拒绝、市场条件、移动页面及存储不可用。详细回归结果见 `docs/evidence/ai-business-workbench-candidate.json`。完整容量的依赖与未实施方案见 [容量审查](AI_BUSINESS_ANALYSIS_CAPACITY.md)。

当前不是任意自然语言自动匹配店铺和规划数据源，尚未完成多店、多周期、真实经营口径与付费模型验收。没有新增数据库迁移、合入 main、生产查询、部署、服务启停或外部发送；五阶段仍在开发。

## 第十六批：目录与多卷底座（尚未开放入口）

`evidence_v2.py` 将来源目录与小型证据头分开；按精确身份排序、拒绝重复身份，48 项目录仍保留 64 MiB/2000 页事实档位。工作流引用只携带证据身份、版本、封存/目录摘要与问题，分页目录必须按可信目录重新核验完整序列，缺尾、重复、乱序和重新计算自报摘要后的篡改均拒绝。16/19/35/48 来源的合成合同演练中，1000 中文字问题的证据头为 3499 字节，工作流引用为 3398 字节；宽目录为 132296 字节，按 38k 页限拆成 13/13/13/9 项，最大页 36293 字节。字段支持与真实网店常量一致，但没有据此查询或证明经营数据可用。

候选迁移 `0019_business_source_directory` 只增加一张来源目录表，不为旧 run 加列、不改写旧计划、检查点或摘要。目录身份固定，检查点单独版本并绑定父任务版本；延迟约束要求同事务完整目录、连续序号、查询总字节有界、全源事实计数在旧上限内，封存前全部来源完成。新表同步最小读写权限、健康检查和备份清单；有 v2 数据时拒绝逆迁移。数据库不替代应用层的真实查询摘要与逐页事实重算；计数字段额度测试也不代表大事实载荷实测。

`volume_plan.py` 对完整表描述生成明确的卷号和半开行区间，保留空表、总行数和原始顺序，原生预算三页只占首卷。核验先按可信重建结果逐节点检查有界类型和结构，再比较摘要，避免先序列化不可信超大清单。`volume_files.py` 复用原双文件 writer，以每表单个迭代器连续消费所有片段；迟到多行、缺行、任一卷失败均不返回完整交付清单。单卷实际字节继续受限，超限整体拒绝，尚未实现按字节动态重拆。旧 renderer 1/2/3、线上文件路由和下载协议不因此改变。

独立复审已修复可变输出流/预算导致清单与文件不一致、伪造预算载荷、来源计数未核对实际 chunk，以及整数 JSON 接受小数/指数写法等问题。最终交付清单返回前再次读取全部文件核对摘要；数据库以实际分片页数、UTF-8 字节和连续序号对账，并通过同源/异源双连接并发提交测试。合成 156 表、26157 行按测试档位每卷 8 表/5000 行生成 21 卷、42 文件，全部文件摘要和 ZIP 成员通过；该卷数不是默认档位的预估。

最终隔离 AI 与业务 PostgreSQL 399 项：397 通过、2 既有跳过；0013→0019 升级、旧 56/60 表原行摘要、真实权限及独立备份恢复通过。Node 全量 2234 项中 2211 通过、20 跳过，3 项未修改的 Windows/Chrome 用例触及 30 秒时限；原文件顺序复测 50/50 通过，保留原失败证据，不宣称超时根因已修复。维护/备份 12 项通过；lint 0 错误、11 既有警告；类型检查保留 153 项基线错误且变更路径无新增，后端边界通过。详见 [第十六批证据](evidence/ai-business-capacity-foundation-candidate.json)。

来源 v2 的采集适配、轻量 Agent 专用工具面、持久多卷任务/下载、5000 行以上商品匹配、派生结果复用及游标续签继续开发。本批基础模块不能单独解除首次工作台的 12 来源或现有文件 120 表门禁，也不代表第五阶段或任何完整阶段完成。

## 第十七批：v2 来源采集与读取接入

显式 `schemaVersion="business-evidence-v2"` 创建独立目录任务，最多 48 个来源、只允许 bulk 取数，默认请求继续走原 v1。目录规范化后同事务保存全部来源；同一请求标识绑定目录、问题及初始自动采集选择，旧 v1 计划字节和幂等不回写。前端首次预览仍生成 v1，不自动替用户扩大范围或切换协议。

后台沿用既有有界调度、来源只读工具和 PageReconciler。每页完成后按父任务及来源版本重新校验，分片、来源检查点、事实字节、父版本及回执/审计原子提交。暂停、取消、撤权和迟到结果不得追加事实；恢复只从已保存检查点继续。所有来源完成后生成紧凑封存回执，绑定父计划、目录和每源身份、版本、检查点、行数、页数及字节。封存不是业务日期齐全证明，也不代表模型完成分析。

新 `GET business-evidence/{id}/sources` 按纯目录合同分页，`GET .../sources/{sourceKey}` 返回单源进度、元数据和核验结果。目录完整重建，不信任自报摘要；详情、来源状态和列表完成组装后复验父版本，变化则返回可刷新冲突，避免旧父版本混入新来源进度。分析表及现有 5000 行商品关联可使用 v2 封存数据，并继续逐页核验。v2 报告创建在任何工作流或模型活动前明确拒绝，旧 AI 元数据工具也拒绝把新版紧凑目录当完整来源；精确分块和分析表协议继续兼容。

所有版本事实字节与 v2 父头/封存回执/目录查询/检查点的 UTF-8 字节共同计入 owner/global 额度。无 v2 时保留旧额度行为；存在 v2 后，旧任务继续写入也不能绕过这部分占用。事实仍限 64 MiB/2000 页，单源检查点限 32768 字节；超限原子失败，不删减元数据后假装完成。无新迁移、生产查询、服务操作、付费调用或外部发送。

多 Agent 交叉复审修复来源名 `content` 被通用附件路由误分类、读取版本混合、坏目录使错误审计反复失败而无法暂停、旧工具误认新目录，以及共享列表中新版详情不能展示等接入问题。v2 失败审计使用独立父任务摘要，损坏目录可暂停且不阻塞下一任务；旧 v1 审计路径保持。验证结果与待办记录于本批候选证据，完成本批不代表完整阶段放行。

最终隔离 PostgreSQL 425 项（423 通过、2 既有跳过），Node 全量检查点 2236 项（2216 通过、20 既有跳过），后续路由/旧工具定向 11 项通过；旧页面 Chrome 13 项、新目录页面 8 项及独立重跑 8 项通过。冻结源码构建通过，类型检查仍为 153 项原有诊断、变更路径无新增；修正一个新增 Hook 依赖警告后定向 lint 零警告。详见 [第十七批证据](evidence/ai-business-v2-collection-candidate.json)。

## 第十八批：v2 轻量引用与独立多 Agent 执行

显式 v2 封存证据可经报告 API 创建固定五节点分析：店铺商品、推广、市场/B 端三个专业节点并行，再由独立复核与报告整合节点接续。工作流只携带证据 ID、版本、计划/目录/封存摘要、来源数量及问题；完整目录按页读取，不复制进 8000 字节工作流输入。固定执行身份仅由服务端创建路径选择，报告和工作流同事务落库；每次运行重新核对创建人、范围及固定引用。报告仍沿用原最终结构与人工复核流程。

新增独立 `business_agent_v2` 工具入口，只含来源目录和分析表。旧 39 个工具定义、72 组角色/范围/入口目录摘要逐项保持一致，普通任务与历史经营报告仍使用原工具入口和请求摘要。新目录工具固定页长 20 并按实际返回数续页；新分析表增加 38000 UTF-8 字节上限，旧工具字符上限保持。完整目录、依赖输出和分析页转录容量在创建前预检，实际运行仍受原模型、轮数、调用数及 192 KiB 转录上限约束，超限不会删页或截断。

每个 Agent 必须有自己的完整目录持久回执。核验绑定同一任务、模型派发、连续工具序号、参数与结果摘要、审计、封存版本及可信目录重建结果；缺页、重复、乱序、跨任务、伪造摘要或未知结果均不能补算成覆盖。校验每次只加载一份有界结果，深嵌套不可信对象不会先整体序列化。每次新模型派发前先核验已保存目录；已收到的模型答案即使本地校验失败，也保留原成功派发和回执，不回滚成未知结果。全部五节点完成、读取内容及人工复核时再次核验各自目录。目录证明只证明来源范围已读取，不证明完整事实已读或诊断必然正确。

两店、两周期合成 PostgreSQL 演练使用真实销售读取与比较计算，模型和工具传输被测试替身替代；三个专业节点实测同时处于模型处理，随后两节点接续、人工复核完成。验证包含五份独立目录证明、10 份工具回执、15 份模型回执及正负销售差额，无真实付费调用。交叉审查修复了错误目录先进入下一轮模型、中文分析页字节预检低估两项缺陷，并新增负向回归。

v2 预算与持久多卷文件尚未接入，创建时明确拒绝；工作台继续允许查看和控制采集，但暂不开放 v2 分析启动。首次预览仍为旧版。相关上限、真实数据与付费模型验收继续推进，不能将此批标为五阶段完成。最终检查见 [第十八批证据](evidence/ai-business-v2-agents-candidate.json)。

## 第十九批：统一封存读取与完整多卷文件适配

预算和文件制表共用 `business_sealed.Reader`，沿用旧版来源顺序和原始页字节，v2 从已封存目录逐源读取。构造时核验权限、父版本及封存状态，每页核对精确查询、来源类型、分页、人民币分单位、源版本、日期及行身份，完整消费后再次核对页数、源汇总和 v2 原文字节。市场店铺必须为空，品类、榜单范围及 SKU/SPU 口径分别核验；ERP 增加渠道核对；主数据按当前批次和快照核验，不能套用历史业务日期。来源摘要继续与固定封存结果相互核对，不把自报摘要当作独立权威。

内部预算解析已能复用 v2 封存证据，原对象筛选、同店不重叠维度、金额计算和返回字段保持；同一旧证据完整规范结果与原读取投影逐字节对照。v2 报告的固定预算入口和专用预算工具继续明确拒绝，尚未完成参数引用及模型读取证明接入。

`prepare_volumes()` 在有界 SQLite 临时空间中建立全部原始明细、逐维分析、本期/同比/环比、空表及报告内容，再按实际完整表描述生成分卷计划。`build_volumes()` 在同一准备上下文内单次消费，生成所有临时 HTML/XLSX 并重验全卷摘要；关闭准备包、重复调用、替换范围/计划/预算/报告绑定或任一卷失败均不返回完整交付。准备包以有序逐表摘要绑定列语义，避免将 156 张宽表一起展开后触及对象节点上限。原单表 100 万行、160 列、每文件 256 MiB 以及旧 renderer 1/2/3 保持，旧双文件入口拒绝 v2。

19 个真实 owning reader 合成来源生成 156 张表、2 卷共 4 个文件；浏览器验证完整导航、搜索、CSV、1440px 桌面及 390px 手机显示。新版独立布局修复长比较列名撑宽下拉框，旧 HTML 模板字节保持。最终相关 PostgreSQL 155 项通过。全量 Node 一项未修改的启动测试发生 IPC 反序列化异常，该文件独立复测 23 项通过；保留原失败，不声称全量全绿。

这一步接通的是封存数据到完整临时文件的内部链路，尚未新增持久卷分片表、后台任务、恢复及下载路由，不代表工作台已可下载 v2 多卷。正式数据和付费模型仍未验收。合成全源制表、文件与兼容性检查见 [第十九批证据](evidence/ai-business-v2-export-candidate.json)。

## 第二十批：持久多卷与恢复

新增 renderer 4 与 `0020_business_volume_files`：独立不可变卷分片表区分 attempt、卷号、格式和序号。卷 0 保存最多 16 MiB 的完整 JSON 交付清单，卷 1—100 各保存 HTML/XLSX；根记录只保存最多 128 KiB 的紧凑文件目录。旧 renderer 1/2/3、旧分片键和 ready 分支保持，存在 v4 任务或卷分片即拒绝回退迁移。新增表同步受限角色、健康检查、备份和域快照清单。

创建须显式选择 `deliveryMode=volumes` 并绑定当前账号；后台从固定已封存来源构建，期间不再查询业务源或调用模型。全部卷和完整 JSON 的实际文件摘要核验后才发布 ready。完整清单已经暂存时沿用原 attempt 重新核验；只有部分分片时开启新 attempt 重建整批，保留旧分片并继续计费。单文件 256 MiB、任务 1 GiB、用户 2 GiB、全局 8 GiB 不增加；临时输出按所有文件总高水位收费，并为 JSON 清单预留容量。数据表 spool 自身仍有独立临时空间限制。

数据库核验分片真实 SHA、追加状态、父版本、连续序号、全部 attempt 字节和完整目录结构；服务层另外流式重算整文件 SHA，并从完整表描述重新构建分卷计划核验表片段、来源/报告身份和内部摘要。两层职责不同，不能将数据库结构检查称为报告事实正确。暂停、取消、权限变化、审计失败和清单缺失均不得发布部分文件。

分卷读取路径为 `/api/ai/business-files/{id}/volumes/{volumeIndex}/chunks/{format}?sequence=1`；卷 0 只允许 JSON，普通卷只允许 HTML/XLSX。沿用当前 owner、管理员范围及报告绑定校验，旧分片路径拒绝 v4。工作台完整范围规划、固定预算和真实经营验收继续推进。

文件页面按报告协议显示多卷入口，用户选择单个 HTML、Excel 或完整 JSON 清单；下载逐块核验，再比对整文件与最新完整目录及账号。报告/账号切换中止并隔离迟到响应。独立审查修复布尔草稿标志参与绑定时变成整数的问题，以及切换报告后旧下载回调泄漏提示的问题。通用备份工具按 0020/0019 区分新 62 表和旧 61 表，保留更早备份版本。

## 第二十一批：首次 v2 工作台与完整合成链路

首次预览显式发送 `business-plan-request-v2`，服务端保留旧请求无版本字段时的 v1 响应。新预览完整展开精确范围，分别报告单来源查询、全部查询、小计划及轻量输入字节；不能构造完整计划时使用 null 表示尚不可测算。19/48 来源可预览，超过 48 或包含不支持条件时保留完整范围并拒绝创建，不自动删减。预览只是范围与表示容量检查，真实模型的调用及上下文准入仍在分析启动时执行。

工作台核验预览、请求及证据协议、来源与覆盖矩阵、容量上限和必要摘要。旧未确认请求继续保存并发送原始 body，不随新页面升级；v2 须已封存、具有原问题并收到服务端明确许可，才开放手动模拟或真实分析按钮。本批不自动调用模型，仍拒绝 v2 固定预算。

独立合成验收用真实领域读取器和实际业务服务完成：19 来源预览→采集封存→三个专业 Agent 并行→独立复核→报告整合→人工复核接口→持久两卷 HTML/XLSX 与完整 JSON 清单。模型输出为受控夹具；156 张表及 19 个原始来源表齐全，下载文件逐一核验摘要。此验收证明工程链路，不能代表真实模型的分析质量、店铺历史数据齐全或正式上线。

Chrome 共 25 项检查通过，覆盖超限完整显示、异常预览拒绝、原请求恢复、账号与任务切换、目录版本变化和移动页面。独立复审发现并修复前端校验器未接入提交路径的问题。完整记录见 [第二十一批候选证据](evidence/ai-business-workbench-v2-candidate.json)。本批无新迁移，未宣称五阶段或任一完整阶段已验收。

## 第二十二批：固定预算参数与可信读取底座

合法预算参数最多 48000 UTF-8 字节，可能大于报告快照的 32768 字节上限。候选迁移 `0021_business_budget_plans` 新增不可变参数表与报告 nullable 唯一外键，AI 自有表为 63 张；旧报告外键为 NULL，旧快照不改写。参数、工作流及报告须在同一事务提交，延迟完整性约束拒绝孤立参数，报告建立后不能补写或换绑参数。

参数规范 JSON 最多 48000 字节，绑定元数据最多 4096 字节；用户总量 8 MiB/200 条，全局 64 MiB/2000 条，历史记录继续计费。应用与数据库沿用同一全局版本行锁核验额度；数据库限制为 READ COMMITTED，锁等待后重新读取计费总量，防止并发越界。新表只授予 reader 查询、writer 查询和插入权限，健康检查覆盖外键、唯一性、写栅栏、不可变与两侧延迟完整性触发器。

绑定包含报告、owner/scope、证据版本、小计划、目录、封存、原始分析请求、预算参数和独立计算器版本。内部读取每次重新授权、复核完整封存并从确定性计算器重算，缺失或篡改不能降级为无预算报告。新预算页最多 38000 UTF-8 字节，只返回完整行前缀和精确 nextOffset，单行超限明确拒绝。

本批不开放新预算 Agent profile，不增加工具，不自动创建可执行预算报告。后续还须一起接通原子报告创建、三个独立工具、每个 Agent 的读取证明、实际上下文预检及多卷预算交付。100 个目标的参数上限不等于模型准入承诺；当前工作台继续明确显示 v2 固定预算尚未开放。

隔离升级与独立恢复验证了旧 62 表原列摘要、原报告 NULL 外键、真实角色门禁、参数与快照原字节及恢复后的完整证据重算。11 项数据库存储测试包括真实 64 MiB/2000 行、用户额度和双连接竞争。完整回归与修复后的夹具复测结果见 [第二十二批候选证据](evidence/ai-business-budget-storage-candidate.json)。

最终隔离升级、旧表摘要、受限角色、六块独立恢复与逆迁移拒绝通过；AI 408 项中 406 通过、2 项原有跳过。首轮 Node 两项备份失败已修复，相关 12 项回归通过；首次全量结果仍保留。新旧下载浏览器各 5 项、实际合成文件校验、生产构建与页面检查通过。该批无生产读写、迁移、发布、付费调用或通知，见 [第二十批证据](evidence/ai-business-persistent-volumes-candidate.json)。

## 第二十三批：固定预算多 Agent 执行与文件闭环

在第二十二批不可变参数基础上，新预算报告使用独立 `business-agent-budget-reference-v1` 协议和 `business_agent_budget_v1` 工具入口。参数、工作流和报告原子创建；同一请求可查回原结果，修改参数须创建新版本并保留旧报告。原 v1、无预算 v2 工具定义和目录摘要保持原样，本批没有新增数据库迁移。

五个 Agent 各自读取完整来源目录；推广、独立复核和报告整合必须读完全部固定预算页，其余 Agent 一旦开始读取预算也必须读完。完成、人工复核和文件发布均从本 Agent 的持久派发及结果重新核验，拒绝跳页、串用回执、摘要变化及未知结果自动重放。新预算分页固定 20 个目标，最多 38000 UTF-8 字节，完整行不足一页时返回精确下一偏移量。

模型调用前按实际目录、预算页、模型协议及最坏依赖转义检查输入、调用数、轮次和上下文。所有节点输入不超过 24 KiB，模型上下文不得通过删除已读页面压缩；过大的合法参数计划可存储但不能绕过执行容量。五 Agent 文字输出采用有限篇幅，完整明细由确定性计算及文件服务交付。

预算文件沿用 renderer 4 持久多卷。首卷提供离线 HTML 试算和三张原生 Excel 预算公式页，清单固定预算参数摘要。创建、恢复和发布重新核验预算及分析回执；逐块下载仅核验不可变绑定，不重复扫描全部事实数据。初次预算工作台选择与试算入口仍在后续接入，本批开放的是报告 API 和完整执行链路。

真实领域读取器、封存服务、五 Agent 调度、独立与人工复核、文件生成及下载组成合成验收；仅模型和网络边界使用夹具，不证明真实模型经营判断质量。测试与限制见 [第二十三批候选证据](evidence/ai-business-budget-execution-candidate.json)。

## 第三十八批：完整五 Agent 容量预览

固定新版 graph、结构化诊断与完整动作字段，按五角色全部包页、固定预算、实际工具帧和依赖最坏转义计算上下文、调用及轮数。11项纯测试通过；小样例可容纳，55分区低候选样例最终报告204916字节，超过192 KiB，必须拒绝且不裁剪。此预览不授予运行资格，尚未注册新工具或执行入口，详见 [容量预览](AI_BUSINESS_SCREENING_PREFLIGHT.md) 和 [证据](evidence/ai-business-screening-preflight-candidate.json)。

## 第三十七批：五角色无损阅读包

固定筛查可以转为五角色各自的完整必读包：全局来源、覆盖、计数和缺口一致，候选按实际来源职责分配，独立复核与整合保留全部候选。固定列和字典减少重复信息，解码逐值还原；单页最多100条/38000 UTF-8字节，完整前缀与真实下一偏移可验证。

内部服务从真实持久结果构造，分页前后复权，不重扫事实；63项纯回归及8项真实PG链路通过。实际工具帧探针的小例复核/整合各4页约148KB，大例12页约486KB仍超限；小例也尚未计完整指令、依赖、预算与token。未注册新工具、报告协议或Agent已读证明。见 [角色包说明](AI_BUSINESS_SCREENING_PACKAGES.md) 与 [候选证据](evidence/ai-business-screening-packages-candidate.json)。

## 第三十六批：推广计划与单元的真实封存接入

内部服务现可从授权报告的完整目录读取京东推广计划、单元和匹配方式三种视图，显式选基期并核对全部事实；来源完整退出后再检查权限，才返回分页或精确行引用。含绑定的整页不超过38000 UTF-8字节，超量只取完整行前缀，下一偏移按实际行数返回。

8项隔离PostgreSQL测试通过，包含真实reader角色的三视图/基期/精确行回读及业务表和写入权限拒绝。纯计算与相关筛查回归75项通过，独立长中文分页及最终撤权复审通过。仍未注册公开工具、报告视图选择、规则或文件；天猫当前计划粒度不可还原的限制保留。见 [候选证据](evidence/ai-business-promotion-owning-candidate.json)。

## 第三十五批：筛查结果持久复用

完整成功筛查现在可以由可信内部对象原子保存，并由后续使用者按固定页复用。读取重新核验账号、报告、来源与封存绑定，不重复扫描事实；不同 Agent 的读取证明仍须分别建立。候选、遗漏数量及覆盖缺口完整保存，没有把保留候选改称全部异常。

迁移0023新增两张仅追加表，AI自有表增至65；单次16MiB、单用户64MiB/20份、全局256MiB/200份，计入全部页与元数据。数据库核对完整页链、真实字节和并发额度，审计失败全部回滚。真实受限writer演练发现的父行锁权限问题已修正，未扩大表权限；升级与独立恢复核验65表摘要、旧报告和新页，恢复读取不重新计算事实。实现见 [固定存储](AI_BUSINESS_SCREENING_STORAGE.md)。

本批没有注册新Agent工具或执行协议。下一步为五角色构造无损必读包，再接模型容量准入、各自回执和交付文件，不能把存储验证当作五阶段完成。

## 第三十四批：推广计划和单元派生视图

新增京东推广计划、单元及单元匹配方式三种纯计算视图。按平台、店铺和完整父级身份聚合，两期金额、空值与控制合计守恒；缺身份记录保留核查桶，不冒充同一实体比较。来源流全部核对后才可读取临时表，页内完整字段保留，超容量拒绝。天猫目前多计划已按商品日合并，本片明确不支持计划粒度，不能反推分摊。

20项新增测试及12项旧聚合回归通过；独立90组两期随机守恒检查通过。半途停止扫描的Windows文件占用已修复，多个未读完游标、调用方异常和临时目录实际删除均复验通过。旧八维、工具、Agent及文件协议保持原样；三视图仍需下一片接入真实封存服务、引用与文件，见 [候选证据](evidence/ai-business-promotion-views-candidate.json)。

## 第三十三批：固定范围的内部完整筛查

筛查计划从报告固定问题、完整封存目录及关联参数推导，保留每个来源、维度、窗口和缺口；不让模型自行略过问题来源。7来源样例枚举40表55规则分区；10来源65分区超限时保留全部计划并在取事实前拒绝。市场规则未实现、缺基期或缺关联均单列，不作已扫描处理。

内部服务实时复验报告、工作流、账号、目录与封存绑定，消费原生及映射完整表后才返回进程内结果。分页保留完整记录，最多20项/38000 UTF-8字节，前后重新授权。计划覆盖、实际表执行和来源日期完整分开标记；空表或缺日不会被误报为日期完整。结果尚未持久化或接入Agent工具、读取回执和交付文件。

62项纯计算/映射回归及18项隔离PostgreSQL新旧报告测试通过。首次PG发现映射表检查点元信息不同于原生页，已按真实合同修复并删去掩盖差异的测试字段；最终重跑通过，独立复审无剩余阻断。见 [第三十三批证据](evidence/ai-business-screening-integration-candidate.json)。参考文件结构与六张诊断验收卡分别记于 [质量对照](AI_BUSINESS_REFERENCE_QUALITY.md) 和 [诊断验收卡](AI_BUSINESS_DIAGNOSTIC_ACCEPTANCE.md)，均不代表真实模型质量已通过。

## 第三十二批：从已导入来源选择工作台范围

工作台新增可选网店来源选择器，默认收起，原手工填写流程保持可用。每页显示 20 个精确来源，支持筛选、前后分页及从首页重读；只在明确点击时添加对应数据集，同店去重合并，跨平台同名保留为不同店铺。仅初始空白占位可替换，ERP 渠道、市场条件、日期与窗口不自动改变；四店上限明确拒绝，不显示虚假的添加成功。

AI 只读适配要求当前账号摘要，并使用同一实时 principal 签名读取网店元数据；账号不符在领域请求前拒绝。来源页的完整字段、摘要、身份、日期及排序经过核验，添加前重读同页并核验版本。筛选变化、取消、禁用、卸载和切账号均隔离迟到结果；父级按最新表单合并，不按旧行下标覆盖。范围变更撤销旧预览，结果未知的提交继续锁定原 body 与请求编号。

52 项 Node、68 项新旧 Chrome 检查、构建及 20 项页面验证通过；全库类型检查与既有 223 行基线逐行一致，变更文件 lint 无错误。独立 Agent 复审和手机视觉检查通过。本批没有 ERP/市场来源目录、自然语言自动匹配、数据完整性或真实业务效果承诺，见 [第三十二批候选证据](evidence/ai-business-source-picker-candidate.json)。

## 第三十一批：全表异常筛查纯计算底座

新增独立筛查器，逐行消费原生或商品关联的完整结果表，计算七类推广、退款、毛利、费用及流量成交变化规则。各表、各规则分别统计扫描、可判断、不适用、命中、保留和遗漏数量；保留稳定排序的有限候选，完整行摘要仍覆盖未入选的尾部记录。不同维度和规则可能命中同一事实，金额不得相加解释为总损失。

只有所有行消费完且全部来源上下文正常退出后才返回结果；尾部损坏、退出失效、容量超限不返回部分成功。日期覆盖及实际比较区间随结果保留，单期缺日仅描述已观察记录；来源日期完整不证明每个 SKU 每日完整，闰日同比仍是区间总量比较。规则只识别数值现象，不证明因果，市场来源在本版明确列为规则不支持。

该模块只返回 `prepared_unpublished`、`authorityVerified=false`，没有授权能力、发布接口或模型调用。后续必须由 owning service 固定完整范围、核对封存及权限，再接入新的 Agent 协议与文件披露；现有 Agent 尚未使用此全表筛查。18 项新增纯测试与 18 项旧回归通过，包括真实原生及映射引擎兼容，见 [筛查设计](AI_BUSINESS_DIAGNOSTIC_COVERAGE_DESIGN.md) 和 [第三十一批证据](evidence/ai-business-diagnostic-screening-candidate.json)。

## 第三十批：精确网店来源选项接口

新增只读 `GET /api/netshop/analysis-options`，从已完成导入批次的标量元数据返回精确平台、店铺及分析数据集身份。数据库内去重、按同一排序规则进行游标分页，每页固定 20 项；不读取销售事实、原始 JSON、文件名或导入人。接口继续要求无范围限制管理员，独立注册于 reader，writer 不提供该路由。

签名游标绑定账号、筛选、目录版本及上一身份，过期或来源变化必须从首页重读。日期只表示历史发布批次包络，明确 `coverageVerified=false`；身份曾出现不证明当前事实或所选期间完整。未知与重复参数、非法元数据和完整页超出 38000 UTF-8 字节均明确拒绝，不截断身份。

网店后端新旧接口 21 项隔离 PostgreSQL 测试、前端签名桥与旧 reader 11 项测试通过。本批尚未接入工作台选择器；ERP 精确渠道投影、市场真实筛选组合、自然语言范围建议继续开发。没有新增模型工具或迁移，见 [范围选项设计](AI_BUSINESS_SCOPE_OPTIONS_DESIGN.md) 和 [第三十批候选证据](evidence/ai-business-source-options-candidate.json)。

## 第二十九批：单次完整复核内复用已核验计算

集成报告每次 `content()` 校验建立独立、显式传递的复用范围，仅保留已完整计算并正常退出来源上下文的规范分析页或预算结果。每次命中前后和范围退出仍重新授权、核验固定报告、目录、封存及参数绑定；五个 Agent 的派发、摘要、顺序、成功审计和完整性证明分别检查。缓存不存权限、Agent 完成状态或未经重算的工具回执。

真实合成五 Agent 验证中，同一映射页重算 3 次降为 1 次、原生页 2 次降为 1 次、预算 6 次降为 1 次；完整内容和五份证明的规范字节相同。下一次复核重新计算，文件创建、恢复和发布各自使用新范围。固定最多 64 项、4 MiB 规范载荷与键/摘要计费，超缓存额度回到正常计算，不提高业务容量；这不是进程 RSS 上限。

此批没有数据库迁移，也不包含跨请求缓存、完整导出跨维度复用或模型读取全量异常。42 项隔离后端测试及纯边界测试通过，详见 [复用设计与边界](AI_BUSINESS_DERIVED_REUSE_DESIGN.md) 和 [第二十九批候选证据](evidence/ai-business-content-reuse-candidate.json)。

## 第二十八批：工作台显式选择商品关联

已封存的 v2 任务可逐个选择 ERP 销售对应的同平台、同店铺当前商品主数据，分别覆盖本期、环比和同比来源。界面先验证完整目录、页摘要、来源顺序与任务身份，不预选或猜配；未选择时保持原报告路径。普通分析和固定预算分析均使用同一组明确选择，提交后冻结完整参数与请求编号。

账号/任务切换隔离旧选择；同一任务版本、目录变化或重读失败时保留“原本要求关联”的意图，不能自动生成无关联报告。重新核验后须重选或明确清空。请求保存前检查完整 UTF-8 大小及会话记录长度，超量不截断、不发起请求，也不把正常容量拒绝当作浏览器存储损坏。

完整组件、新旧工作台浏览器、Node、实际封存读取与模拟后端流程、构建和页面回归通过；根 Agent 检查了桌面展示，并精简实现用语。真实模型效果和正式采用仍未验收。记录见 [第二十八批候选证据](evidence/ai-business-mapping-workbench-candidate.json)，后续按 [剩余开发与验收清单](AI_BUSINESS_REMAINING_ACCEPTANCE.md) 继续，批次数不等于原五阶段数。

## 第二十七批：商品关联、预算与五 Agent 文件闭环

显式传入 `mappingPairs` 的 v2 报告使用新的集成执行协议。固定关联计划与可选推广预算在同一事务内绑定报告；每个 Agent 独立核验完整来源目录，商品、独立复核和整合节点分别读取固定映射分析，预算必读节点读完固定预算。原生与映射行引用均重新计算核验，ERP 销售不混作推广归因或情景收益。

新增三个只读工具及独立容量预检，旧 44 项工具定义和全部旧 surface 目录摘要不变。回执的摘要正确仍不代表数值可信：后续调度会从固定来源重新核对成功分析页，篡改结果使任务失败且保留原模型和工具回执，不自动重复调用。

多卷文件包含每对来源的完整关联分组、SKU/SPU 汇总、兼容的同比/环比和来源证明；有预算时继续提供首卷公式与离线试算。三项关联计划/算法标识作为清单整体绑定，缺一或篡改均拒绝。旧报告的 HTML、XLSX 和清单规范字节保持一致。首次工作台商品关联选择另在后续批次接入。

迁移 0022 不增加表，AI 仍为 63 张自有表。隔离升级及独立恢复检查旧行摘要、受限角色、函数/触发器健康、有无预算的两类新报告恢复读取，以及存在新数据时拒绝逆迁移。五 Agent 的合成端到端覆盖真实领域读取、封存、调度、独立与人工复核、持久文件及完整下载；模型和网络边界使用夹具，未验收真实模型判断或生产经营效果。完整结果见 [第二十七批候选证据](evidence/ai-business-integrated-report-candidate.json)。

## 第二十六批：固定商品关联计划与映射分析

新增严格的关联计划协议，最多 47 个显式销售/主数据配对、16000 UTF-8 字节；来源均从完整封存目录核验，计划摘要只绑定选择与算法，必须与目录、封存及两端来源绑定共同使用。同比环比要求同平台、店铺、渠道、原始日期区间，并复用同一当前主数据；当前主数据回溯历史金额不代表历史真实商品归属。

内部映射分析服务提供完整 SKU/SPU 汇总，保留 11 项 ERP 指标、销售行数、歧义和未匹配金额。SKU 跨 SPU 重新合并，禁止倍乘金额；缺身份、缺日期或缺侧不计算比较，零/负基期不计算增长率。允许独立导出基期表；空来源保留空结果，不制造零销售行。当前与基期关联串行执行，各自最多 128 MiB，与派生分区合计不超过 256 MiB。描述来源不扫描事实，每个来源配对只完整读取一次。

内部引用解析会重算指定行的数值，保留当前主数据与销售绑定，拒绝失效行、非 ERP 指标和不可用增长率。旧诊断入口继续拒绝新映射引用。此批未注册新的公开分析接口、未启用集成报告 profile，也未迁移数据库；下一步按 [集成协议设计](AI_BUSINESS_ANALYSIS_INTEGRATED.md) 接入工具、报告和文件。测试见 [第二十六批候选证据](evidence/ai-business-mapped-analysis-candidate.json)。

## 第二十五批：完整封存上的分区商品关联

新增独立只读 `mapping-v2` 接口，显式选择同一平台、店铺的 ERP 销售与本期商品主数据。两个来源全部读取并核对后，才返回按稳定身份分页的关联结果。使用临时 SQLite 分区，维持 64 MiB / 2000 页事实、25 万组和 256 MiB 临时盘边界；页内完整行不超过 38000 UTF-8 字节，含证据绑定的完整 API 响应不超过 65536 字节。关闭或中途停止扫描时释放游标并清理 Windows 临时目录。

关联仍按网店规格编码精确匹配商家编码，相同候选去重，歧义与未匹配金额单独保留，不复制销售给多个 SKU。当前主数据只代表当前映射，不能冒充历史商品归属或关键词利润。旧 `mapping` 的 5000 行限制、返回协议和报告行为保持原样。

隔离 PostgreSQL 验证实际领域读取器、52 页持久证据和封存后的 5001 行关联，旧接口明确拒绝、新接口全量核对通过；纯计算验证每源 30001 行，另由独立 Agent 复核旧口径一致性。新接口尚未进入 Agent 报告或多卷文件，后续须先固定关联计划再接入分析、复核和导出。验证见 [第二十五批候选证据](evidence/ai-business-product-mapping-candidate.json)。

## 第二十四批：从封存目标配置首次预算

工作台已封存的 v2 任务可以逐页浏览完整来源目录，从本期推广来源选择店铺、品类、SPU、SKU、关键词或搜索词的真实分析行。目标保存服务端返回的稳定身份，不要求用户填写行 ID；缺失身份不可选，同店铺的重叠来源或不同聚合维度不能混选。来源与目标页面核验任务版本、目录及封存绑定，跨账号、切换任务和迟到响应不能混入当前选择。

金额、权重、上下限、观察条件及情景因子初次均为空，用户明确填写后才能免费试算；贡献毛利率可留空表示未知。最多 100 个目标、5 个情景、48000 字节完整参数，超量不截断。改动目标或参数立即使旧试算失效，提交前再核对参数及摘要。试算不保存参数、不调用模型，也不表示模型上下文一定足够。

新增目标 GET 和试算 POST 均走 AI reader；每次完整复核封存来源，目标页最多 38000 UTF-8 字节，试算最多 2 MiB。选择与预览通过后，模拟或正式分析继续使用原工作台的账号绑定、持久请求编号和完整 body；提交结果未知时刷新后确认同一次请求，不另建预算版本。正式分析仍须手动点击，报告和参数由第二十三批服务原子创建。

验证记录见 [第二十四批候选证据](evidence/ai-business-budget-builder-candidate.json)。与前批相同，合成验收不代表真实经营效果或生产采用。

## 验证方式

在独立 worktree 中运行：

```powershell
& '.runtime\test-venv\Scripts\python.exe' -X utf8 tools/ai-postgres-rehearsal.py --tests-only --test-label business_analysis --test-label netshop --port 55485
& '.runtime\test-venv\Scripts\python.exe' -X utf8 tools/ai-postgres-rehearsal.py --tests-only --test-label ai_assistant --test-label business_analysis --test-label sales.tests.test_analysis --test-label sales.tests.test_consumers_api --test-label netshop --port 55485
& '.runtime\test-venv\Scripts\python.exe' -X utf8 tools/ai-postgres-rehearsal.py --upgrade-only --business-evidence-upgrade --port 55485
node --import tsx --test tests/business-analysis-tools.test.ts tests/django-netshop-service.test.ts
node --import tsx --test tests/django-postgres-maintenance.test.ts tests/django-sales-consumer-reader.test.ts
npm run build
npm run test:unit
node tools/check-django-production-boundary.mjs
```

PostgreSQL 启动器使用独立随机目录、凭据、数据库与 55440—55999 端口，禁止在正式检出运行。`--test-label` 只能用于 `--tests-only`，不能缩减迁移升级演练的验证范围。`--upgrade-only` 执行完整选定升级及恢复，测试另按有界套件串行运行；每个子进程仍限制300秒。测试中的 B2B、金额和店铺均为合成数据。

首批无数据库模型或迁移，验证见 `docs/evidence/ai-business-analysis-foundation-candidate.json`。第二批包含迁移与权限变更的候选源码，已在隔离 PostgreSQL 演练升级、旧 56 表行摘要不变、真实角色权限、终态保护和独立 dump/restore；验证见 `docs/evidence/ai-business-analysis-evidence-candidate.json`。两批均未重启生产、调用付费模型、创建真实报告或发送通知。
