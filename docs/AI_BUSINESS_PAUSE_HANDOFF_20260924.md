# AI 经营分析续工暂停交接（2026-09-24）

本清单是开发交接，不是五阶段终验或生产采用记录。用户要求“运行完小阶段后，暂停任务，并写交接清单，然后 commit、push”。本次仅收束已开始的 0043 封存消费门禁和 0044 市场 v2 停放报告，不再启动后续阶段。

## 工作区与采用状态

- 隔离工作树：`D:\.codex\worktrees\ai-business-current-integration\运营管理系统`；分支：`codex/ai-business-current-integration`。正式主 checkout `D:\运营管理系统` 的用户未提交文件未触碰。当前分支包含 `origin/main` 的 `4ad06070` 基线，暂停前须提交并推送本清单及本批代码。
- 所有数据库演练均在本工作树 `.runtime/ai-pg-*` 的一次性本机 PostgreSQL 中进行；未部署、迁移或重启正式服务，未导入真实业务数据、调用付费模型、发消息或执行投流。
- 曾用于 30 分钟后继续任务的 `30-ai` heartbeat 已按用户要求删除；其他数据工作流监控与备份自动化未改。

## 这批已完成并验证

| 子片 | 可用边界 | 验收证据 |
| --- | --- | --- |
| 0043 v4 封存消费候选 | 77 张 AI 表；新 seal 必须在同事务有消费回执，旧封存行保留但缺回执时内部验签拒绝。专用角色仍 `NOLOGIN`，旧直接封存被撤权，没有应用验 MAC 的提交包装，因此当前无法产生正式新封存。 | `.runtime/ai-pg-012deaba565e/tests.log`：组合 32 项通过、8 项历史接口跳过；`.runtime/ai-pg-c66f6d5a6ef4/business-v4-seal-consumption-upgrade-evidence.json`：0042→0043、旧 renderer 1–7 字节、前后备份恢复与空回执逆迁移通过。纯备份 19 项、维护 Node 14 项通过。 |
| 0044 市场 v2 停放报告 | 新画像只存可由数据库复核的旧封存报告根、双市场来源/观察日、价格带、固定图与候选工具顺序；真实 `allowed_tools=[]`，工作流 `paused/market_material_not_admitted`，无节点/Agent、模型、公开路由或文件。三张派生市场表摘要未被写成数据库权威。 | `.runtime/ai-pg-7bd506abd6a3/tests.log`：5 项隔离 PostgreSQL 通过。**尚未做 0043→0044 专属升级、前后备份恢复与完整健康门禁验收。** |
| 同报告内部材料 | 市场三张类型表和财务自然月材料均有同一封存根/来源完整重放候选；市场 v2 无逐页独立签名工具收据，财务 v3 超过 64 页/8 MiB 整份拒绝。市场新画像创建候选仍不授予派发权。 | 市场材料与财务材料分别在前述隔离 PG 组合测试通过；状态和口径见 `AI_BUSINESS_INTEGRATION_STATUS.md`。 |

## 恢复后的第一批顺序

1. 先检查 `git status`、`origin/main` 是否前进，以及正式主 checkout 的用户改动；不要把旧测试证据当作新主线验收。对 0044 补独立 0043→0044 升级/逆迁移、备份恢复、旧报告文件字节和权限/健康检查；保留本次失败日志及修复记录。
2. 市场 v2 在停放报告上增加**拥有方三表完整材料准入**和真实第五工具注册，再接同一 Agent job/providerCallId 的持久读取回执、数值引用、独立复核、人审和版本化 renderer8。没有本人真实派发结果前，`agentReadPersisted=false`，不得把预览或人工构造记录当作证明。
3. v4 封存需受保护应用真实 HMAC 验证、同事务消费/提交及未知结果查询；再解决 180 秒固定租期与参考推广 575,095 行的续租/断点和磁盘峰值，之后才考虑专用凭据、AI reader 签名句柄及正式 Agent/文件接线。0043 数据库目前只验证 MAC 形状，不能把旧 `sealed` 或消费候选当作权威。
4. 仍缺可信店铺自然日总览及**完整区间去重 UV**字段/日期/重复导入合同；B 端与 ERP/平台销售包含关系未核清。财报不按日/SKU分摊，市场 TOP 样本不归本店销售，不跨来源相加。待这些口径和店铺/品类/SPU/SKU/关键词三期来源接入后，再做真实模型六卡、原生 Excel/浏览器、至少两店的业务对账及受控生产采用。参考 XLSX 约 42.7 MB，30 个 sheet XML 解压约 432.6 MB，不能只以压缩大小估算容量。

## 暂停边界

本次交接不授权下一轮发布、正式迁移、凭据激活、真实业务补跑、付费模型调用或消息发送。恢复时以用户的新指令和当时主线状态为准；`30-ai` 不要自动重建。所有未完成项继续明确标为未验收，五阶段目标尚未关闭。
