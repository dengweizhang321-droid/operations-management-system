# 0073 隔离 shadow snapshot 候选

这是**默认关闭、仅合成数据的测试脚本**，入口为 `tools/protected-ai-shadow-snapshot-0073.py`。本轮实现该脚本的冻结 0073 runner 显式接线与纯测试；前七次隔离 PostgreSQL 在 shadow 快照前拒绝，第八、九次已完成加密与事务恢复、但最终根比较拒绝，**没有完成跨集群一致性验收，也未修改正式备份、恢复、PrepareApp、迁移或旧 stream-v1 字节**。脚本的 `status=passed` 只有未来在受控隔离源与第二新集群实际跑完且所有根一致时才可能生成；当前没有该结果。

脚本仅接受 `.runtime/ai-pg-<12位十六进制>`、loopback 的 `teruisi_ai_rehearsal`/`ai_rehearsal_admin`、精确 0072→0073 合成升级证据、不同的隔离目标端口，以及显式 `--enabled`。缺任一条件先拒绝。执行时要求源私钥表原本为空，再插入一条随机合成 verifier key；源库因此只是可丢弃测试库。要求已有 HTML 和 XLSX 非空 file chunks，逐块独立复算 SHA-256，受限计数和总字节。全库源证据、9 个 protected 表行根、含 13 个受保护角色的全局角色清单及成员关系、文件根都从同一只读 repeatable-read 导出快照读取；快照 ID 的摘要和各根进入版本化加密上下文。`pg_dump --snapshot` 标准输出直入现有 v2-stream-v1 AES-GCM 封装，使用**进程内新生成的随机 256 位密钥**，不落地明文 dump；先验证错误密钥拒绝，再以同一加密文件双遍认证并向第二新集群的 `pg_restore --single-transaction --exit-on-error` 送入。恢复后逐项比较原库 content 根、protected 行根、文件根、全局角色，并调用 0067–0073 的既有目录验证。测试还要求正式备份入口在产生归档前以精确受保护迁移原因拒绝。

这不是正式备份路径：随机密钥没有托管、轮换、授权、灾备或重启后获取机制，进程结束后**无法长期恢复此归档**；`formalBackupPathVerified=false`、`longTermKeyCustodyVerified=false`、`productionWrites=false`。现有正式备份仍采用 `--no-owner --no-privileges`，在受保护迁移收据出现时提前拒绝；正式恢复仍拒绝 protected TOC。旧无保护历史备份的兼容性只因正式代码未改而保留，尚需在独立旧库做前后恢复实测。测试身份 `ai_rehearsal_admin` 是隔离集群管理员，不证明正式受控 operator、密钥管理员或生产授权链。

本冻结源码工作树以提交 `1bd52362` 为基线，0067–0073 迁移与既有 stream-v1 实现未改。`ai-postgres-rehearsal.py` 仅在同时显式提供 `--business-protected-shadow-snapshot-0073 --upgrade-only --port <源隔离端口> --shadow-target-port <不同目标隔离端口>` 时启用：新集群迁移前经 `protected-ai-shadow-role-preflight-0073.py` 只创建缺少的六个 AI、网店、财务证据角色；已存在者必须是 NOLOGIN、NOINHERIT、无密码/成员/直接依赖/角色设置，漂移整事务拒绝，不修复。普通 runner、正式 helper、sales/market 角色与原权限入口不改。随后执行现有 0072→0073 全链升级，于精确 seed 写出且源集群仍运行时调用 shadow。runner 要求旧文件块计数为 7；返回后又验证 HTML、XLSX 均在、恢复行根/owner/ACL 成功且 authority 标志仍关闭。源数据库密码由本次 runner 随机生成并经现有测试环境交给子进程，不读取、重置或重启任何历史 runroot；原 runner 的临时密码文件仍由原 finally 清理。其他 runner 模式和旧 0072 路径不走此 hook。

首轮在源 55824、目标 55825 的独立运行于 0072→0073 seed 通过后失败；7 块旧文件、前后恢复、空逆迁移与正式备份早拒均有回执，但**未进入 exported snapshot，也未生成密文或第二集群恢复证据**。原因是 shadow 原版 `_native` 对 Windows `pg_ctl start` 使用 PIPE 捕获，PostgreSQL 常驻子进程继承 PIPE 后，`subprocess.run` 等待 EOF；目标数据库实际已启动。精确终止本次 shadow 子进程后，原 runner 停源、单独对本次目标目录执行 `pg_ctl stop`；双 `pg_ctl` 为 no server、双端口无监听、两份临时密码文件均清理、shadow 明文 dump 数为 0。旧历史 runroot 未触碰，首轮失败证据在 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\shadow-0073-55824-55825-20260926`，SHA 清单保留。

冻结树的 shadow 脚本最初从整合树逐字节复制；首轮后将 `_native` 标准流改为临时**文件句柄**，以免继承 PIPE 阻塞。第二轮源 55840、目标 55841 的 0073 seed 再次通过，并实际进入源 `collect_evidence`，但冻结 runner 缺 `teruisi_netshop_reader`，正式 helper 在网店来源 marker ACL 检查报 `UndefinedObject`；未生成 exported snapshot、密文或恢复证据。双集群自然停机、双端口无监听、两份临时密码文件清理、shadow 明文 dump 为 0。第二轮失败证据在 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\shadow-0073-55840-55841-20260926`。因此追加上述六角色**仅隔离测试**前置，shadow 自身也回读其目录属性；第三轮未越过身份门禁，不能把任一旧 seed 充作成功。下次启动前必须重新获串行资源放行，确认两个**新端口**无监听、D 盘至少 12–15 GiB 剩余并记录源码/脚本 SHA；运行须回读新 runroot 的 `shadow-snapshot-0073/evidence.json`，核两个集群均已停机及没有明文 shadow dump，再归档非敏感证据摘要。不得将此路径与含 0074+ 文件的 test-only installer 混称为同源验收。

第三轮源 55844 的六角色 preflight 在迁移前以 `database identity drifted` 拒绝，目标 55845 从未创建；源自然停机，双端口无监听、临时密码清理、无明文或密文 shadow 文件。失败日志未包含数据库身份原值，不能断言实际地址是哪一种表现。只读本机 PostgreSQL 17.11 目录确认 `pg_authid` 不含 `rolconfig`，角色设置另由 `pg_db_role_setting` 检查；后续代码已据此修正。身份读取改为精确识别 `127.0.0.1`、`127.0.0.1/32`、`::1`、`::1/128` 和**仅映射 127.0.0.1** 的 IPv4-mapped IPv6，错误仅记录数据库/用户/回环/端口/恢复状态的布尔检查结果，不输出密码或连接串；非回环、错库、错用户、错端口、恢复库仍拒绝。第三轮失败证据在 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\shadow-0073-55844-55845-20260927`。此身份修订仅通过纯测试，尚未得到新一轮 PG 证据。

第三轮后又静态核到 runner 的通用 `environment.PGDATABASE=postgres`，而角色 preflight 的 `psycopg.connect(dbname=teruisi_ai_rehearsal)` 显式连接业务测试库；两者此前故意不一致以服务 `createdb`，但不应传给同一身份证明子进程。现仅显式 shadow 子进程改为 `PGDATABASE=teruisi_ai_rehearsal`，保持 runner 原通用环境和普通模式不变；preflight 同时要求环境与显式 dbname 一致。成功回执也只包含五项身份布尔值，runner 必须全部为 true。旧日志无法证明不一致就是唯一根因，下一轮真实隔离结果仍为必需。

第四轮源 55850、目标 55851：六角色前置成功，五项身份布尔均为 true，角色原先为 0、创建 6；0073 seed 和 7 块旧文件也再次通过。shadow 在插入新合成私钥之前的 `_roles(source)` 读回六角色时，`verify_catalog_rows` 拒绝迁移后的**角色属性**，并非依赖/ACL 数量检查；未创建目标目录、未导出快照或密文。源自然停机，双端口无监听、临时密码清理、shadow 明文 dump 为 0。失败证据在 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\shadow-0073-55850-55851-20260927`。原失败日志没有具体字段，静态检查当时尚未定位到由前驱演练调用的角色采用函数，因此未放宽约束；只追加按角色、按属性**布尔**的失败诊断，不回显密码或原始目录行。第五轮随后确定了正常角色采用路径。

第五轮源 55853、目标 55854 的布尔诊断明确显示 `teruisi_ai_reader` 仅 `noLogin=false`、`passwordNull=false`，其余属性通过；源 0073 seed 仍通过，shadow 在私钥插入前拒绝，目标未创建、双端口停、临时密码清理。静态追踪到既有 `business-promotion-profile-upgrade-rehearsal.py` 调用 `database_contract.provision()`，它有意将 AI reader/writer 升为独立随机密码的 LOGIN，并配置 reader 只读、writer 可写及 15/30 秒超时。这是正常测试角色采用，非被保护目录漂移。第五轮失败证据在 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\shadow-0073-55853-55854-20260927`。现把迁移前六角色 NOLOGIN/null/零依赖与迁移后两 AI LOGIN/passwordPresent、四网店/财务仍 NOLOGIN/null 的不变量分开；后者还核精确角色设置及 `database_contract` 全表权限/模型 19 列。目标第二集群给两 AI 角色**另生成独立随机测试密码**，不复制或存储源密码/哈希，并重建其三项角色设置；恢复后只比较有无凭据、角色设置、ACL 与内容根。此修订尚未得到第六轮 PG 验收，不得将第五轮误报为加密恢复成功。

第六轮源 55855、目标 55856：角色前置及迁移后分阶段检查、0073 seed/7 块旧文件均通过；shadow 建立目标集群并进入源证据收集，却在正式 `postgres-consistent-backup.py::collect_evidence` 的旧 `0059→0058.verify_catalog` 处报 `budget v10 publish file guard drift`，尚未导出快照或密文。`0058.verify_catalog` 无条件拿五个文件 guard 的 v10 `NEW_SQL` 对最终正文；`0066` 已在安装时先验证这个前驱，再以 v11 `NEW_SQL` 正常替换其中四个，`0070` 验证其中两项的 v11 正文。旧 v10 校验器因此不适用于最终 0073，不可捕获异常当成功，也不可改正式 helper。失败证据在 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\shadow-0073-55855-55856-20260927`；源/目标均停机、双端口无监听、临时密码清理、无明文/密文 shadow 文件。

后续新增**显式 test-only** `protected_ai_shadow_evidence_0073.py`，版本为 `protected-shadow-logical-evidence-0073-v1`。当前 v11 文件目录用既有 `health._verify_promotion_trial_file_guard(..., slim_stage_enabled=True)` 钉五 guard、renderer11 约束、ACL、触发器及 stage 函数，再运行适用的 0067–0073 验证；同一 exported repeatable-read snapshot 中有界计算全部 public 表规范行哈希根、九张 protected 表完整行根、非空 HTML/XLSX 原字节根、全部迁移收据、角色属性/GUC/全表ACL及目录 owner/ACL 根，输出 `shadowContentSha256`。**它不等于正式 backup helper 的 `contentSha256`**，manifest/result 固定 `formalContentSha256Verified=false`、`formalBackupPathVerified=false`；正式 helper仍只用于提前拒绝测试。`pg_dump --snapshot` 的流密文另外记录 plaintext SHA，错误密钥、截断、篡改分别拒绝；第二集群双遍认证并事务恢复后逐根比较。该版本尚未通过完整 PG 验收，也不提供正式备份采用或长期密钥恢复能力。

第七轮源 55857、目标 55858 的 seed 和当前目录门禁通过，test-only collector 在枚举 public 表时因初版 256 表上限拒绝；没有导出快照或密文。两集群均停机、双端口无监听、临时密码清理、明文 dump 为 0。只读 `pg_restore --list` 核精确后 0073 合成 dump 有 **282 张 public 表与 282 项 TABLE DATA**、38 项 SEQUENCE，故该上限确不足；仅将 test-only 表数量上限调到 **320**，完整行数 100 万与规范字节 512 MiB 边界不变，纯测 282 准入/321 拒。失败证据在 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\shadow-0073-55857-55858-20260927`。此修订尚未通过新一轮 PG；不得把旧七轮的 seed、角色或归档片段拼成成功。

第八轮源 55859、目标 55860 已通过 0073 seed、当前目录与 282 表采集、exported RR snapshot、pg_dump→原 stream-v1 AES-GCM、错误密钥/截断/篡改拒绝、目标双遍认证与 `pg_restore --single-transaction`；**最终源/目标根不一致**，所以没有 `status=passed`、manifest 或长期可恢复备份。双集群均停机、双端口无监听、临时密码清理，明文 shadow dump 为 0。加密文件约 2.12 MB、SHA 与失败日志已归档 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\shadow-0073-55859-55860-20260927`；进程内随机密钥未保存，该密文不具长期恢复能力。泛型失败无法指出差异根，后续只增加非敏诊断：分别列出逻辑总根、public 行、九表、文件块、角色属性/密码存在布尔/GUC/全表ACL、owner/约束/函数等目录分根与迁移收据是否相同；若行根不同，仅给首个表名、行数与哈希，不输出原始行、私钥或密码。源与目标端口只在加密上下文而不在互比逻辑根，独立目标角色密码只以存在布尔入根；序列值与目录节可通过分根明确定位。诊断尚未经下一轮 PG 验证，不能据此放宽差异拒绝。

第九轮源 55863、目标 55864 的非敏比较已复现：全 282 张 public 表的 457 行、九张受保护表、文件块、角色属性/密码存在布尔/GUC/全表 ACL 与迁移收据根均相同；目录中 schema、列、函数、序列、触发器也相同，**relations、constraints、indexes 三节不同**，因此总 `shadowContentSha256` 仍拒绝。加密文件约 2.12 MB、SHA 与失败日志在 `E:\codex-artifacts\ai-business-trial-acceptance-20260925\archived-pg\shadow-0073-55863-55864-20260927`，双集群已停、密码文件清理、无明文 dump。当前 relations 使用名称/类型/owner/`relacl::text`/row security；constraints 使用表/约束名/类型/`pg_get_constraintdef`/校验与延迟标志；indexes 使用表/索引名/`pg_get_indexdef`/有效与就绪标志，**没有直接纳入 OID 或 relfilenode**。静态源码不足以断定哪一字段在本次跨集群差异，也不预先忽略任何目录节。只新增每节首个差异对象的类型、`public.<表/对象名>`、各字段相等布尔与不同字段两端 SHA；不输出原定义、ACL 文本或数据。具体字段仍待下一次获准的隔离测试，未据此放宽接受条件。

**剩余接线及验收**：当前 HEAD 的 test-only migration installer 钉在旧源码并拒绝 0074 及以上迁移文件，不能把它在当前源码上的成功虚构成 0073 来源。需先在独立、冻结 0073 源码/迁移清单中取得精确合成 seed，核 `.runtime` 路径和 source/target 端口，串行运行此 shadow，保留退出码、无明文归档、双集群停机和有限证据的 SHA。须真实确认 HTML/XLSX 均存在、私钥初始为空，验证错误密钥、ACL/owner 漂移负例、密钥/文件内容相同与篡改拒绝，并对前 0067 旧备份做独立恢复比较。真实持久密钥托管及受控正式身份设计另需授权与工程实现；在此之前不得打开正式备份/恢复门禁或认定报告已可发布。
