# 吉客云 4098 导出前失败安全关闭

状态：2026-09-23 候选已准备，尚未发布或关闭正式运行。用户要求恢复流程并安全关闭旧任务；涉及应用维护的正式采用仍需本轮明确授权。

## 已核实故障

定时 execution `4098` 于上海时间 00:10:00.032–00:10:04.434 在 `B·接口校验与五表下载` 返回 `API_LOGIN_PAGE_NOT_UNIQUE`。正式运行的 API 导出源码与本次审查代码一致：错误发生在唯一页面检查，早于登录提交及五表 API 导出回调。它表示候选页面数量不等于 1，原错误没有记录数量，不能断言是零页、多个页面或密码失效。

原计划为 `session_api_v1 / exporting`，`exports={}` 且没有 `exportIntent`；浏览器事件、导入运行、校验演练与下载四类路径均不存在。原 active 指针仍指向 4098。共享 helper 已空闲。原共享错误 execution `4099` 等待到上海时间 01:10:04；该错误未被现有人工处理分类命中，等待重试不能绕过旧运行未闭合门禁。

已部署 Worker/helper 为 `20260922T105105Z-d8e06b9d16695c3f`，其恢复校验器不认识本次错误，不能只用候选工具写回执后宣称已恢复。

## 最小恢复变更

复用 `lib/jackyun/preflight-recovery.ts` 与原 `tools/jackyun-preflight-recovery.ts plan/apply` operator，仅增加 4098 的精确审计例外：固定 execution、工作流、节点顺序、错误、请求地址、时间、原 n8n 数据摘要和计划字节摘要。新关闭原因是 `audited_4098_page_selection_before_api_exports`。

仍须持有原全局运行锁、helper 空闲、无活跃吉客云执行、无成功重试、active 身份匹配，以及四类业务效果路径不存在。发布前重新读取证据，仅 create-only 写入关闭回执；原计划、active 与 n8n error 历史不变。新完整执行建立计划时再次复验回执及所有效果路径；原执行拒绝重放。跨日后的新执行使用新日期，不复用失败当天的数据范围。

本补丁不修改登录逻辑、重试分类、00:10 调度或业务导入规则，也不提供通用自动闭合。登录根因及重试错误分类仍需另行处理，不能承诺关闭后下一次导出一定成功。

## 正式采用步骤

1. 按 [启动与发布流程](STARTUP_RELEASE_OPTIMIZATION.md) 准备干净候选，完成正式在线候选构建及部署前备份、独立恢复核验。
2. 得到应用维护授权后，再核验所有在途任务和 4099 当前状态，协调共享执行窗口，经唯一 Worker 生命周期入口维护并采用候选。不得直接修改不可变 release 或删除 active；不重启 n8n、不进行数据库迁移。
3. 服务恢复且 helper 空闲后，用候选 operator 对 4098 重新生成 plan，再以该计划精确摘要 apply。不得盲目使用准备阶段提案；期间若有新 execution、产物或所有者变化，停止并重查。
4. 回读关闭回执，复验原计划/active/n8n 失败摘要未变、四路径仍不存在、正式 helper 包含本次校验器；完成全栈版本、启动绑定、后备份与 E 盘归档。
5. 后续完整业务执行仍由原 n8n 计划或既有安全重试接手。关闭 4098 不是五表导入成功，只有真实 execution 与五表批次独立回查才能验收业务恢复。

## 验证

`tests/jackyun-4098-recovery.test.ts` 验证关闭前阻止/关闭后仅新完整执行可继续、跨日日期、原始字节保留、重复关闭拒绝、旧执行重放拒绝、身份与时间/摘要变化、四类效果路径、迟到产物、策略/批准摘要/回执篡改及共享锁竞争。与既有 preflight 测试合计 27 项通过。独立 Worker 构建、helper 构建和 20 项页面检查通过；后端边界 456 模块无违规，lint 无错误、11 项既有警告。

全量测试详情及本机只读核验见 [候选证据](evidence/jackyun-4098-candidate-20260923.json)。财务静态路由断言及缺失 `color-surface-subtle` 两项失败已在未改动的 `6906267a` 基线独立复现，未作为本次恢复补丁扩大修复范围。
